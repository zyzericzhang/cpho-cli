# Phase 5 User Docs Decisions

User docs are organized by skill, not by CLI/REPL layer. REPL common operations are included inside the relevant chapters because users encounter them while running a skill.

The compose chapter documents the Phase 4 command group and explicitly notes that output paths must stay inside the workspace.

