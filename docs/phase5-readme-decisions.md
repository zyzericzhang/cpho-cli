# Phase 5 README Decisions

The README now describes the implemented Phase 3/4 surface instead of the old Phase 1/eval state. The old `cpho eval` mention was removed because that command has been intentionally removed.

The demo asset is a checked-in SVG terminal transcript under `.github/assets/`. This satisfies the README embedding goal without adding a recorder dependency to the test environment.

The examples section points to repository-local examples first. Public IPhO image assets remain deferred until source/license verification is performed explicitly.
