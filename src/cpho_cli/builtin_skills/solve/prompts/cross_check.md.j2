Derivations:
{{ subproblem_derivations | default('', true) }}

Official answer:
{{ answer_structure | default('', true) }}

OCR warnings:
{{ ocr_warnings | default([], true) }}

Flag discrepancies and likely source.

