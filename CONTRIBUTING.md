# Contributing

欢迎提交 issue 和 PR。

- Bug issue 请包含命令、输入文件类型、期望行为和实际输出。
- Feature request 请说明真实教学/题库场景。
- PR 请保持改动聚焦，并为行为变化补测试。
- 不要提交 API key、真实私有题库或 `.cpho/` 输出。
- 提交前运行 `uv run pytest -q` 和 `uv run ruff check .`。
