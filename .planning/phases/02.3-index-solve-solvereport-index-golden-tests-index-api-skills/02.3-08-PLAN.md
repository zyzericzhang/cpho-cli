---
phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills
plan: "08"
type: execute
wave: 7
depends_on:
  - 02.3-06
  - 02.3-07
files_modified:
  - src/cpho_cli/core/index/builder.py
  - src/cpho_cli/core/index/tagging.py
  - src/cpho_cli/core/index/topic_assignment.py
  - tests/test_index_builder.py
  - tests/test_index_tagging.py
  - tests/test_topic_builder_integration.py
autonomous: true
requirements:
  - D-16
  - D-17
  - D-18
  - D-19
  - D-20
  - D-21
  - SC-01
  - SC-08
must_haves:
  truths:
    - "D-20/SC-08: build_index supports a vision flag and defaults to OCR-only when vision is false."
    - "D-20: vision mode uses shared multimodal routing only when the current model supports the required file/image modality; otherwise it falls back to OCR text with a visible warning/stat."
    - "SC-01 remains true for default indexing: no SolveReport is loaded and default tag normalization uses OCR text plus vocabulary."
    - "D-16/D-17/D-19/D-21: index vision path supports PDFs through OpenRouter file blocks and supported images through image_url blocks without PDF-to-image conversion."
  artifacts:
    - path: src/cpho_cli/core/index/builder.py
      provides: "build_index vision flag and OCR fallback behavior"
      contains: ["vision"]
    - path: src/cpho_cli/core/index/tagging.py
      provides: "vision-aware refine_tags while preserving OCR-only default"
      contains_absent: ["SolveReport", "solve_report_tags"]
    - path: src/cpho_cli/core/index/topic_assignment.py
      provides: "optional vision context for topic assignment with non-blocking fallback"
      contains: ["vision"]
  key_links:
    - from: src/cpho_cli/core/index/builder.py
      to: src/cpho_cli/core/index/tagging.py
      via: "vision/source_file arguments on refine_tags without SolveReport input"
      pattern: "refine_tags"
    - from: src/cpho_cli/core/index/tagging.py
      to: src/cpho_cli/core/multimodal.py
      via: "neutral content-block helper imported from core"
      pattern: "build_multimodal_content"
---

<objective>
Add opt-in multimodal indexing in the core builder/tagging path.

Purpose: Plan 06 makes multimodal available to skills; this plan applies the same shared helper surface to index tagging and topic assignment while preserving deterministic OCR-only default behavior.
Output: build_index vision flag, core tag/topic multimodal calls, and fake-provider tests for default OCR, PDF file blocks, image_url blocks, and fallback.
</objective>

<execution_context>
@/Users/ericzhang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericzhang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-RESEARCH.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
@.planning/phases/02-tag-indexing/02-04-SUMMARY.md
@.planning/phases/02-tag-indexing/02-05-SUMMARY.md
</context>

<must_haves>
See frontmatter `must_haves`; executor must satisfy those truths, artifacts, and key_links before writing the summary.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add vision-aware tag refinement behind an explicit builder flag</name>
  <files>src/cpho_cli/core/index/builder.py, src/cpho_cli/core/index/tagging.py, tests/test_index_builder.py, tests/test_index_tagging.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-RESEARCH.md
    - src/cpho_cli/core/index/builder.py
    - src/cpho_cli/core/index/tagging.py
    - src/cpho_cli/core/multimodal.py
    - tests/test_index_builder.py
    - tests/test_index_tagging.py
  </read_first>
  <behavior>
    - D-20/SC-08: build_index(..., vision=False) uses the existing OCR text path and sends no multimodal content blocks.
    - D-20: build_index(..., vision=True) supplies the problem PDF/image as multimodal context when model capabilities support it.
    - SC-01: neither default nor vision path reads SolveReport or solve_report_path.
  </behavior>
  <action>Extend build_index with vision: bool = False and thread that flag into tag provider calls. Extend refine_tags only as needed to accept optional source_file/source_capabilities and to build messages using the shared content-block helper from Plan 06. Default behavior must continue passing OCR text and vocabulary only. When vision=True but capability detection is text-only or unavailable, record a warning/progress stat and call the existing OCR text path. Tests must prove default index does not send file/image blocks, vision PDF sends file blocks, vision image sends image_url blocks, and text-only capability falls back to OCR text.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_index_builder.py tests/test_index_tagging.py -x` passes.
    - Default build_index tests capture no image_url or file_data blocks.
    - Vision tests capture correct content block type for PDF and image inputs.
    - `rg -n "SolveReport|solve_report_path|TagSource\\.SOLVE_REPORT" src/cpho_cli/core/index/builder.py src/cpho_cli/core/index/tagging.py` prints no matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_index_builder.py tests/test_index_tagging.py -x</automated>
    <automated>rg -n "SolveReport|solve_report_path|TagSource\\.SOLVE_REPORT" src/cpho_cli/core/index/builder.py src/cpho_cli/core/index/tagging.py; test $? -eq 1</automated>
  </verify>
  <done>D-20 and SC-08 are implemented in the core tag path while preserving SC-01 default behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry vision context into topic assignment without blocking index builds</name>
  <files>src/cpho_cli/core/index/builder.py, src/cpho_cli/core/index/topic_assignment.py, tests/test_topic_builder_integration.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
    - src/cpho_cli/core/index/builder.py
    - src/cpho_cli/core/index/topic_assignment.py
    - src/cpho_cli/core/multimodal.py
    - tests/test_topic_builder_integration.py
  </read_first>
  <behavior>
    - Topic assignment remains non-blocking as established in Phase 02 Plan 07.
    - In vision mode, topic assignment can receive the same source_file/capability context used by tag refinement.
    - If topic vision payload construction or provider support fails, index build falls back to existing OCR text topic behavior or keeps topic assignment non-blocking.
  </behavior>
  <action>Extend assign_topic only as needed to accept optional source_file/source_capabilities and reuse the neutral content-block helper. Keep topic failures non-fatal. Update builder integration tests so topic assignment receives vision context when requested and default runs are unchanged.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_topic_builder_integration.py -x` passes.
    - Topic assignment tests prove the default path still sends text-only prompts.
    - Vision-mode tests prove failures remain non-blocking.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_topic_builder_integration.py -x</automated>
    <automated>rg -n "vision|build_multimodal_content" src/cpho_cli/core/index/topic_assignment.py tests/test_topic_builder_integration.py</automated>
  </verify>
  <done>D-20 index vision behavior includes topic assignment without weakening existing non-blocking behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cpho index --vision -> remote provider | User opts into sending PDFs/images from local workspace to OpenRouter-compatible provider. |
| OCR text/file content -> tag/topic prompts | OCR text and file payloads are untrusted model inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-24 | Information Disclosure | build_index vision mode | mitigate | Default remains OCR-only; vision requires explicit flag from later shell plan and fails closed to text when unsupported. |
| T-02.3-25 | Tampering | vision tag normalization | mitigate | Even in vision path, canonical_mapping_pass still validates vocabulary IDs; no SolveReport source is accepted. |
| T-02.3-26 | Denial of Service | topic assignment provider errors | mitigate | Preserve non-blocking topic assignment fallback semantics. |
| T-02.3-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>uv run pytest tests/test_index_builder.py tests/test_index_tagging.py tests/test_topic_builder_integration.py -x</automated>
<automated>rg -n "SolveReport|solve_report_path|TagSource\\.SOLVE_REPORT" src/cpho_cli/core/index/builder.py src/cpho_cli/core/index/tagging.py src/cpho_cli/core/index/topic_assignment.py; test $? -eq 1</automated>
</verification>

<success_criteria>
- SC-08 is complete in core: build_index has an opt-in vision flag and OCR-only default.
- SC-01 remains true after vision integration.
- D-16 through D-21 are represented in index internals without duplicating multimodal helpers in index code.
</success_criteria>

<output>
Create `.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-08-SUMMARY.md` when done.
</output>
