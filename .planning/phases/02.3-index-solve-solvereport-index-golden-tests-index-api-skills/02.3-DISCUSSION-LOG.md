# Phase 02.3: Index 读写分离 + Solve 降级 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-24
**Phase:** 02.3-index-solve-solvereport-index-golden-tests-index-api-skills
**Areas discussed:** SolveReport 解耦策略, 标签读写 API 设计, 标签分离存储 + Provenance, golden_tests 移除范围, solve 降级方式, skill 标签写入权限, index 重建行为, 测试策略

---

## SolveReport 解耦策略

| Option | Description | Selected |
|--------|-------------|----------|
| A | 直接删除耦合代码 | |
| B | 用通用 enrichment hook 替换 | |
| C | 先删后建（先解耦验证基线再实现 API） | ✓ |

**User's choice:** C — 先删除耦合，验证 OCR-only 基线可用，再在同一 phase 实现读写 API。
**Notes:** 用户对 OCR_FALLBACK 路径的标签质量有信心，不需要中间过渡机制。

---

## 标签读写 API 设计

| Option | Description | Selected |
|--------|-------------|----------|
| A | CLI only | |
| B | Python API only | |
| C | Python API 核心 + CLI 薄包装 | ✓ |

**User's choice:** C — `core/index/api.py` 新增写函数，CLI 子命令做薄包装。遵循芯-壳分离。
**Notes:** 写入语义为追加（add），提供显式 `tag-set` 做替换。

---

## 标签分离存储 + Provenance

| Option | Description | Selected |
|--------|-------------|----------|
| A | IndexEntry 新增 user_tags + provenance 字段 | ✓ |
| B | 独立 provenance store | |
| C | 单字段追加模式 | |

**User's choice:** A — `user_tags` 与 `llm_tags` 分离，provenance 记录 skill_name + timestamp + reasoning_snippet。
**Notes:** 重建只覆盖 llm_tags，user_tags 原封不动。

---

## golden_tests 移除范围

| Option | Description | Selected |
|--------|-------------|----------|
| A | 全部删除（含 splitting） | ✓ |
| B | 只删 solve eval，保留 splitting | |
| C | 移入 fixtures | |

**User's choice:** A — `golden_tests/` 整个目录 + `core/eval.py` + `models/eval.py` + CLI `cpho eval` 全部删除。
**Notes:** 后续 test_splitting_golden.py 需适配或删除。

---

## solve 降级方式

| Option | Description | Selected |
|--------|-------------|----------|
| A | 重写为 YAML skill，通过 SkillRuntime 执行 skill.yml | ✓ |
| B | 保留 solve_problem() 但改为通过 skill runtime 调用 | |
| C | 最小移动——移到 builtin_skills/，CLI 改为 cpho run solve | |

**User's choice:** A — solve 真正执行 builtin_skills/solve/skill.yml 的 7 步 DAG，通过 SkillRuntime。solve 是 SkillRuntime 的第一个真实验证案例。
**Notes:** SkillRuntime 可能需要增强以支持实际执行（python_tool handler、LLM handler 对接）。

---

## skill 标签写入权限

| Option | Description | Selected |
|--------|-------------|----------|
| A | 只能从已有 vocabulary 选择 | |
| B | 允许任意标签，非 vocabulary 标记为 unverified | ✓ |
| C | 只能提议 candidate tags（不直接写入） | |

**User's choice:** B — 写入自由，但标记 canonical/unverified 状态。vocabulary 管理员后续可提升 unverified → canonical。
**Notes:** Phase 02.3 只实现数据模型和写入逻辑，不实现提升 UI。

---

## index 重建行为

| Option | Description | Selected |
|--------|-------------|----------|
| A | --force 只重建 llm_tags，user_tags 不动 | ✓ |
| B | --force 重建 llm_tags + 标记 user_tags stale | |
| C | 新增 --force-all 完全重置 | ✓ |

**User's choice:** A + C — `--force` 只覆盖 llm_tags，新增 `--force-all` 完全重置。
**Notes:** 不需要 stale 标记机制——user_tags 是主动写入的，信任其判断。

---

## 测试策略

| Option | Description | Selected |
|--------|-------------|----------|
| A | 纯单元测试（fake providers） | |
| B | 手工验收清单 | |
| C | 保留 eval 框架骨架 | |

**User's choice:** 直接去掉 eval，用户手动运行、手动检查。
**Notes:** 不做自动化回归。Phase 3 会驱动重新设计更通用的 eval 框架。

---

## Claude's Discretion

- user_tags vs llm_tags 在 IndexEntry 中的具体字段结构和命名
- CLI 子命令的具体参数设计
- test_splitting_golden.py 的处理方式
- SkillRuntime 的 handler 实现细节（python_tool handler、LLM handler 与 core/llm.py 对接）

## Deferred Ideas

- `unverified` 标签提升为 canonical 的 UI/审核流程 → Phase 3
- 标签冲突自动合并策略 → 后续 Phase
- vocabulary 可视化管理 UI → 后续 Phase
- 更通用的 skill eval 框架 → Phase 3（由 explain/quiz 驱动）
