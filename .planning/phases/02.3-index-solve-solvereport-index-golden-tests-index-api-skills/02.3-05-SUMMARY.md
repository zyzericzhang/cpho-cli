---
phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills
plan: "05"
subsystem: solve
tags: [skill-runtime, llm, prompts]
requires:
  - phase: 02.3-03
    provides: user tag write API
provides:
  - runtime-backed solve_problem
  - shared llm/python_tool SkillRuntime handlers
  - complete builtin solve prompt template coverage
affects: [solve, cli, skills, runtime]
tech-stack:
  added: []
  patterns: [strict-json llm handler, constrained python_tool transform]
key-files:
  created:
    - src/cpho_cli/core/skill_handlers.py
    - src/cpho_cli/builtin_skills/solve/prompts/answer_structure.md.j2
    - src/cpho_cli/builtin_skills/solve/prompts/discrepancies.md.j2
  modified:
    - src/cpho_cli/core/solve.py
    - src/cpho_cli/builtin_skills/solve/skill.yml
    - src/cpho_cli/builtin_skills/solve/prompts/normalize.md.j2
    - src/cpho_cli/builtin_skills/solve/prompts/derive.md.j2
    - src/cpho_cli/builtin_skills/solve/prompts/cross_check.md.j2
    - src/cpho_cli/builtin_skills/solve/prompts/final_report.md.j2
key-decisions:
  - "solve_problem keeps its public signature but executes the builtin solve skill through SkillRuntime."
  - "python_tool remains a named blackboard transform only; it does not execute dynamic Python."
  - "Final solve_report output is validated as SolveReport before writing reports."
patterns-established:
  - "Shared SkillRuntime handlers live in core/skill_handlers.py."
  - "Each solve LLM step owns a dedicated prompt template."
requirements-completed: [D-12, D-13, D-14, D-15]
duration: 22min
completed: 2026-05-25
---

# Phase 02.3-05 Summary

**`cpho solve` now uses the builtin SkillRuntime path while preserving the existing CLI shape.**

## Performance

- **Duration:** 22 min
- **Tasks:** 3
- **Files modified:** 12

## Accomplishments

- Added `make_llm_handler` and `python_tool_handler` in `core/skill_handlers.py`.
- Routed `solve_problem` through `SkillRuntime` and the builtin `solve` skill.
- Added dedicated prompt templates for answer-structure validation and discrepancy marking.
- Updated solve prompt templates so StrictUndefined rendering matches declared step inputs.
- Added fake-provider tests for handler behavior, prompt rendering, dry-run behavior, and six LLM calls across seven runtime steps.

## Task Commits

1. **Tasks 1-3:** `0217b7b` (`feat(02.3-05): route solve through skill runtime`)

## Verification

- `uv run pytest tests/test_runtime.py tests/test_skills.py tests/test_solve.py -x` passed.
- `uv run cpho solve --help` passed and preserved the `PROBLEM`, `--answer`, `--config`, `--provider`, `--output-dir`, and `--dry-run` surface.
- `uv run python -c "... prompt_template exists ..."` passed.
- `rg -n "eval\\(|exec\\(|subprocess|__import__" src/cpho_cli/core/skill_handlers.py` returned no matches.
- `rg -n "provider\\.complete" src/cpho_cli/core/solve.py` returned no matches.

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

- One initial test assertion expected `Problem text:` while the prompt intentionally says `Problem OCR text:`. The assertion was corrected and tests then passed.

## User Setup Required

None.

## Next Phase Readiness

Notebook/REPL integration can now call the stable index tag-write API and runtime-backed solve path without relying on eval/golden artifacts.

---
*Phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills*
*Completed: 2026-05-25*
