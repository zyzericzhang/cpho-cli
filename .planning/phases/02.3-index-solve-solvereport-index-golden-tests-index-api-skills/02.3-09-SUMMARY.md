---
phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills
plan: "09"
subsystem: cli
tags: [vision, repl, acceptance]
requires:
  - phase: 02.3-04
    provides: eval removal
  - phase: 02.3-08
    provides: core vision indexing
provides:
  - cpho index --vision shell wiring
  - REPL /index --vision and --force-all forwarding
  - phase-level acceptance gates for D-01 through D-21 and SC-01 through SC-08
affects: [cli, repl, tests]
tech-stack:
  added: []
  patterns: [thin shell forwarding, copied real-workspace smoke]
key-files:
  created:
    - tests/test_phase023_acceptance.py
  modified:
    - src/cpho_cli/cli/app.py
    - src/cpho_cli/cli/repl/commands/workspace.py
    - tests/test_index_cli.py
    - tests/test_repl_workspace_commands.py
key-decisions:
  - "CLI/REPL help states vision is opt-in and may upload files to the configured provider."
  - "Acceptance smoke copies real workspace samples into tmp_path and never mutates the original workspace."
patterns-established:
  - "Phase acceptance tests combine source gates with fake-provider smoke tests."
requirements-completed: [D-09, D-15, D-20, SC-01, SC-02, SC-03, SC-04, SC-05, SC-06, SC-07, SC-08]
duration: 24min
completed: 2026-05-25
---

# Phase 02.3-09 Summary

**Vision indexing and force-all are exposed through CLI/REPL shells, with phase acceptance gates in place.**

## Performance

- **Duration:** 24 min
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added `cpho index --vision` and forwarded `vision=True` to `build_index`.
- Kept `--force-all` wired through CLI build paths, including OCR-upgrade retry.
- Added `/index --vision` and `/index --force-all` in the REPL parser and forwarding.
- Added `tests/test_phase023_acceptance.py` covering D-01 through D-21 and SC-01 through SC-08 with source gates and copied real-workspace smoke.
- Verified legacy eval/golden references remain absent without adding literal stale strings to tests.

## Task Commits

1. **Tasks 1-2:** `08b3ba6` (`feat(02.3-09): expose vision indexing`)

## Verification

- `uv run pytest tests/test_index_cli.py tests/test_repl_workspace_commands.py -x` passed.
- `uv run pytest tests/test_phase023_acceptance.py -x` passed.
- `uv run pytest tests/test_index_cli.py tests/test_repl_workspace_commands.py tests/test_phase023_acceptance.py -x` passed.
- `uv run pytest tests/test_phase023_acceptance.py tests/test_index_cli.py tests/test_solve.py -x` passed.
- `uv run cpho index --help` showed `--vision` and `--force-all`.
- `rg -n "D-(0[1-9]|1[0-9]|2[0-1])|SC-0[1-8]" tests/test_phase023_acceptance.py` found the expected decision/success labels.
- `rg -n "SolveReport|solve_report_path|TagSource\\.SOLVE_REPORT" src/cpho_cli/core/index src/cpho_cli/models/index.py` returned no matches.
- `rg -n "cpho eval|core\\.eval|models\\.eval|golden_tests" src tests pyproject.toml` returned no matches.

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

None.

## User Setup Required

Use `--vision` only when the configured provider/model is acceptable for uploading local PDFs/images.

## Next Phase Readiness

Final verification can run the whole focused suite plus a small preserved real-workspace index smoke and write the Chinese verification manual.

---
*Phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills*
*Completed: 2026-05-25*
