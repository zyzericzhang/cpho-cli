---
phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills
plan: "04"
subsystem: eval
tags: [cli, fixtures, cleanup]
requires:
  - phase: 02.3-03
    provides: user tag write API
provides:
  - CLI with no eval command
  - repository without core/models eval modules
  - splitting fixture relocated to tests/fixtures/splitting
affects: [cli, tests, splitting]
tech-stack:
  added: []
  patterns: [fixture migration before deletion]
key-files:
  created:
    - tests/fixtures/splitting/ipho_style_multi_problem.expected.json
    - tests/fixtures/splitting/ipho_style_multi_problem.pdf
    - tests/fixtures/splitting/ipho_style_multi_problem_answers.pdf
  modified:
    - src/cpho_cli/cli/app.py
    - tests/test_cli.py
    - tests/test_splitting_golden.py
  deleted:
    - src/cpho_cli/core/eval.py
    - src/cpho_cli/models/eval.py
    - tests/test_eval.py
    - golden_tests
key-decisions:
  - "Deleted the eval surface instead of replacing it with another golden/eval framework."
  - "Kept the splitting regression data because it validates deterministic paper splitting without live LLM calls."
patterns-established:
  - "Splitting regression fixtures live under tests/fixtures/splitting."
requirements-completed: [D-10, D-11, D-15, SC-03]
duration: 8min
completed: 2026-05-25
---

# Phase 02.3-04 Summary

**The unverified eval/golden framework is removed, with the useful splitting fixture preserved.**

## Performance

- **Duration:** 8 min
- **Tasks:** 2
- **Files modified:** 12

## Accomplishments

- Moved `ipho_style_multi_problem` splitting fixture files from `golden_tests/splitting` to `tests/fixtures/splitting`.
- Updated `tests/test_splitting_golden.py` to load fixtures from the new test fixture path.
- Removed `cpho eval`, `core/eval.py`, `models/eval.py`, and `tests/test_eval.py`.
- Deleted the remaining `golden_tests` tree.
- Updated CLI tests to assert `eval` is unavailable.

## Task Commits

1. **Tasks 1-2:** `8af5a7b` (`feat(02.3-04): remove eval golden framework`)

## Verification

- `uv run pytest tests/test_cli.py tests/test_splitting_golden.py -x` passed.
- `uv run cpho --help` passed and listed no `eval` command.
- `test ! -d golden_tests` passed.
- `rg -n "cpho eval|core\\.eval|models\\.eval|golden_tests" src tests pyproject.toml` returned no matches.

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

None.

## User Setup Required

None.

## Next Phase Readiness

The solve pipeline can now be demoted to SkillRuntime without depending on eval/golden artifacts.

---
*Phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills*
*Completed: 2026-05-25*
