---
phase: 01-core-foundation
plan: "05"
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - src/cpho_cli/core/eval.py
  - src/cpho_cli/cli/app.py
  - src/cpho_cli/models/eval.py
  - golden_tests/README.md
  - golden_tests/sample_mechanics/spec.yml
  - golden_tests/sample_mechanics/EXPECTATION.md
  - tests/test_eval.py
  - tests/test_phase1_e2e.py
autonomous: true
requirements:
  - CORE-05
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
user_setup:
  - "Real 20-30 problem golden suite requires user-provided copyrighted/private physics PDFs and answer keys. This plan creates the runner and 3-5 starter spec slots; the user supplies real source files when available."
must_haves:
  truths:
    - "CORE-05/D-14/D-16: golden evaluation supports manual-first criteria and records human-defined expectations."
    - "CORE-05/D-15: each golden case is stored as per-problem YAML plus optional `EXPECTATION.md`."
    - "CORE-05/D-17: both `uv run pytest` and `uv run cpho eval golden_tests/` run the evaluation path."
    - "CORE-05/D-18: starter suite supports 3-5 manual cases and can scale to 20-30 regression cases."
    - "Phase goal: an end-to-end mocked or fixture-backed regression proves config, discovery, OCR, solve, and report generation connect."
  artifacts:
    - src/cpho_cli/core/eval.py
    - src/cpho_cli/models/eval.py
    - golden_tests/
    - tests/test_eval.py
    - tests/test_phase1_e2e.py
  key_links:
    - "`cpho eval golden_tests/` -> eval runner -> solve pipeline -> per-problem pass/fail report"
    - "per-problem `spec.yml` -> Pydantic EvalCase model -> criteria checks"
    - "pytest end-to-end test -> mocked OCR/LLM -> report criteria assertions"
---

<objective>
Create the golden evaluation runner and Phase 1 end-to-end regression gate.

Purpose: This plan makes output quality regression visible through a repeatable command and pytest integration.
Output: Per-problem YAML spec model, eval command, starter golden_tests structure, and an end-to-end regression test.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-PATTERNS.md
@.planning/phases/01-core-foundation/01-04-SUMMARY.md
</context>

<threat_model>
ASVS level: 1
Block on severity: high

Threats:
- MEDIUM: Golden tests may contain copyrighted/private problem PDFs or full answer keys.
- MEDIUM: Evaluation outputs may include model outputs with sensitive local file contents.
- LOW: LLM judge output may be mistaken for authoritative pass/fail.

Required mitigations:
- Commit only sample placeholder fixtures or user-approved public samples.
- Add `.gitignore` coverage for local golden source PDFs and eval outputs.
- Label LLM judge results as advisory; human-defined criteria remain source of truth.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement per-problem golden spec model and loader</name>
  <files>src/cpho_cli/models/eval.py, src/cpho_cli/core/eval.py, golden_tests/README.md, golden_tests/sample_mechanics/spec.yml, golden_tests/sample_mechanics/EXPECTATION.md, tests/test_eval.py</files>
  <read_first>src/cpho_cli/core/config.py, src/cpho_cli/models/solve.py, .planning/phases/01-core-foundation/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: a `spec.yml` with criteria loads into `EvalCase`.
    - Test 2: invalid priority or missing criteria raises an eval config error.
    - Test 3: `EXPECTATION.md` can be read as human guidance but is not required for machine parsing.
  </behavior>
  <action>Implement D-14/D-15/D-16/D-18 eval data contracts. Define `EvalCriterion` with `id`, `area`, `priority`, and `expectation`; define `EvalCase` with problem path, answer path, expected report assertions, criteria list, known failure modes, and optional expectation markdown path. Implement `load_eval_cases(root: Path) -> list[EvalCase]` that scans child directories for `spec.yml`, uses `yaml.safe_load`, validates with Pydantic, and returns deterministic ordering. Create `golden_tests/README.md` explaining manual-first workflow and starter 3-5 case growth toward 20-30 regression cases. Use placeholder sample paths in `sample_mechanics/spec.yml`; do not add copyrighted PDFs.</action>
  <verify>
    <automated>uv run pytest tests/test_eval.py::test_load_eval_case tests/test_eval.py::test_invalid_eval_case_fails -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/cpho_cli/models/eval.py` defines `EvalCriterion` and `EvalCase`.
    - `golden_tests/sample_mechanics/spec.yml` includes at least three criteria with ids.
    - `golden_tests/README.md` states LLM judge is advisory and human criteria are authoritative.
    - Tests prove invalid specs fail before solve execution.
  </acceptance_criteria>
  <done>Golden cases have a stable YAML format ready for manual-first regression tracking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `cpho eval golden_tests/` runner</name>
  <files>src/cpho_cli/core/eval.py, src/cpho_cli/cli/app.py, tests/test_eval.py, tests/test_cli.py</files>
  <read_first>src/cpho_cli/core/solve.py, src/cpho_cli/models/eval.py, src/cpho_cli/cli/app.py, .planning/phases/01-core-foundation/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: mocked eval run returns per-problem PASS/FAIL counts.
    - Test 2: `cpho eval --help` exits 0 and documents the golden root argument.
    - Test 3: missing files are reported per case without crashing the whole run.
  </behavior>
  <action>Implement D-17 eval runner. Add `run_eval(root: Path, config_path: Path | None, output_dir: Path, dry_run: bool = False) -> EvalRunResult`. For each loaded `EvalCase`, call `solve_problem` unless `dry_run` is true, evaluate report JSON against explicit criteria/assertions, and write `eval-report.json` plus a Markdown summary with per-problem status, failed criteria, skipped cases, and output paths. Update Typer `eval` command to call `run_eval` and print aggregate counts. Keep LLM judge out of Phase 1 automation unless a future skill explicitly adds it.</action>
  <verify>
    <automated>uv run pytest tests/test_eval.py tests/test_cli.py -q && uv run cpho eval --help</automated>
  </verify>
  <acceptance_criteria>
    - `uv run cpho eval --help` exits 0.
    - Eval result model includes `total`, `passed`, `failed`, and `skipped`.
    - Markdown report includes every case id from loaded specs.
    - Dry-run does not call OpenRouter.
  </acceptance_criteria>
  <done>`cpho eval golden_tests/` provides the user-facing regression command required by CORE-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 1 end-to-end regression gate</name>
  <files>tests/test_phase1_e2e.py, tests/test_eval.py, tests/test_solve.py</files>
  <read_first>src/cpho_cli/core/solve.py, src/cpho_cli/core/eval.py, src/cpho_cli/models/solve.py, .planning/ROADMAP.md</read_first>
  <behavior>
    - Test 1: mocked end-to-end run starts from a problem file and answer key and produces a report with answer references.
    - Test 2: low-confidence OCR blocks appear in the final report warnings.
    - Test 3: eval reports FAIL when a required criterion is absent from the solve report.
  </behavior>
  <action>Implement the final CORE-01 through CORE-05 regression. Create a fixture-backed test that uses temporary problem/answer files, mocked document/OCR provider output, mocked OpenRouter responses, the built-in solve skill, and the eval runner. Assert the final report contains derivation steps, official-answer references, discrepancy list, OCR warnings, and tags. Add a failure test where a criterion references `official_answer_references` and fails when the mocked report omits it.</action>
  <verify>
    <automated>uv run pytest tests/test_phase1_e2e.py tests/test_eval.py tests/test_solve.py -q && uv run pytest</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase1_e2e.py` covers solve output with mocked OCR and mocked LLM.
    - `tests/test_phase1_e2e.py` asserts OCR low-confidence warnings are visible.
    - `uv run pytest` exits 0.
    - `uv run ruff check .` and `uv run mypy .` exit 0 after the full phase.
  </acceptance_criteria>
  <done>The phase has a single automated regression proving all Phase 1 layers are connected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest` succeeds.
- [ ] `uv run cpho eval --help` succeeds.
- [ ] `uv run cpho eval golden_tests/ --dry-run` succeeds.
- [ ] `uv run ruff check .` succeeds.
- [ ] `uv run mypy .` succeeds.
</verification>

<success_criteria>
- CORE-05 is implemented with pytest and `cpho eval golden_tests/`.
- Phase 1 has an end-to-end regression gate over config, discovery, OCR, solve, and report generation.
- The golden suite starts small but has a clear path to the roadmap's 20-30 problem regression set.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-05-SUMMARY.md`.
</output>

