---
phase: 04-related-problems-compose-exceptions
plan: 04-02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/cpho_cli/core/related.py
  - src/cpho_cli/cli/repl/session.py
  - src/cpho_cli/cli/repl/commands/related.py
  - src/cpho_cli/cli/repl/commands/__init__.py
  - tests/test_related.py
  - tests/test_repl_related_commands.py
  - docs/phase4-related-decisions.md
autonomous: true
requirements:
  - SKILL-RELATED
---

# 04-02 Plan: Related Problems Skill

## Objective

Wrap `find_related_problems()` as a user-facing service and REPL command with markdown export and `SessionState.last_related`.

## Tasks

1. Add `core.related.find_related_report(...)` returning scored related rows and writing markdown.
   - Verify: seeded index tests assert score order, max results, min shared tags, and markdown content.
2. Add `SessionState.last_related` and `/search-related <problem_id> [--top N] [--min-shared N]`.
   - Verify: REPL tests assert table output and `last_related` persistence.
3. Preserve explicit downstream handoff: no compose auto-injection except via later `--from last-related`.
   - Verify: related command does not write composition files.

## Acceptance

- `/search-related` works from current problem or explicit id.
- Results include problem id, score, topic, source, and top tags.
- Machine tags are never mutated.

