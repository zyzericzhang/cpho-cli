---
phase: 04-related-problems-compose-exceptions
plan: 04-05
type: execute
wave: 5
depends_on:
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - src/cpho_cli/cli/app.py
  - src/cpho_cli/cli/repl/commands/compose.py
  - src/cpho_cli/cli/repl/commands/__init__.py
  - tests/test_compose_cli.py
  - tests/test_repl_compose_commands.py
  - docs/phase4-compose-cli-decisions.md
autonomous: true
requirements:
  - SKILL-COMPOSE
  - SKILL-RELATED
---

# 04-05 Plan: Compose CLI and REPL Integration

## Objective

Expose composition creation, automatic selection, and PDF assembly through CLI and REPL while preserving explicit `last_related` handoff.

## Tasks

1. Extend `cpho compose` into subcommands: `new`, `build`, and `auto`.
   - Verify: Typer tests cover help, stub creation, build output paths, and auto no-match errors.
2. Add REPL `/compose` command with equivalent `new/build/auto` actions.
   - Verify: REPL tests cover current workspace, `--from last-related`, and output messages.
3. Use boundary checks for user-supplied composition/output paths.
   - Verify: CLI/REPL tests reject paths outside workspace.

## Acceptance

- Existing topic compose behavior is either preserved behind compatible flags or intentionally documented as superseded.
- Related results are never consumed implicitly.

