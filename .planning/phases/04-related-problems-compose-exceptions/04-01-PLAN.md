---
phase: 04-related-problems-compose-exceptions
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cpho_cli/core/boundary.py
  - src/cpho_cli/core/runtime.py
  - src/cpho_cli/models/runtime.py
  - tests/test_boundary.py
  - tests/test_runtime.py
  - docs/phase4-boundary-decisions.md
autonomous: true
requirements:
  - ROBUST-BOUNDARY
---

# 04-01 Plan: Boundary and Runtime Checkpoint Foundation

## Objective

Add shared workspace boundary checks and step-level runtime checkpoints so later Phase 4 skills can fail clearly and recoverably.

## Tasks

1. Add `core.boundary.ensure_in_workspace(workspace, path)` and `ensure_workspace_available(workspace)`.
   - Verify: tests cover inside path, traversal path, symlink-resolved outside path, and missing workspace.
2. Extend runtime checkpoint records so successful step checkpoints can be written after each passed step without changing handler APIs.
   - Verify: runtime test asserts checkpoint files exist for passed steps and failed step.
3. Document the decision that `resolve()`-based comparison is the v1 soft-link policy.
   - Verify: `test -s docs/phase4-boundary-decisions.md`.

## Acceptance

- User path errors are Chinese and explicit.
- Existing runtime tests still pass.
- No Phase 4 skill code is introduced in this foundation slice.

