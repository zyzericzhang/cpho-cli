---
phase: 08-community-kb-error-handling
plan: 08-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cpho_cli/models/community.py
  - src/cpho_cli/core/community_sync.py
  - tests/test_community_sync.py
autonomous: true
requirements:
  - KB-05
---

<objective>
Implement GitHub release tarball community KB sync into a read-only cache.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Community sync core</name>
  <files>src/cpho_cli/models/community.py, src/cpho_cli/core/community_sync.py, tests/test_community_sync.py</files>
  <action>Add community config loading, GitHub release tarball download, extraction, basic knowledge frontmatter validation, metadata writing, read-only chmod, idempotent skip, and force refresh.</action>
  <verify><automated>uv run pytest tests/test_community_sync.py -q</automated></verify>
  <acceptance_criteria>MockTransport tests prove sync writes only cache directory and never private workspace knowledge.</acceptance_criteria>
</task>
</tasks>

<verification>
Run `uv run pytest tests/test_community_sync.py -q`.
</verification>

<success_criteria>
Community KB sync can populate `~/.cache/cpho/community-kb/<repo-name>/` from pinned GitHub releases.
</success_criteria>

<output>
Create `.planning/phases/08-community-kb-error-handling/08-01-SUMMARY.md` when done.
</output>

