---
phase: 08-community-kb-error-handling
plan: 08-02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/cpho_cli/core/knowledge/resolver.py
  - src/cpho_cli/cli/app.py
  - tests/test_community_sync.py
  - docs/user/community-kb.md
autonomous: true
requirements:
  - KB-05
---

<objective>
Expose community sync through CLI and ensure `KnowledgeResolver` consumes synced community KB after private matches.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: CLI sync and resolver env hook</name>
  <files>src/cpho_cli/cli/app.py, src/cpho_cli/core/knowledge/resolver.py, tests/test_community_sync.py</files>
  <action>Add `cpho knowledge sync`; use `CPHO_COMMUNITY_KB_DIR` override for tests; ensure resolver returns private before community and includes repo_name.</action>
  <verify><automated>uv run pytest tests/test_community_sync.py -q</automated></verify>
  <acceptance_criteria>CLI sync succeeds with mocked transport/core call and resolver reads community matches.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Community KB user docs</name>
  <files>docs/user/community-kb.md, docs/user/README.md, tests/test_docs_user.py</files>
  <action>Document config file format, pinned releases, cache location, read-only behavior, and private > community priority.</action>
  <verify><automated>uv run pytest tests/test_docs_user.py -q</automated></verify>
  <acceptance_criteria>Docs do not imply sync writes private KB.</acceptance_criteria>
</task>
</tasks>

<verification>
Run `uv run pytest tests/test_community_sync.py tests/test_docs_user.py -q`.
</verification>

<success_criteria>
Users can sync community KB and resolver can consume it safely.
</success_criteria>

<output>
Create `.planning/phases/08-community-kb-error-handling/08-02-SUMMARY.md` when done.
</output>

