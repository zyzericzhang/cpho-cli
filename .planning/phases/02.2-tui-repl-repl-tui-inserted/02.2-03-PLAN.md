---
phase: 02.2-tui-repl-repl-tui-inserted
plan: 03
type: execute
wave: 3
depends_on:
  - 02.2-02
files_modified:
  - src/cpho_cli/cli/repl/commands/workspace.py
  - src/cpho_cli/cli/repl/session.py
  - tests/test_repl_workspace_commands.py
autonomous: true
requirements:
  - TUI-01
  - TUI-02
  - TUI-04
must_haves:
  truths:
    - "`/workspace <path>` switches active workspace, re-validates via `discover_workspace`, reloads index_meta, and persists last_workspace."
    - "`/status` prints workspace path, index 题数/标签数 (or '未建立'), active provider, max_results, output_format — all 中文."
    - "`/config` prints active_provider, model name, list of configured provider profile names (no secrets)."
    - "`/index --dry-run` runs `build_index(workspace, dry_run=True, ...)` and prints the preview stats block from `cli/app.py:154-183`; the user then sees a 中文 confirmation prompt before any real `build_index` call (D-20)."
    - "`/index` without `--dry-run` first emits the dry-run preview, asks `确认执行? [y/N]`, and only runs `build_index(dry_run=False, ...)` on `y` (D-20)."
    - "`/reload-index` re-reads index metadata into `session.index_meta` and refreshes the tag completion cache hook (Plan 04 owns the cache; Plan 03 just calls a thin reset function it imports)."
    - "`/resume` loads `.cpho/repl_state.json` from the active workspace, validates index_mtime/index_version match; on mismatch restores workspace+history but discards last_search_query/result_ids/current_problem_id with a 中文 提示 (D-23)."
    - "Every command that depends on `core` APIs catches `(ConfigError, IndexBuildError, IndexNotFoundError, VocabularyError)` and calls `self._cmd.perror` then returns — REPL never exits on user errors (D-21)."
  artifacts:
    - path: "src/cpho_cli/cli/repl/commands/workspace.py"
      provides: "WorkspaceCommandSet with do_workspace, do_status, do_config, do_index, do_reload_index, do_resume"
      exports: ["WorkspaceCommandSet"]
    - path: "src/cpho_cli/cli/repl/session.py"
      provides: "save_session_state(session, workspace_root) + load_session_state(workspace_root, config) helpers for /resume"
      exports: ["save_session_state", "load_session_state"]
  key_links:
    - from: "src/cpho_cli/cli/repl/commands/workspace.py"
      to: "cpho_cli.core.index.build_index"
      via: "WorkspaceCommandSet.do_index calls build_index twice (dry_run=True, then dry_run=False after confirmation)"
      pattern: "build_index\\("
    - from: "src/cpho_cli/cli/repl/commands/workspace.py"
      to: "cpho_cli.core.workspace.discover_workspace"
      via: "WorkspaceCommandSet.do_workspace validation"
      pattern: "discover_workspace\\("
    - from: "src/cpho_cli/cli/repl/commands/workspace.py"
      to: "Plan 04 tag completion cache"
      via: "imports and calls `refresh_tag_cache(session)` (Plan 04 provides; Plan 03 calls). Defined in `src/cpho_cli/cli/repl/commands/search.py` to keep cache co-located with its consumers."
      pattern: "refresh_tag_cache"
---

<objective>
Implement the workspace-and-index family of slash commands. After this plan, a user can boot the REPL, switch workspaces, inspect status, run `/index --dry-run` with cost preview + confirmation, reload index metadata, and resume a prior session — but `/search` and `/show` (Plan 04) are still empty.

Purpose: Concentrate every command that mutates session.workspace_path or touches build_index into one CommandSet so Plan 04 can focus purely on read paths. D-19 (index-missing message) is reused by Plan 04 but the message string is defined here as a shared constant.
Output: Fully wired WorkspaceCommandSet; session.py gains save/load helpers for the per-workspace resume state; one test file with ≥ 8 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-PATTERNS.md
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-02-PLAN.md
@AGENTS.md
@src/cpho_cli/cli/app.py
@src/cpho_cli/cli/repl/app.py
@src/cpho_cli/cli/repl/session.py
@src/cpho_cli/cli/repl/commands/workspace.py
@src/cpho_cli/core/index/__init__.py
@src/cpho_cli/core/workspace.py
@src/cpho_cli/core/config.py

<interfaces>
<!-- Core APIs this plan consumes (read these signatures verbatim) -->

From src/cpho_cli/core/index/__init__.py (re-export surface):
```python
from cpho_cli.core.index import (
    IndexBuildError,
    IndexNotFoundError,
    OcrUpgradeDecisionRequired,
    VocabularyError,
    build_index,
)
```

From src/cpho_cli/core/index/builder.py (signature seen in cli/app.py:115):
```python
stats = build_index(
    workspace,
    config_path=config,
    provider_name=provider,
    force=force,
    only_new=only_new,
    dry_run=dry_run,
    ocr_strategy=ocr_strategy,
)
```

From src/cpho_cli/core/workspace.py:73:
```python
def discover_workspace(root: Path) -> WorkspaceDiscoveryResult: ...
```
Returns object with `pairs`, `unmatched_problems`, `ambiguous` attributes (see core/workspace.py).

cmd2 argparse + confirmation idioms:
```python
import argparse
import cmd2

index_parser = cmd2.Cmd2ArgumentParser(description="(re)build the workspace index")
index_parser.add_argument("--dry-run", action="store_true", help="只预览待处理工作量")
index_parser.add_argument("--force", action="store_true", help="忽略 fingerprint 强制重建")
index_parser.add_argument("--only-new", action="store_true", help="仅索引新增题目")
index_parser.add_argument("--ocr-strategy", choices=("prompt", "reuse", "rebuild", "new-only"), default="prompt")

class WorkspaceCommandSet(cmd2.CommandSet):
    @cmd2.with_argparser(index_parser)
    def do_index(self, ns: argparse.Namespace) -> None:
        ...
        confirm = self._cmd.read_input("确认执行? [y/N] ").strip().lower()
        if confirm != "y":
            self._cmd.poutput("已取消。")
            return
```

`/resume` state file path:
- Per-workspace: `<workspace_root>/.cpho/repl_state.json`
- Plan 02 reserved `STATE_REL_PATH = Path(".cpho/repl_state.json")` in session.py.
- save_session_state writes `session.to_persistable_dict()` as JSON.
- load_session_state reads JSON, calls `SessionState.from_persistable_dict(...)`. If `index_mtime_ns` or `index_version` mismatches current `load_index_meta(workspace).index_mtime_ns/version`, the loader returns the SessionState with `last_search_query=None, last_search_result_ids=[], current_problem_id=None` AND a `stale_reason: str` companion field that the `/resume` command surfaces.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend session.py with save_session_state / load_session_state helpers</name>
  <read_first>
    - src/cpho_cli/cli/repl/session.py (Plan 02 baseline — SessionState + to_persistable_dict + load_index_meta)
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md (D-22, D-23, D-24)
  </read_first>
  <files>src/cpho_cli/cli/repl/session.py</files>
  <action>
    Add to `src/cpho_cli/cli/repl/session.py`:

    1. Constant near top of module: `RESUME_STATE_REL_PATH = Path(".cpho/repl_state.json")` (replaces or aliases the `STATE_REL_PATH` placeholder from Plan 02).

    2. `@dataclass class ResumeResult:` with fields `session: SessionState`, `stale_reason: str | None`. `stale_reason` is set to a 中文 message when index_mtime/index_version mismatch.

    3. `def save_session_state(session: SessionState) -> Path`:
       - target = `session.workspace_path / RESUME_STATE_REL_PATH`
       - Ensure parent dir exists (`target.parent.mkdir(parents=True, exist_ok=True)`).
       - Write `json.dumps(session.to_persistable_dict(), ensure_ascii=False, indent=2)` to target via atomic temp+rename (write to `target.with_suffix(".json.tmp")` then `os.replace`).
       - Return target path.
       - On OSError, raise `RuntimeError(f"无法保存会话状态: {exc}")` — caller (`/resume` or app shutdown) catches.

    4. `def load_session_state(workspace_root: Path, *, config: AppConfig) -> ResumeResult | None`:
       - target = `workspace_root / RESUME_STATE_REL_PATH`
       - If not target.exists(), return `None`.
       - Read JSON; rebuild via `SessionState.from_persistable_dict(data, config=config)`. If `from_persistable_dict` raises `ValueError("已保存的 workspace 不存在")`, return `None` (caller falls back).
       - Compute current `meta = load_index_meta(workspace_root)`. The saved dict has `index_mtime_ns` and `index_version` fields. If `meta is None` OR `meta.index_mtime_ns != data.get("index_mtime_ns")` OR `meta.index_version != data.get("index_version")`:
         - Reset `session.last_search_query = None`, `session.last_search_result_ids = []`, `session.current_problem_id = None`.
         - `stale_reason = "索引已更新（mtime/version 变化），已丢弃过期搜索/题目上下文，请重新 /search"` (per D-23).
       - Else `stale_reason = None`.
       - Set `session.index_meta = meta` either way.
       - Return `ResumeResult(session=session, stale_reason=stale_reason)`.

    5. Augment `SessionState.to_persistable_dict()` to also write `"index_mtime_ns"` and `"index_version"` at top level (Plan 02 already mandated this — verify the helper emits these keys; if not, add them now).
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_session.py -x -q 2>&1 | tail -10 ; uv run python -c "from cpho_cli.cli.repl.session import save_session_state, load_session_state, ResumeResult; print('ok')"</automated>
  </verify>
  <done>
    - `save_session_state`, `load_session_state`, `ResumeResult` exported from `session.py`
    - Stale-index detection sets `stale_reason` and resets search context per D-23
    - JSON write is atomic (temp + rename) so an interrupted save does not corrupt the file
    - Existing `tests/test_repl_session.py` from Plan 02 still passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement WorkspaceCommandSet (/workspace, /status, /config, /index, /reload-index, /resume)</name>
  <read_first>
    - src/cpho_cli/cli/app.py (lines 87-183 — full index_command analog including OcrUpgradeDecisionRequired branch)
    - src/cpho_cli/cli/repl/commands/workspace.py (Plan 01 empty stub)
    - src/cpho_cli/cli/repl/session.py (Tasks 1 + Plan 02 helpers)
    - src/cpho_cli/core/workspace.py (discover_workspace + WorkspaceDiscoveryResult)
    - src/cpho_cli/core/config.py (load_config, find_default_config_path)
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md (D-18, D-19, D-20, D-21, D-23)
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-PATTERNS.md (workspace.py section, lines 235-293)
  </read_first>
  <files>src/cpho_cli/cli/repl/commands/workspace.py</files>
  <action>
    Replace the empty stub in `src/cpho_cli/cli/repl/commands/workspace.py` with the full CommandSet. Structure:

    1. Imports:
       ```python
       from __future__ import annotations
       import argparse
       from pathlib import Path
       import cmd2
       from cmd2 import Cmd2ArgumentParser, with_argparser
       from cpho_cli.core.config import ConfigError, load_config
       from cpho_cli.core.index import (
           IndexBuildError,
           IndexNotFoundError,
           OcrUpgradeDecisionRequired,
           VocabularyError,
           build_index,
       )
       from cpho_cli.core.workspace import discover_workspace
       from cpho_cli.cli.repl.session import (
           load_index_meta,
           load_session_state,
           save_last_workspace,
           save_session_state,
       )
       ```
       Module-level constant: `INDEX_MISSING_MSG = "未找到索引，请先运行 cpho index --workspace {workspace}"` (D-19 — exported and reused by Plan 04's `/search`/`/show`).

    2. `WorkspaceCommandSet(cmd2.CommandSet)` with these `do_*` methods (each preceded by a `Cmd2ArgumentParser` defined at module level):

       a. `do_workspace(self, ns)`: argparse with positional `path: str | None` (default None — shows current workspace if omitted). If `path is None`, print `self._cmd.session.workspace_path`. Else: `new = Path(path).expanduser().resolve()`. If not `new.exists()`, `self._cmd.perror(f"路径不存在: {new}")` and return. Wrap `discover_workspace(new)` in `try/except OSError`. Update `self._cmd.session.workspace_path = new`. Reload index_meta via `load_index_meta(new)`; set `self._cmd.session.index_meta` to result (None ok). Call `save_last_workspace(new)`. Call `_refresh_tag_cache_if_available(self._cmd)` (helper that does `try: from cpho_cli.cli.repl.commands.search import refresh_tag_cache; refresh_tag_cache(self._cmd) except ImportError: pass` — Plan 04 provides `refresh_tag_cache`; if not present, no-op gracefully). Print 中文 confirmation: `f"已切换到工作空间: {new}"` and one-line summary `f"配对试卷 {len(pairs)} 套，未配对 {len(unmatched)} 套" if discover succeeded`.

       b. `do_status(self, ns)`: argparse with no args. Render multi-line:
          ```
          工作空间: {session.workspace_path}
          索引: {题数 X / 标签 Y or '未建立'}
          LLM provider: {session.config.active_provider}
          模型: {session.config.model.name or '未配置'}
          max_results: {session.max_results}
          output_format: {session.output_format}
          ```
          Use `self._cmd.poutput("\n".join(...))`.

       c. `do_config(self, ns)`: argparse with no args (read-only — write goes via `set` Settable per D-10). Render `active_provider`, `model.name`, `model.temperature`, and a list of configured provider profile keys (`list(session.config.providers.keys())`). Never print api_key fields. Mirror 中文 style of `do_status`.

       d. `do_index(self, ns)`: argparse with flags `--dry-run`, `--force`, `--only-new`, `--ocr-strategy` (choices = `_OCR_STRATEGY_CHOICES = ("prompt", "reuse", "rebuild", "new-only")`). Behavior per D-20:
          - Step A (always): call `build_index(workspace, config_path=None, provider_name=None, force=ns.force, only_new=ns.only_new, dry_run=True, ocr_strategy=ns.ocr_strategy)`. (config_path/provider stay None — REPL inherits session.config; if needed in Phase 3 we'll add explicit overrides.) Wrap in `try/except (ConfigError, IndexBuildError, VocabularyError) as exc: self._cmd.perror(str(exc)); return`. Also catch `OcrUpgradeDecisionRequired` — mirror the a/b/c/d prompt block from `cli/app.py:124-150` but using `self._cmd.read_input` instead of `typer.prompt`. On invalid choice → `self._cmd.perror` + return (do NOT exit REPL).
          - Print the same preview block as `cli/app.py:154-183` but prefixed with `"[预览] "` so the user knows nothing has been written. Use `self._cmd.poutput`.
          - Step B: if `ns.dry_run` was True, stop here. Else, prompt `confirm = self._cmd.read_input("确认执行? [y/N] ").strip().lower()`. If `confirm != "y"`, print `"已取消。"` and return. Then call `build_index(..., dry_run=False, ...)` with same kwargs (force/only_new/ocr_strategy carry through; do NOT re-prompt for OcrUpgradeDecisionRequired in the real run because we already resolved it in Step A — pass the resolved `ocr_strategy` and `force` from that branch). Re-print stats (without `[预览]` prefix). On success, refresh `session.index_meta = load_index_meta(workspace)` and call `_refresh_tag_cache_if_available(self._cmd)`.

       e. `do_reload_index(self, ns)`: argparse with no args. `session.index_meta = load_index_meta(session.workspace_path)`. Print `f"已刷新索引元数据：题数 {meta.problem_count} / 标签 {meta.tag_count}"` or `"索引未建立"` if `meta is None`. Call `_refresh_tag_cache_if_available(self._cmd)`. Wrap loader in `try/except RuntimeError` → `self._cmd.perror`.

       f. `do_resume(self, ns)`: argparse with no args. Call `result = load_session_state(session.workspace_path, config=session.config)`. If `result is None`: `self._cmd.poutput("当前工作空间无可恢复会话。")` and return. Else: replace `self._cmd.session` fields one by one (`workspace_path`, `last_search_query`, `last_search_result_ids`, `current_problem_id`, `max_results`, `output_format`, `index_meta`) — do NOT swap the whole object (Settable bindings hold a reference to the original SessionState). Print 中文 summary: `f"已恢复会话: workspace={ws}, 上次搜索='{q}' (结果 {n} 道), 当前题目={pid or '无'}"`. If `result.stale_reason`, also `self._cmd.pwarning(result.stale_reason)`.

    3. Add a top-level helper `def _refresh_tag_cache_if_available(cmd) -> None:` that does the import-time try/except described above. Defined inside `workspace.py` so the module has no hard dependency on `search.py` (which would create a circular import via `registry.py`).

    4. Hook into Plan 02's app shutdown: in `src/cpho_cli/cli/repl/app.py`, override `def postloop(self) -> None: try: save_session_state(self.session) except RuntimeError: pass`. This is a small edit — file is already in Plan 02's `files_modified` but adding a `postloop` hook is acceptable as a Plan 03 cross-cut. (If reviewer prefers no cross-cut, place the save call at the end of `do_quit`/`do_exit` instead. Decision rationale documented in summary.)
       - Edge case: this plan's `files_modified` MUST list `src/cpho_cli/cli/repl/app.py` if and only if we modify it. To keep the dependency model simple, we DEFER the save-on-shutdown to Plan 05's testing pass — Plan 03 only provides the helper. Remove the app.py edit; document this in the summary.

  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_workspace_commands.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All six `do_*` methods defined and decorated with `@with_argparser`
    - INDEX_MISSING_MSG constant exported for Plan 04 to import
    - `_refresh_tag_cache_if_available` uses lazy import so workspace.py has no static dependency on search.py
    - `do_index` runs the dry-run preview FIRST, prompts for confirmation, and only then runs the destructive build (D-20)
    - OcrUpgradeDecisionRequired branch reuses the a/b/c/d 中文 prompt from `cli/app.py:124-150`
    - `do_resume` mutates `session` in place and surfaces `stale_reason` via `pwarning`
    - Tests pass (next task)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write tests/test_repl_workspace_commands.py</name>
  <read_first>
    - tests/test_repl_app_banner.py (Plan 02 — cmd2 testing pattern with `onecmd_plus_hooks` and monkey-patched `~/.cpho`)
    - tests/test_index_builder.py (look for fixtures that stub build_index without OCR/LLM)
    - src/cpho_cli/cli/repl/commands/workspace.py (Task 2 output)
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md (D-19, D-20, D-21, D-23)
  </read_first>
  <files>tests/test_repl_workspace_commands.py</files>
  <action>
    Create `tests/test_repl_workspace_commands.py` with these tests. Each test uses `tmp_path` fixture, `monkeypatch` to redirect `Path.home()` to a tmp dir, and instantiates `ReplApp(workspace=tmp_path, config_path=None)`. Use `monkeypatch.setattr("cpho_cli.cli.repl.commands.workspace.build_index", fake_build_index)` to stub `build_index` — fake returns a hand-constructed `IndexRunStats` with non-zero `total_problems` so the preview prints.

    Tests:

    1. `test_workspace_no_arg_prints_current`: `app.onecmd_plus_hooks("workspace")` → captured stdout contains str(tmp_path).
    2. `test_workspace_switch_to_existing_dir`: create `other = tmp_path / "other"; other.mkdir()`. Run `app.onecmd_plus_hooks(f"workspace {other}")`. Assert `app.session.workspace_path == other.resolve()` AND captured stdout contains `"已切换到工作空间"`.
    3. `test_workspace_nonexistent_dir_errors_but_does_not_exit`: `app.onecmd_plus_hooks("workspace /no/such/path")`. Assert `app.session.workspace_path == tmp_path` (unchanged); capsys.readouterr().err contains `"路径不存在"`.
    4. `test_status_lists_workspace_and_provider`: `app.onecmd_plus_hooks("status")`. Assert stdout has `str(tmp_path)`, `"openrouter"`, `"max_results"`, `"output_format"`.
    5. `test_status_shows_unset_when_no_index`: same setup, no `.cpho/`. Assert stdout has `"未建立"`.
    6. `test_config_lists_provider_no_secrets`: write a `config.local.yml` with `active_provider: openrouter` and a `providers: {openrouter: {api_key: "sk-secret"}}`. Pass `config_path` to ReplApp. Run `app.onecmd_plus_hooks("config")`. Assert `"openrouter"` present in stdout AND `"sk-secret"` NOT present.
    7. `test_index_dry_run_calls_build_index_with_dry_run_true`: monkeypatch `build_index` to a fake that records kwargs; `app.onecmd_plus_hooks("index --dry-run")`. Assert recorded kwargs include `dry_run=True`. Assert stdout contains `"[预览]"`.
    8. `test_index_no_dry_run_prompts_for_confirmation_and_cancels_on_n`: monkeypatch `build_index`. Monkeypatch `ReplApp.read_input` to return `"n"`. Run `app.onecmd_plus_hooks("index")`. Assert `build_index` was called once (dry-run step) but the second call did not happen. Assert stdout has `"已取消"`.
    9. `test_index_no_dry_run_proceeds_on_y`: monkeypatch `ReplApp.read_input` to return `"y"`. Assert `build_index` was called twice, second call with `dry_run=False`.
    10. `test_index_error_keeps_repl_alive`: monkeypatch `build_index` to raise `IndexBuildError("boom")`. Run `app.onecmd_plus_hooks("index --dry-run")`. Assert `app.session.workspace_path` unchanged and stderr contains `"boom"`.
    11. `test_reload_index_refreshes_meta`: write `.cpho/index.jsonl` with 2 lines, instantiate app, then add a third line, then `app.onecmd_plus_hooks("reload-index")`. Assert `app.session.index_meta.problem_count == 3`.
    12. `test_resume_returns_none_when_no_state_file`: `app.onecmd_plus_hooks("resume")`. Assert stdout contains `"当前工作空间无可恢复会话"`.
    13. `test_resume_restores_search_context_when_index_unchanged`: pre-seed `.cpho/repl_state.json` via `save_session_state` after manually setting `session.last_search_query = "力学"` and `session.last_search_result_ids = ["p001", "p002"]`. New ReplApp instance. Run `resume`. Assert `app.session.last_search_query == "力学"` and `app.session.last_search_result_ids == ["p001", "p002"]`.
    14. `test_resume_drops_stale_search_when_index_mtime_changes`: seed state then `touch` the index file (mtime bump). Run `resume`. Assert `app.session.last_search_query is None` AND `app.session.last_search_result_ids == []` AND stderr/pwarning contains `"索引已更新"`.

    Each test:
    - Monkey-patches `Path.home()` to `tmp_path / "home"` so persistent_history_file lands in tmp.
    - Disables banner output via `app.preloop` not being called (use `app.onecmd_plus_hooks` only — `preloop` runs only inside `cmdloop`).
    - Uses `capsys.readouterr()` after each command. Note: cmd2's `perror` writes to `app.stderr`; for `capsys` to capture, set `app.stderr = sys.stderr` in the fixture.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_workspace_commands.py -x -q 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 14 tests in `tests/test_repl_workspace_commands.py` all pass
    - Each covers a distinct D-XX decision: D-16 (workspace fallback), D-19 (index missing message constant), D-20 (dry-run + confirm), D-21 (errors don't exit), D-23 (stale-resume), D-10 (no secrets in `/config`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/workspace <path>` argument | Untrusted path; passed to `discover_workspace` which resolves and validates (`_safe_files` uses `resolved.relative_to(resolved_root)`) |
| `/index` invocation | Triggers OCR + LLM; cost-bearing; requires explicit confirmation per D-20 |
| `.cpho/repl_state.json` write | Local file in the workspace; trusted (single-user tool) |
| LLM provider call (inside build_index) | Network egress; handled by `core/llm.py` retry logic per D-21 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-03-01 | Tampering | Path traversal via `/workspace ../../etc` | mitigate | `Path(path).expanduser().resolve()` then `discover_workspace` enforces `relative_to(resolved_root)` filter at `core/workspace.py:_safe_files`. REPL adds no shell-out, no string interpolation (D-20: 不 shell-out). |
| T-02.2-03-02 | Repudiation | `/index` writes index.jsonl without audit | accept | Existing core/index builder maintains stats; REPL does not duplicate logging. |
| T-02.2-03-03 | Information Disclosure | `/config` accidentally prints api_key | mitigate | `do_config` whitelists fields: `active_provider`, `model.name`, `model.temperature`, list of provider profile NAMES (not values). Test 6 enforces. |
| T-02.2-03-04 | Denial of Service | User runs `/index` without `--dry-run` on huge workspace | mitigate | D-20 dry-run preview + 中文 confirmation prompt before destructive call. Cancellation via `n` exits cleanly. |
| T-02.2-03-05 | Tampering | Corrupted `.cpho/repl_state.json` | mitigate | `load_session_state` wraps JSON parse + `from_persistable_dict` in try/except; on failure returns `None` (treated as "no resumable state"). Atomic write (temp + rename) prevents partial saves. |
| T-02.2-03-06 | Elevation of Privilege | `do_resume` swaps in arbitrary workspace_path | accept | Resume only loads state for the current workspace; cannot pivot across workspaces. `from_persistable_dict` rejects non-existent paths. |
</threat_model>

<verification>
- `tests/test_repl_workspace_commands.py` (14 tests) passes
- Existing `tests/test_repl_session.py` and `tests/test_repl_app_banner.py` still pass (no regressions)
- Manual smoke (optional): `cpho repl --workspace ~/Desktop/物理竞赛资料` → run `/status`, `/config`, `/index --dry-run`; observe preview block + cost-warning; type `n` to cancel. Type `/resume` → "无可恢复会话".
- `ruff check src/cpho_cli/cli/repl/commands/workspace.py`, `mypy src/cpho_cli/cli/repl/commands/workspace.py` pass
</verification>

<success_criteria>
- WorkspaceCommandSet exposes six commands; help auto-generated via argparse (D-06)
- `/index` enforces dry-run preview + confirmation (D-20)
- `/resume` discards stale search context on index mtime/version change (D-23)
- All command failures stay in REPL (D-21)
- `INDEX_MISSING_MSG` available for Plan 04 import
- `refresh_tag_cache` invocation is via lazy import so this file has no static coupling to Plan 04
</success_criteria>

<output>
Create `.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-03-SUMMARY.md` when done.
</output>
