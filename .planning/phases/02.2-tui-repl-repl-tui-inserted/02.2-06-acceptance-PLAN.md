---
phase: 02.2-tui-repl-repl-tui-inserted
plan: 06
type: execute
wave: 6
depends_on:
  - 02.2-05
files_modified:
  - tests/conftest.py
  - tests/test_repl_phase02_2_acceptance.py
autonomous: true
requirements:
  - TUI-01
  - TUI-02
  - TUI-03
  - TUI-04
user_setup: []
---

<objective>
用端到端验收测试关闭 Phase 02.2：覆盖 ROADMAP 四项 success criteria、关键 D-XX 决策、真实工作空间 smoke 路径，以及 prompt_toolkit 自建框架的结构性约束。

Purpose: 防止后续实现偏回 cmd2 或把 skill/command 逻辑硬编码进主循环；确保 `/search` → `/show` 的用户闭环在测试中可复现。
Output: `tests/test_repl_phase02_2_acceptance.py` + 必要测试 fixture。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md
@.planning/spikes/02.2-repl-framework-comparison/COMPARISON.md
@src/cpho_cli/cli/repl/app.py
@src/cpho_cli/cli/repl/commands.py
@src/cpho_cli/cli/repl/commands/search.py
@src/cpho_cli/cli/repl/commands/workspace.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shared REPL fixture with synthetic index</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py existing fixtures
    - tests/test_index_storage.py fixture patterns
    - src/cpho_cli/models/index.py required IndexEntry fields
  </read_first>
  <action>
    Append fixtures only; do not rewrite existing fixture names:
    - `repl_workspace_with_index(tmp_path, monkeypatch)` creates isolated `XDG_CONFIG_HOME`, a temp workspace, `.cpho/index.jsonl`, OCR cache text files, and minimal vocabulary stub if needed for tag cache.
    - Validate every fixture entry through `IndexEntry.model_validate(...)` before writing JSONL.
    - `repl_app(repl_workspace_with_index)` constructs `ReplApp(workspace=ws, config_path=None)` and returns `(app, ws, problem_ids)`.

    The fixture must not touch the developer's real `~/.config/cpho`, `~/.cache/cpho`, or `/Users/ericzhang/Desktop/物理竞赛资料`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_phase02_2_acceptance.py --collect-only -q</automated>
  </verify>
  <done>Acceptance tests can instantiate REPL with a deterministic local index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ROADMAP success criteria tests</name>
  <files>tests/test_repl_phase02_2_acceptance.py</files>
  <read_first>
    - .planning/ROADMAP.md Phase 02.2 success criteria
    - .planning/REQUIREMENTS.md TUI-01 through TUI-04
    - tests/conftest.py fixtures from Task 1
  </read_first>
  <action>
    Create acceptance tests with docstrings naming the exact ROADMAP criterion:
    1. `cpho repl` help path: instantiate app, run `/help`, assert all Phase 02.2 commands appear (`/search`, `/show`, `/workspace`, `/status`, `/config`, `/index`, `/reload-index`, `/resume`, `/help`, `/set`, `/run`, `/explain`, `/quiz`).
    2. `/search 力学` or equivalent fixture tag search populates `last_search_query` and `last_search_result_ids`.
    3. `/show 1` after search displays OCR text, tag IDs, source path/page, and updates `current_problem_id`.
    4. `/show 1 --full` calls `display.pager` with full OCR text.
    5. Structural test: `app.py` contains generic registry installation/dispatch only and no hard-coded `explain`, `quiz`, or concrete skill names.
    6. Structural test: adding a fake `Command(name="/fake", ...)` to a fresh registry makes `/help` show it without editing display/layout code.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_phase02_2_acceptance.py -x -q -k "criterion"</automated>
  </verify>
  <done>Each ROADMAP success criterion has at least one automated assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: decision and regression gates</name>
  <files>tests/test_repl_phase02_2_acceptance.py</files>
  <read_first>
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md D-07, D-18, D-20, D-22, D-23
    - .planning/spikes/02.2-repl-framework-comparison/COMPARISON.md conclusion
  </read_first>
  <action>
    Add targeted gates:
    - D-07: `rg "import prompt_toolkit|from prompt_toolkit" src/cpho_cli/core` returns no matches.
    - D-18: ReplApp starts on workspace without `.cpho/index.jsonl`; `/status` still works and says index is not built.
    - D-20: `/index` without `--dry-run` still performs a dry-run preview first and asks for confirmation before real build; monkeypatch build/confirm to verify call order.
    - D-22/D-23: write session on clean exit; new app `/resume` restores search context when index metadata matches and drops it when metadata changes.
    - Spike regression: no `import cmd2` anywhere under `src/cpho_cli/cli/repl/`; this phase intentionally uses prompt_toolkit directly.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_phase02_2_acceptance.py -x -q</automated>
  </verify>
  <done>Critical architecture decisions are covered by executable regression gates.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: final lint/type/test sweep</name>
  <files></files>
  <read_first>
    - pyproject.toml tool configuration
    - all `src/cpho_cli/cli/repl/` files
  </read_first>
  <action>
    Run:
    - `uv run ruff check src/cpho_cli/cli/repl tests/test_repl_*.py`
    - `uv run mypy src/cpho_cli/cli/repl`
    - `uv run pytest -x`

    Fix only failures caused by this phase's REPL work. If unrelated tests fail, document exact failing tests and why they are unrelated; do not refactor adjacent modules.
  </action>
  <verify>
    <automated>uv run ruff check src/cpho_cli/cli/repl tests/test_repl_*.py && uv run mypy src/cpho_cli/cli/repl && uv run pytest -x</automated>
  </verify>
  <done>Full REPL phase is linted, type-checked, and tested.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_repl_phase02_2_acceptance.py -x -q`
- `uv run pytest tests/test_repl_commands.py tests/test_repl_session.py tests/test_repl_persistence.py tests/test_repl_runtime.py tests/test_repl_workspace_commands.py tests/test_repl_search_commands.py -x -q`
- `uv run ruff check src/cpho_cli/cli/repl tests/test_repl_*.py`
- `uv run mypy src/cpho_cli/cli/repl`
- `uv run pytest -x`
</verification>

<success_criteria>
- ROADMAP Phase 02.2 four success criteria are all covered by tests.
- Spike decision is preserved: prompt_toolkit self-built framework, no cmd2 in REPL.
- REPL works without index for non-index commands.
- Search/show work against a realistic index fixture and preserve session context.
- Phase 02.2 is ready for `$gsd-verify-work` or `$gsd-audit-milestone`.
</success_criteria>

<output>
Create `.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-06-SUMMARY.md` when done, mapping each acceptance test to ROADMAP criterion or CONTEXT decision.
</output>
