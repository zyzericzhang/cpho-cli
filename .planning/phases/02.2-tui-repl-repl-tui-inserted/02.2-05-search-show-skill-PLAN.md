---
phase: 02.2-tui-repl-repl-tui-inserted
plan: 05
type: execute
wave: 5
depends_on:
  - 02.2-04
files_modified:
  - src/cpho_cli/cli/repl/commands/search.py
  - tests/test_repl_search_commands.py
autonomous: true
requirements:
  - TUI-02
  - TUI-03
  - TUI-04
user_setup: []
---

<objective>
实现首批题库读取命令 `/search` 与 `/show`，以及标签补全缓存。命令只通过 `Command` registry 注册，直接调用 `core/index/api.py`，不缓存完整 `IndexEntry`，不触发 OCR/LLM/index rebuild。

Purpose: 交付用户可见的 Phase 02.2 核心闭环：在 REPL 中按标签搜索题目，随后用搜索结果序号查看题目详情。
Output: `commands/search.py` + 搜索/展示测试。
</objective>

<context>
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-PATTERNS.md
@.planning/spikes/02.2-repl-framework-comparison/COMPARISON.md
@docs/tui-repl-pattern.md
@src/cpho_cli/cli/repl/commands.py
@src/cpho_cli/cli/repl/session.py
@src/cpho_cli/cli/repl/display.py
@src/cpho_cli/cli/repl/completers.py
@src/cpho_cli/cli/repl/commands/workspace.py
@src/cpho_cli/core/index/api.py
@src/cpho_cli/core/index/__init__.py
@src/cpho_cli/models/index.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tag cache and command-specific completers</name>
  <files>src/cpho_cli/cli/repl/commands/search.py, tests/test_repl_search_commands.py</files>
  <read_first>
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md D-14, D-26
    - src/cpho_cli/core/index/vocabulary.py
    - src/cpho_cli/models/index.py TagCategory / TaggedReference shape
  </read_first>
  <action>
    Create `search.py` with:
    - `_TAG_CACHE: dict[str, dict[str, str]]` for `physics_model`, `math_technique`, `heuristic`.
    - `def refresh_tag_cache(session: SessionState) -> None` loads vocabulary for `session.workspace_path`; on `VocabularyError`, clears cache and returns.
    - `class TagCompleter(Completer)` or a small completer factory for prompt_toolkit that suggests tag IDs and displays `display_zh` as metadata.
    - Completion supports category-specific flags:
      - `/search --physics-model <id>`
      - `/search --math-technique <id>`
      - `/search --heuristic <id>`
      - comma-separated IDs keep completing the current segment after the last comma.

    Tests monkeypatch `load_vocabulary` with a small stub and assert cache population, silent clear on error, and completions by prefix/display metadata.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_search_commands.py -x -q -k "cache or completer"</automated>
  </verify>
  <done>Tag completion is loaded once and can be refreshed by workspace/index commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /search handler</name>
  <files>src/cpho_cli/cli/repl/commands/search.py, tests/test_repl_search_commands.py</files>
  <read_first>
    - src/cpho_cli/core/index/api.py query_index signature
    - src/cpho_cli/cli/repl/commands/workspace.py INDEX_MISSING_MSG
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md D-11, D-19
  </read_first>
  <action>
    Implement `async def do_search(session, args)` and register `/search`:
    - Parse args with `argparse.ArgumentParser(prog="/search", add_help=False)`.
    - Support both simple user form `/search 力学` and explicit filters:
      - positional `query: str | None` maps to a best-effort tag search by internal_id/display alias through cache; if ambiguous or absent, search all tag categories for that token.
      - `--physics-model`, `--math-technique`, `--heuristic` accept comma-separated tag IDs.
      - `--match-mode any|all`; `--limit N` defaults to `session.max_results`.
    - If `session.index_meta is None`, show `INDEX_MISSING_MSG` and return.
    - Call `query_index(session.workspace_path, ...)`.
    - Save `session.last_search_query` as a concise string and `session.last_search_result_ids` as displayed `problem_id` values only.
    - Render results using `display.render_table` with columns `序号 / ID / 题目摘要 / 标签 / 来源`.
    - If no results, print `"未找到匹配题目。"`; do not treat it as an error.
    - If more than limit, print a Chinese truncation note.

    Tests use a temp index via `core.index.storage.write_index` or monkeypatch `query_index`. Cover missing index, positional query, explicit filters, `match_mode=all`, empty result, truncation, and session state update.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_search_commands.py -x -q -k "search"</automated>
  </verify>
  <done>`/search` returns table output and stores only problem IDs in session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /show handler</name>
  <files>src/cpho_cli/cli/repl/commands/search.py, tests/test_repl_search_commands.py</files>
  <read_first>
    - src/cpho_cli/core/index/api.py get_problem_entry signature
    - src/cpho_cli/models/index.py IndexEntry fields
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md D-11, D-13, D-19
  </read_first>
  <action>
    Implement `async def do_show(session, args)` and register `/show`:
    - Parse positional `ref` and `--full`.
    - Numeric `ref` resolves as 1-indexed index into `session.last_search_result_ids`.
    - Non-numeric `ref` is treated as direct `problem_id`.
    - If numeric ref is used before search, show `"尚未搜索，请先 /search ..."` and return.
    - If out of range, show `"序号超出范围"` and return.
    - Call `get_problem_entry(session.workspace_path, problem_id)`.
    - Compact view prints ID, source path/page, indexed date, tag IDs, first 200 chars of OCR cache text if available, and hint for `--full`.
    - Full view reads `entry.ocr_cache_path` and passes rendered full detail to `display.pager`.
    - Sanitize OCR text by replacing ESC (`\x1b`) and bare carriage returns before output.
    - Update `session.current_problem_id` only after successful lookup.

    Tests cover show by search index, show by direct problem_id, out-of-range, no prior search, missing problem, compact output, full pager call, and OCR escape sanitization.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_search_commands.py -x -q -k "show"</automated>
  </verify>
  <done>`/show` works from session context and never reruns OCR or index build.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|--------|-------------|------------|
| OCR cache contains terminal escape sequences | mitigate | Replace ESC and bare CR before compact/full rendering. |
| `/search` with no filters on huge index | accept | `session.max_results` caps displayed/stored result IDs. |
| Missing index silently looks like no results | mitigate | Use explicit `INDEX_MISSING_MSG`, never return empty silently. |
| Tag vocabulary errors break Tab completion | mitigate | Clear cache and return no completions. |
</threat_model>

<verification>
1. `uv run pytest tests/test_repl_search_commands.py -x -q`
2. `python -c "from cpho_cli.cli.repl.commands.search import register, refresh_tag_cache; print('ok')"`
3. Manual smoke on `/Users/ericzhang/Desktop/物理竞赛资料`: build or reuse index, then `cpho repl --workspace ...`, `/search 力学`, `/show 1`, `/show 1 --full`.
</verification>

<success_criteria>
- `/search 力学` and explicit tag-filter forms both work.
- Search results are stored in session as problem IDs only.
- `/show 3` resolves against prior search results and displays OCR + tags + source.
- Missing index is an explicit error, not empty output and not auto-build.
- Command and tag completions remain registry-driven and prompt_toolkit-based.
</success_criteria>

<output>
Create `.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-05-SUMMARY.md` when done, including exact supported `/search` syntax and `/show` resolution rules.
</output>
