---
phase: 02.2-tui-repl-repl-tui-inserted
plan: 04
type: execute
wave: 4
depends_on:
  - 02.2-03
files_modified:
  - src/cpho_cli/cli/repl/commands/workspace.py
  - tests/test_repl_workspace_commands.py
autonomous: true
requirements:
  - TUI-01
  - TUI-02
user_setup: []
---

<objective>
实现 workspace/index 管理类 slash commands：`/workspace`、`/status`、`/config`、`/index`、`/reload-index`、`/resume`。这些命令只通过 `Command` registry 注册，handler 接收 `SessionState`，不依赖 cmd2 或 Typer。

Purpose: 把所有会修改工作空间、索引元数据、持久化 session 的行为集中在一个模块，为 Plan 05 的 `/search`、`/show` 提供稳定状态与错误消息。
Output: `commands/workspace.py` + 单元/集成测试。
</objective>

<context>
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-PATTERNS.md
@src/cpho_cli/cli/repl/commands.py
@src/cpho_cli/cli/repl/session.py
@src/cpho_cli/cli/repl/persistence.py
@src/cpho_cli/cli/repl/display.py
@src/cpho_cli/core/workspace.py
@src/cpho_cli/core/index/__init__.py
@src/cpho_cli/core/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: workspace helpers and register() surface</name>
  <files>src/cpho_cli/cli/repl/commands/workspace.py, tests/test_repl_workspace_commands.py</files>
  <read_first>
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md D-16, D-18, D-19, D-20, D-21, D-23
    - src/cpho_cli/core/workspace.py discover_workspace
    - src/cpho_cli/cli/repl/session.py load_index_meta
  </read_first>
  <action>
    Create `workspace.py` with:
    - `INDEX_MISSING_MSG = "未找到索引，请先运行: cpho index --workspace {workspace}"` exported for Plan 05.
    - `async def do_workspace(session, args)`:
      - no args prints current workspace;
      - one path arg resolves/validates existing directory, calls `discover_workspace`, updates `session.workspace_path`, `session.index_meta`, persists session via `write_session`, and calls a lazy `_refresh_tag_cache_if_available(session)`.
    - `async def do_status(session, args)` prints workspace, index status, active provider, model, `max_results`, `output_format`.
    - `async def do_config(session, args)` prints whitelisted config fields only: active provider, model name/temperature, provider profile names. Never print API keys or provider secrets.
    - `def register(registry)` registers `/workspace`, `/status`, `/config` with category `"工作空间"`.

    Tests cover current workspace display, switch to existing dir, nonexistent dir error without state mutation, missing index status, config output with no secrets, and `register()` keys/categories.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_workspace_commands.py -x -q -k "workspace or status or config"</automated>
  </verify>
  <done>Workspace/status/config commands work without index and never print secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /index dry-run preview and confirmation</name>
  <files>src/cpho_cli/cli/repl/commands/workspace.py, tests/test_repl_workspace_commands.py</files>
  <read_first>
    - src/cpho_cli/cli/app.py index_command stats output and OcrUpgradeDecisionRequired branch
    - src/cpho_cli/core/index/builder.py build_index signature
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md D-20, D-21
  </read_first>
  <action>
    Implement `async def do_index(session, args)` and register `/index`.
    - Parse with `argparse.ArgumentParser(prog="/index", add_help=False)` for `--dry-run`, `--force`, `--only-new`, `--ocr-strategy {prompt,reuse,rebuild,new-only}`.
    - Always call `build_index(..., dry_run=True, ...)` first and print a Chinese preview block via `display.info`/plain output.
    - If user passed `--dry-run`, stop after preview.
    - Otherwise prompt for confirmation through a small injectable helper `confirm_index_run(prompt_text: str) -> bool`. Default implementation uses prompt_toolkit shortcut prompt; tests monkeypatch it.
    - Only on confirmation call `build_index(..., dry_run=False, ...)`.
    - Catch `ConfigError`, `IndexBuildError`, `VocabularyError`, `OcrUpgradeDecisionRequired`, `ValueError` and show Chinese error without raising out of the REPL.
    - On successful real run, refresh `session.index_meta` and lazy refresh tag cache.

    Tests monkeypatch `build_index` and confirmation helper. Cover `--dry-run`, cancel on `n`, proceed on `y`, error containment, and index_meta refresh after success.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_workspace_commands.py -x -q -k "index"</automated>
  </verify>
  <done>`/index` enforces preview before cost-bearing work and never shells out to CLI strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /reload-index and /resume</name>
  <files>src/cpho_cli/cli/repl/commands/workspace.py, tests/test_repl_workspace_commands.py</files>
  <read_first>
    - src/cpho_cli/cli/repl/persistence.py read_session/write_session
    - src/cpho_cli/cli/repl/session.py SessionState/load_index_meta
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md D-22, D-23, D-24, D-25
  </read_first>
  <action>
    Implement and register:
    - `/reload-index`: re-read `load_index_meta(session.workspace_path)`, print fresh count or `"索引未建立"`; lazy refresh tag cache.
    - `/resume`: reads persisted session payload, validates workspace exists, applies only allowed fields in place (`last_search_query`, result IDs, current problem, max_results/output_format if present). If saved index mtime/version differs from current `IndexMeta`, restore workspace but drop stale search/current problem fields and warn in Chinese.

    Do not replace the `SessionState` object, because existing handlers and tests may hold references.

    Tests cover no saved session, restore unchanged session, stale index drops search context, corrupt JSON falls back with an error message instead of crashing, and reload-index updates metadata.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repl_workspace_commands.py -x -q -k "reload or resume"</automated>
  </verify>
  <done>Session restoration is explicit, conservative, and stores only IDs/metadata.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|--------|-------------|------------|
| `/workspace` path points outside intended project | accept | Local single-user tool; path is resolved and must be an existing directory. No shell execution. |
| `/index` triggers OCR/LLM cost | mitigate | Mandatory dry-run preview and explicit confirmation before real build. |
| `/config` leaks API keys | mitigate | Whitelist printed fields; tests assert secret strings are absent. |
| Corrupt session.json crashes REPL | mitigate | `/resume` catches parse/schema errors and starts fresh. |
</threat_model>

<verification>
1. `uv run pytest tests/test_repl_workspace_commands.py -x -q`
2. `python -c "from cpho_cli.cli.repl.commands.workspace import INDEX_MISSING_MSG, register; print('ok')"`
3. Manual smoke: `cpho repl --workspace /Users/ericzhang/Desktop/物理竞赛资料`, run `/status`, `/config`, `/index --dry-run`, cancel real run.
</verification>

<success_criteria>
- All workspace/index commands are registered through `Command` dataclass objects.
- Missing index does not prevent `/status`, `/config`, `/workspace`, `/help`.
- `/index` cannot perform a real build before showing a dry-run preview.
- `/resume` never reloads OCR/LLM/index content and never persists full `IndexEntry`.
</success_criteria>

<output>
Create `.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-04-SUMMARY.md` when done, including exact confirmation behavior for `/index` and stale-session behavior for `/resume`.
</output>
