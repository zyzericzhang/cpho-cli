---
phase: 03-skill-cross-cutting-core-skills
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cpho_cli/core/skill_outputs.py
  - src/cpho_cli/cli/repl/persistence.py
  - src/cpho_cli/cli/repl/session.py
  - src/cpho_cli/cli/repl/commands/set_cmd.py
  - tests/test_skill_outputs.py
  - tests/test_repl_persistence.py
  - tests/test_repl_session.py
  - tests/test_repl_builtin_commands.py
  - docs/phase3-export-settings-decisions.md
autonomous: true
requirements:
  - CROSS-EXPORT
  - SKILL-PROBE
must_haves:
  truths:
    - "Every skill can resolve a markdown export path under XDG data or a user override."
    - "Export filenames preserve readable Chinese problem names while preventing path traversal."
    - "REPL settings expose out.dir and probe.max_rounds without persisting heavyweight skill output."
  artifacts:
    - path: src/cpho_cli/core/skill_outputs.py
      provides: "Shared markdown path resolution, filename sanitization, atomic markdown writes, and append support."
    - path: src/cpho_cli/cli/repl/persistence.py
      provides: "XDG data directory helper for ~/.local/share/cpho."
    - path: src/cpho_cli/cli/repl/session.py
      provides: "Session fields for out_dir, probe_max_rounds, and current_solve_report."
    - path: docs/phase3-export-settings-decisions.md
      provides: "Phase 3 decision notes for export path rules and roadmap/context wording differences."
  key_links:
    - from: src/cpho_cli/cli/repl/commands/set_cmd.py
      to: src/cpho_cli/cli/repl/session.py
      via: "set_cmd updates SessionState.out_dir and SessionState.probe_max_rounds"
    - from: src/cpho_cli/core/skill_outputs.py
      to: src/cpho_cli/cli/repl/persistence.py
      via: "default export root uses data_dir()/outputs/<workspace_hash>/<skill>/..."
---

<objective>
Create the Phase 3 markdown export and session-setting foundation required by all later skill plans.

Purpose: Satisfy D-01 and D-11 without changing any skill semantics yet, while giving Solve, Explain, Probe, and Follow-up one shared output contract.
Output: A tested export helper, XDG data-dir support, REPL settings for `/set out.dir` and `/set probe.max_rounds`, and a decision doc recording the controlled-tag/free-text wording conflict.
</objective>

<context>
@.planning/phases/03-skill-cross-cutting-core-skills/03-CONTEXT.md
@.planning/phases/03-skill-cross-cutting-core-skills/03-RESEARCH.md
@.planning/phases/03-skill-cross-cutting-core-skills/03-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/new-understanding-2026-05-26.md
@src/cpho_cli/cli/repl/persistence.py
@src/cpho_cli/cli/repl/session.py
@src/cpho_cli/cli/repl/commands/set_cmd.py
@tests/test_repl_persistence.py
@tests/test_repl_session.py
@tests/test_phase023_acceptance.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify shared markdown export behavior</name>
  <read_first>
    - src/cpho_cli/cli/repl/persistence.py
    - src/cpho_cli/cli/repl/session.py
    - src/cpho_cli/cli/repl/commands/set_cmd.py
    - tests/test_repl_persistence.py
    - tests/test_phase023_acceptance.py
  </read_first>
  <files>tests/test_skill_outputs.py, tests/test_repl_persistence.py, tests/test_repl_session.py, tests/test_repl_builtin_commands.py</files>
  <action>Write failing tests for D-01 and D-11 before production code. Cover: XDG `data_dir()` honors `XDG_DATA_HOME`; default export path is `data_dir()/outputs/<workspace_hash>/<skill>/<problem_name>.md`; workspace hash is deterministic from the resolved workspace path; filename sanitization preserves Chinese text and removes path separators; override `SessionState.out_dir` is accepted as an absolute or relative Path; `SessionState.probe_max_rounds` defaults to 10; `/set out.dir <path>` and `/set probe.max_rounds N` validate and persist only primitive fields. Include one real-workspace-shaped test that copies or names a sample from `/Users/ericzhang/Desktop/物理竞赛资料` into `tmp_path`; never write to the original real workspace.</action>
  <verify>
    <automated>uv run pytest tests/test_skill_outputs.py tests/test_repl_persistence.py tests/test_repl_session.py tests/test_repl_builtin_commands.py -q</automated>
  </verify>
  <acceptance_criteria>The new tests fail before implementation and assert real Chinese/repeated-space path handling through Path objects, not shell-split strings.</acceptance_criteria>
  <done>The new tests fail before implementation and assert real Chinese/repeated-space path handling through Path objects, not shell-split strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement export helper and REPL settings</name>
  <read_first>
    - src/cpho_cli/cli/repl/persistence.py
    - src/cpho_cli/cli/repl/session.py
    - src/cpho_cli/cli/repl/commands/set_cmd.py
  </read_first>
  <files>src/cpho_cli/core/skill_outputs.py, src/cpho_cli/cli/repl/persistence.py, src/cpho_cli/cli/repl/session.py, src/cpho_cli/cli/repl/commands/set_cmd.py, docs/phase3-export-settings-decisions.md</files>
  <action>Create `skill_outputs.py` with small Path-based helpers: `workspace_hash(workspace_path)`, `safe_problem_filename(problem_id_or_title, suffix)`, `default_markdown_path(workspace_path, skill_name, problem_name, override_dir=None)`, `write_markdown_atomic(path, text)`, and `append_markdown(path, text)`. Add `data_dir()` to REPL persistence using `XDG_DATA_HOME` or `~/.local/share`. Add `SessionState.out_dir: Path | None`, `SessionState.probe_max_rounds: int = 10`, and `SessionState.current_solve_report` with an import that avoids circular imports. Extend `/set` for `out.dir` and `probe.max_rounds`; preserve existing provider/max_results behavior and do not persist `current_solve_report`. Document that D-04/D-05 free-text discrepancies override the older roadmap phrase "controlled tag", while optional persistence still uses existing index user tags.</action>
  <verify>
    <automated>uv run pytest tests/test_skill_outputs.py tests/test_repl_persistence.py tests/test_repl_session.py tests/test_repl_builtin_commands.py -q</automated>
  </verify>
  <acceptance_criteria>Export path resolution works for Chinese filenames and override dirs; `/set out.dir` and `/set probe.max_rounds` update SessionState; session.json remains an allowlisted primitive payload.</acceptance_criteria>
  <done>Export path resolution works for Chinese filenames and override dirs; `/set out.dir` and `/set probe.max_rounds` update SessionState; session.json remains an allowlisted primitive payload.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user setting -> filesystem path | `/set out.dir` and export filename inputs influence local file writes. |
| real workspace -> tests | Tests may read filenames from `/Users/ericzhang/Desktop/物理竞赛资料` but must copy into temp dirs before writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `skill_outputs.safe_problem_filename` | mitigate | Strip path separators and reserved characters from filenames; resolve output directory as a Path. |
| T-03-01-02 | Information Disclosure | `write_session` | mitigate | Keep `current_solve_report` out of session.json; persist only allowlisted primitive fields. |
| T-03-01-03 | Denial of Service | real workspace tests | mitigate | Use temp copies or seeded names; never scan or mutate the full real workspace. |
</threat_model>

<verification>
Run `uv run pytest tests/test_skill_outputs.py tests/test_repl_persistence.py tests/test_repl_session.py tests/test_repl_builtin_commands.py -q`.
</verification>

<success_criteria>
Phase 3 has a shared markdown export contract matching D-01, Probe round configuration matching D-11, and a docs note explaining why Solve discrepancies remain free text despite older roadmap wording.
</success_criteria>

<output>
Create `.planning/phases/03-skill-cross-cutting-core-skills/03-01-SUMMARY.md` when done.
</output>
