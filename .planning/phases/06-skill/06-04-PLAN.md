---
phase: 06-skill
plan: 06-04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - src/cpho_cli/cli/app.py
  - tests/test_knowledge_cli.py
  - docs/user/knowledge.md
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-04
---

<objective>
Expose Phase 6 knowledge foundation through narrow CLI commands and user documentation.
</objective>

<context>
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-RESEARCH.md
@src/cpho_cli/cli/app.py
@docs/user/README.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add `cpho knowledge` commands</name>
  <files>src/cpho_cli/cli/app.py, tests/test_knowledge_cli.py</files>
  <action>Add a `knowledge` Typer sub-app with `normalize`, `publish`, and `find` commands. `normalize` writes a draft, `publish` publishes a reviewed draft, and `find` prints resolver matches for a problem id. Keep flags consistent with existing CLI style: `--workspace`, `--config`, `--provider`, `--dry-run`, and `--quiet` where applicable.</action>
  <verify>
    <automated>uv run pytest tests/test_knowledge_cli.py -q</automated>
  </verify>
  <acceptance_criteria>CLI commands produce paths to drafts/published files/matches and report validation failures clearly.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document Phase 6 knowledge workflow</name>
  <files>docs/user/knowledge.md, docs/user/README.md, tests/test_docs_user.py</files>
  <action>Add user-facing docs for the knowledge directory layout, required frontmatter, normalize/publish workflow, and resolver find command. Link the page from docs/user/README.md.</action>
  <verify>
    <automated>uv run pytest tests/test_docs_user.py -q</automated>
  </verify>
  <acceptance_criteria>docs/user accurately describes the Phase 6 behavior and does not document Phase 7/8 features as shipped.</acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| CLI publishes unreviewed generated content by default | `normalize` and `publish` are separate commands; auto-publish is not the default. |
| Docs imply community sync exists too early | Keep Phase 8 sync documented as future until implemented. |
</threat_model>

<verification>
Run `uv run pytest tests/test_knowledge_cli.py tests/test_docs_user.py -q`.
</verification>

<success_criteria>
The private KB is usable from CLI and documented without overclaiming Phase 7/8 functionality.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-04-SUMMARY.md` when done.
</output>

