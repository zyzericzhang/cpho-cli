---
phase: 06-skill
plan: 06-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cpho_cli/models/skills.py
  - src/cpho_cli/core/skills.py
  - tests/test_skills.py
autonomous: true
requirements:
  - SKILL-PIPE-01
  - SKILL-PIPE-02
  - SKILL-PIPE-03
---

<objective>
Extend the existing declarative skill model into the Phase 6 SkillPipeline foundation without changing v1.0 skill behavior.
</objective>

<context>
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-RESEARCH.md
@src/cpho_cli/models/skills.py
@src/cpho_cli/core/skills.py
@src/cpho_cli/core/runtime.py
@src/cpho_cli/builtin_skills/solve/skill.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add pipeline description models</name>
  <files>src/cpho_cli/models/skills.py, tests/test_skills.py</files>
  <action>Add optional `description`, `default_model`, and `requires_multimodal` fields to `SkillStep`. Add `PipelineStepDescription`, `PipelineEdge`, `PipelineDescription`, and `SkillSpec.describe(skill_root: Path | None = None)`. Include implicit producer edges and explicit `depends_on` edges. Add `SkillPipeline = SkillSpec` alias.</action>
  <verify>
    <automated>uv run pytest tests/test_skills.py -q</automated>
  </verify>
  <acceptance_criteria>Existing built-in skill YAML validates unchanged; describe() returns stable step metadata and dependencies.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Expose resolved prompt paths through loaded skills</name>
  <files>src/cpho_cli/core/skills.py, tests/test_skills.py</files>
  <action>Keep current prompt path safety checks, and add tests showing `loaded.spec.describe(loaded.root)` includes resolved prompt paths for prompted steps while preserving duplicate-output validation.</action>
  <verify>
    <automated>uv run pytest tests/test_skills.py -q</automated>
  </verify>
  <acceptance_criteria>Phase 7 can render a model panel from `LoadedSkill.spec.describe(LoadedSkill.root)` without reparsing skill YAML.</acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Prompt path traversal in panel metadata | Reuse existing prompt path containment checks before returning resolved paths. |
| v1.0 skill breakage | New fields are optional with defaults; built-in skill load tests remain green. |
</threat_model>

<verification>
Run `uv run pytest tests/test_skills.py -q`.
</verification>

<success_criteria>
SkillPipeline metadata is available, v1.0 skill definitions remain compatible, and no runtime behavior changes are introduced.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-01-SUMMARY.md` when done.
</output>

