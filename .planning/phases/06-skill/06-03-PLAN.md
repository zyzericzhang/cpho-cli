---
phase: 06-skill
plan: 06-03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - pyproject.toml
  - uv.lock
  - src/cpho_cli/core/knowledge/normalize.py
  - src/cpho_cli/core/knowledge/prompts/normalize_knowledge.md.j2
  - tests/test_knowledge.py
autonomous: true
requirements:
  - KB-02
  - KB-03
---

<objective>
Implement the two-step knowledge normalization flow: draft first, publish after review.
</objective>

<context>
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-RESEARCH.md
@src/cpho_cli/core/llm.py
@src/cpho_cli/core/multimodal.py
@src/cpho_cli/core/documents.py
@src/cpho_cli/core/skill_handlers.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Draft knowledge normalization</name>
  <files>src/cpho_cli/core/knowledge/normalize.py, src/cpho_cli/core/knowledge/prompts/normalize_knowledge.md.j2, pyproject.toml, uv.lock, tests/test_knowledge.py</files>
  <action>Add `normalize_knowledge_file(workspace_root, source_path, config_path=None, provider_name=None, dry_run=False)` that writes a draft to `.cpho/knowledge/drafts/`. Draft frontmatter must include `standardized`, `canonical_tag_id`, `last_normalized_hash`, and `last_user_edit_hash`. Text inputs can produce a deterministic draft shell in dry-run/tests. Image/docx paths must be routed through LLM normalization when not dry-run; docx text extraction should use `mammoth` without adding system dependencies.</action>
  <verify>
    <automated>uv run pytest tests/test_knowledge.py -q</automated>
  </verify>
  <acceptance_criteria>Draft generation preserves source text, records hashes, and never writes directly to published knowledge.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Publish reviewed drafts</name>
  <files>src/cpho_cli/core/knowledge/normalize.py, tests/test_knowledge.py</files>
  <action>Add `publish_knowledge_draft(workspace_root, draft_path)` that validates required frontmatter and copies the reviewed draft to `.cpho/knowledge/files/published/`. If the user edited the draft, update `last_user_edit_hash` but preserve `last_normalized_hash` so future minimum-diff behavior can detect edits.</action>
  <verify>
    <automated>uv run pytest tests/test_knowledge.py -q</automated>
  </verify>
  <acceptance_criteria>Publish rejects invalid drafts and writes a valid published knowledge file for resolver consumption.</acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| LLM overwrites user-authored knowledge | Normalize writes drafts only; publish is explicit. |
| User edits lost on rerun | Hash fields distinguish normalized base from user-edited draft. |
| Non-Python docx dependency creep | Use `mammoth`; do not introduce LibreOffice/system tooling. |
</threat_model>

<verification>
Run `uv run pytest tests/test_knowledge.py -q`.
</verification>

<success_criteria>
Users can convert freeform knowledge into reviewable drafts and publish valid drafts into the private KB.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-03-SUMMARY.md` when done.
</output>

