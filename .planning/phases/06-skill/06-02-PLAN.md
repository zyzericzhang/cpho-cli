---
phase: 06-skill
plan: 06-02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cpho_cli/models/knowledge.py
  - src/cpho_cli/core/knowledge/__init__.py
  - src/cpho_cli/core/knowledge/store.py
  - src/cpho_cli/core/knowledge/resolver.py
  - tests/test_knowledge.py
autonomous: true
requirements:
  - KB-01
  - KB-04
---

<objective>
Implement the private Knowledge Base file format and `KnowledgeResolver.find_for_problem(problem_id)` API.
</objective>

<context>
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-RESEARCH.md
@src/cpho_cli/core/index/api.py
@src/cpho_cli/core/index/storage.py
@src/cpho_cli/core/index/vocabulary.py
@src/cpho_cli/models/index.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add knowledge frontmatter parser and validation</name>
  <files>src/cpho_cli/models/knowledge.py, src/cpho_cli/core/knowledge/store.py, tests/test_knowledge.py</files>
  <action>Add models for `KnowledgeSource`, `KnowledgeFrontmatter`, `KnowledgeDocument`, and `KnowledgeMatch`. Implement markdown frontmatter parsing for `.md/.markdown/.tex/.txt/.rst` knowledge files and validate required fields, especially `canonical_tag_id`, against `load_merged_vocabulary(workspace_root)`. Error messages must include the file path and missing/unknown field.</action>
  <verify>
    <automated>uv run pytest tests/test_knowledge.py -q</automated>
  </verify>
  <acceptance_criteria>Valid knowledge files load; missing frontmatter and unknown canonical tags fail explicitly.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement resolver using index tags</name>
  <files>src/cpho_cli/core/knowledge/resolver.py, src/cpho_cli/core/knowledge/__init__.py, tests/test_knowledge.py</files>
  <action>Add `KnowledgeResolver(workspace_root: Path)` with `find_for_problem(problem_id: str) -> list[KnowledgeMatch]`. It reads the indexed problem, collects canonical tag ids from physics/math/heuristic/user tags, scans private knowledge files under `.cpho/knowledge/files/` and `.cpho/knowledge/files/published/`, and returns exact tag matches before same-category fallback. Add internal community scan hook for `~/.cache/cpho/community-kb/` but keep sync implementation for Phase 8.</action>
  <verify>
    <automated>uv run pytest tests/test_knowledge.py -q</automated>
  </verify>
  <acceptance_criteria>Resolver finds a matching private knowledge file for a fixture indexed problem and returns private matches before community matches.</acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Invalid knowledge file silently ignored | Raise `KnowledgeError` with path and field unless caller explicitly chooses lenient scan. |
| Community content precedence over private notes | Resolver sorts private before community. |
</threat_model>

<verification>
Run `uv run pytest tests/test_knowledge.py -q`.
</verification>

<success_criteria>
Knowledge files can be validated and resolved by problem id using v1.0 index tags.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-02-SUMMARY.md` when done.
</output>

