---
phase: 09-cross-platform-installer
plan: 09-04
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-03
files_modified:
  - .github/workflows/release.yml
  - packaging/installer.iss
  - packaging/RELEASE-CHECKLIST.md
  - README.md
  - docs/user/install.md
  - docs/user/README.md
autonomous: false
requirements:
  - INSTALLER-03
must_haves:
  truths:
    - "D-04: Install docs and release flow serve both non-command-line physics coaches/students and technical command-line users."
    - "D-05: Windows delivery is a complete .exe installer that users can download and run; bundle size is secondary to full functionality."
    - "D-06: Mac delivery is a documented Homebrew/command-line path rather than a .dmg installer."
    - "D-08: macOS signing is not part of v1.1; README documents the expected unsigned-app warning path rather than requiring Apple Developer ID spend."
    - "D-09: Windows installer artifacts are published for free on GitHub Releases and README links users there."
    - "D-10: Mac easy-path support targets Apple Silicon; Intel Mac users get pipx/uv tool install documentation."
    - "D-11: Linux receives no dedicated install section in README for this phase."
---

<objective>
Ship the approved Windows installer path through GitHub Actions and document Windows/Mac install flows.
</objective>

<context>
@.planning/phases/09-cross-platform-installer/09-CONTEXT.md
@.planning/phases/09-cross-platform-installer/09-RESEARCH.md
@.planning/phases/09-cross-platform-installer/09-PATTERNS.md
@packaging/SPIKE-REPORT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
<task type="manual">
  <name>Task 1: Enforce spike decision</name>
  <files>packaging/RELEASE-CHECKLIST.md</files>
  <read_first>packaging/SPIKE-REPORT.md, .planning/phases/09-cross-platform-installer/09-02-SUMMARY.md</read_first>
  <action>Read `packaging/SPIKE-REPORT.md`. If it does not contain `Recommendation: build-installer` and a recorded user approval, stop and write `packaging/RELEASE-CHECKLIST.md` with the blocked reason and the fallback-docs next step. If approved, create `packaging/RELEASE-CHECKLIST.md` listing required artifacts: PyInstaller onedir smoke pass, Inno Setup installer pass, GitHub Release upload pass, SmartScreen docs present, Mac docs present.</action>
  <verify><manual>User confirms the spike recommendation was approved.</manual></verify>
  <acceptance_criteria>Release work does not proceed without `Recommendation: build-installer` and explicit user approval recorded in the checklist.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Windows installer wrapper</name>
  <files>packaging/installer.iss, packaging/RELEASE-CHECKLIST.md</files>
  <read_first>packaging/cpho.spec, packaging/build_windows.ps1, packaging/smoke_packaged_windows.ps1, pyproject.toml</read_first>
  <action>Create an Inno Setup script `packaging/installer.iss` that packages `dist/cpho/**`, installs to `{autopf}\CPHO CLI`, creates a Start Menu shortcut for `cpho.exe`, includes version from `pyproject.toml`, and does not request admin privileges unless Inno Setup requires it for `{autopf}`. Update `packaging/RELEASE-CHECKLIST.md` with the exact local command for compiling the installer and the expected output filename `cpho-cli-<version>-windows-x64-setup.exe`.</action>
  <verify><automated>uv run python - <<'PY'
from pathlib import Path
s = Path('packaging/installer.iss').read_text(encoding='utf-8')
for needle in ['AppName=CPHO CLI', 'cpho.exe', 'DefaultDirName', 'OutputBaseFilename']:
    assert needle in s
PY</automated></verify>
  <acceptance_criteria>`packaging/installer.iss` produces a named Windows setup executable containing the PyInstaller `dist/cpho` tree.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Release workflow</name>
  <files>.github/workflows/release.yml</files>
  <read_first>.github/workflows/windows-compat.yml, packaging/build_windows.ps1, packaging/smoke_packaged_windows.ps1, packaging/installer.iss, .planning/phases/09-cross-platform-installer/09-RESEARCH.md</read_first>
  <action>Add a release workflow triggered by tags matching `v*`. It must run on `windows-2022`, install uv, run `uv sync --dev`, run tests, build the PyInstaller bundle, run packaged smoke, install/execute Inno Setup for `packaging/installer.iss`, upload the setup executable as a workflow artifact, and upload it to the matching GitHub Release with `gh release upload "$env:GITHUB_REF_NAME" <artifact>` using `GITHUB_TOKEN` and `contents: write`.</action>
  <verify><automated>uv run python - <<'PY'
from pathlib import Path
s = Path('.github/workflows/release.yml').read_text(encoding='utf-8')
for needle in ['windows-2022', 'contents: write', 'gh release upload', 'packaging/installer.iss', 'smoke_packaged_windows.ps1']:
    assert needle in s
PY</automated></verify>
  <acceptance_criteria>Release workflow builds, smokes, artifacts, and uploads the Windows setup executable on `v*` tags.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Install documentation</name>
  <files>README.md, docs/user/install.md, docs/user/README.md</files>
  <read_first>README.md, docs/user/README.md, .planning/phases/09-cross-platform-installer/09-CONTEXT.md, packaging/SPIKE-REPORT.md</read_first>
  <action>Add `docs/user/install.md` with three sections: `Windows` using GitHub Releases installer download from `https://github.com/zyzericzhang/cpho-cli/releases/latest`, `Mac Apple Silicon` using Homebrew install of uv followed by `uv tool install git+https://github.com/zyzericzhang/cpho-cli`, and `Intel Mac / fallback` using pipx or uv tool install. Document SmartScreen as a per-app warning with `More info -> Run anyway`; do not instruct users to disable Defender. Update README Quick Start and docs index to link this install page and remove the old Out of Scope sentence saying cross-platform installers are future work.</action>
  <verify><automated>uv run pytest tests/test_docs_user.py -q</automated></verify>
  <acceptance_criteria>README and docs/user index link `docs/user/install.md`, Windows docs link GitHub Releases, Mac docs mention Homebrew + uv tool install, and SmartScreen docs do not say to disable Defender.</acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Installer ships without user-approved spike | Manual checklist blocks release workflow work until approval is recorded. |
| GitHub token over-permissioned | Release workflow only requires `contents: write` for release upload. |
| Docs encourage unsafe SmartScreen behavior | Install docs describe only the per-app override and explicitly avoid global Defender disablement. |
</threat_model>

<verification>
Run `uv run pytest tests/test_docs_user.py -q`, inspect release workflow assertions, and perform a tag dry-run or documented workflow dispatch rehearsal before publishing a real release.
</verification>

<success_criteria>
An approved Windows installer can be built, smoked, and attached to GitHub Releases, while Windows and Mac users have clear install instructions.
</success_criteria>

<output>
Create `.planning/phases/09-cross-platform-installer/09-04-SUMMARY.md` when done.
</output>
