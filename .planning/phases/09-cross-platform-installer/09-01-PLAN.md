---
phase: 09-cross-platform-installer
plan: 09-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/windows-compat.yml
  - scripts/phase09_windows_smoke.ps1
  - src/cpho_cli/cli/app.py
  - tests/test_cli.py
  - tests/test_phase09_windows_acceptance.py
  - docs/phase9-windows-compat.md
autonomous: true
requirements:
  - INSTALLER-01
must_haves:
  truths:
    - "D-01: Windows compatibility preserves full feature parity with Mac; dependency failures are fixed at root rather than bypassed with feature cuts."
    - "D-02: Chinese and Unicode output on Windows is readable and non-broken, without requiring pixel-identical Mac output."
    - "D-03: Windows verification runs automatically in GitHub Actions on Windows runners for every relevant code update."
---

<objective>
Establish an automated Windows compatibility baseline before any installer work starts.
</objective>

<context>
@.planning/phases/09-cross-platform-installer/09-CONTEXT.md
@.planning/phases/09-cross-platform-installer/09-RESEARCH.md
@.planning/phases/09-cross-platform-installer/09-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Runtime diagnostics command</name>
  <files>src/cpho_cli/cli/app.py, tests/test_cli.py</files>
  <read_first>src/cpho_cli/cli/app.py, pyproject.toml, tests/test_cli.py, .planning/phases/09-cross-platform-installer/09-RESEARCH.md</read_first>
  <action>Add `cpho diagnostics` as a Typer command that prints one `OK` or `FAIL` line for: package version, package data resource lookup for `builtin_skills`, vocabulary YAML, model catalog JSON, `fitz` import, `rapidocr` import, and `onnxruntime` import. Add `--packaging-smoke` to make any FAIL exit nonzero. The command must not call external APIs and must not require private config.</action>
  <verify><automated>uv run pytest tests/test_cli.py -q</automated></verify>
  <acceptance_criteria>`uv run cpho diagnostics --packaging-smoke` exits 0 in the dev environment and prints checks for `fitz`, `rapidocr`, `onnxruntime`, and package data.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Windows CI workflow</name>
  <files>.github/workflows/windows-compat.yml</files>
  <read_first>.planning/phases/09-cross-platform-installer/09-CONTEXT.md, pyproject.toml, tests/test_phase08_acceptance.py</read_first>
  <action>Add a GitHub Actions workflow named `Windows compatibility` that runs on pull_request and push, uses `windows-2022`, checks out the repo, installs uv, runs `uv sync --dev`, runs `uv run pytest -q`, runs `uv run cpho --help`, runs `uv run cpho diagnostics --packaging-smoke`, and runs `pwsh scripts/phase09_windows_smoke.ps1`. The workflow must not require private API keys or the real private workspace.</action>
  <verify><automated>uv run python - <<'PY'
from pathlib import Path
p = Path('.github/workflows/windows-compat.yml')
s = p.read_text(encoding='utf-8')
assert 'windows-2022' in s
assert 'uv run pytest -q' in s
assert 'uv run cpho diagnostics --packaging-smoke' in s
assert 'scripts/phase09_windows_smoke.ps1' in s
PY</automated></verify>
  <acceptance_criteria>.github/workflows/windows-compat.yml contains `windows-2022`, `uv run pytest -q`, `uv run cpho --help`, `uv run cpho diagnostics --packaging-smoke`, and `scripts/phase09_windows_smoke.ps1`.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Windows smoke script</name>
  <files>scripts/phase09_windows_smoke.ps1, docs/phase9-windows-compat.md</files>
  <read_first>README.md, src/cpho_cli/cli/app.py, src/cpho_cli/core/ocr.py, src/cpho_cli/core/boundary.py, .planning/phases/09-cross-platform-installer/09-RESEARCH.md</read_first>
  <action>Create `scripts/phase09_windows_smoke.ps1` that builds a temporary workspace with nested Chinese directory names, generates a one-page PDF in that workspace using PyMuPDF, runs `uv run cpho diagnostics --packaging-smoke`, runs `uv run cpho index <workspace> --dry-run`, and optionally runs the same dry-run path against `$env:CPHO_REAL_WORKSPACE` when that environment variable is set. Create `docs/phase9-windows-compat.md` documenting the smoke commands, the optional real workspace variable, and the expected pass/fail lines.</action>
  <verify><automated>pwsh -NoProfile -File scripts/phase09_windows_smoke.ps1</automated></verify>
  <acceptance_criteria>PowerShell smoke exits 0 on macOS/Linux with PowerShell available or on Windows runner, and docs mention `CPHO_REAL_WORKSPACE` plus Chinese nested-path coverage.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Phase 9 Windows acceptance tests</name>
  <files>tests/test_phase09_windows_acceptance.py</files>
  <read_first>tests/test_phase08_acceptance.py, src/cpho_cli/cli/repl/display.py, src/cpho_cli/core/boundary.py, pyproject.toml</read_first>
  <action>Add acceptance tests that synthesize a nested Chinese workspace path, assert `ensure_workspace_available` and `ensure_in_workspace` preserve paths under that workspace, assert `render_table` handles Chinese headers/cells without negative widths or exceptions, and assert package data listed in `pyproject.toml` covers `builtin_skills`, `core/index/prompts`, `core/knowledge/prompts`, `vocabulary`, and `data/model_catalog`.</action>
  <verify><automated>uv run pytest tests/test_phase09_windows_acceptance.py -q</automated></verify>
  <acceptance_criteria>Acceptance tests prove Windows-relevant path/display/package-data assumptions without using private files.</acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Windows CI passes while package data is missing | Acceptance checks package-data declarations and smoke imports runtime-heavy dependencies. |
| Private workspace leaks into CI | Smoke script uses synthetic fixtures by default and only reads `CPHO_REAL_WORKSPACE` when explicitly set. |
| Unicode path issues hidden by ASCII fixtures | Tests and smoke script use nested Chinese directory names. |
</threat_model>

<verification>
Run `uv run pytest tests/test_phase09_windows_acceptance.py -q`, `pwsh -NoProfile -File scripts/phase09_windows_smoke.ps1`, and verify the GitHub workflow includes the Windows runner/full pytest commands.
</verification>

<success_criteria>
Every push/PR can automatically prove CPHO CLI imports and baseline tests pass on Windows with Chinese-path smoke coverage.
</success_criteria>

<output>
Create `.planning/phases/09-cross-platform-installer/09-01-SUMMARY.md` when done.
</output>
