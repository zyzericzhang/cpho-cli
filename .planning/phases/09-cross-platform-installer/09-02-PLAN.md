---
phase: 09-cross-platform-installer
plan: 09-02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .github/workflows/packaging-spike.yml
  - packaging/cpho.spec
  - packaging/build_windows.ps1
  - packaging/build_nuitka_windows.ps1
  - packaging/smoke_packaged_windows.ps1
  - packaging/SPIKE-REPORT.md
autonomous: false
requirements:
  - INSTALLER-02
must_haves:
  truths:
    - "D-12: The spike passes only if packaging succeeds, CI builds automatically, and clean-VM smoke passes."
    - "D-13: The user personally reviews the spike report and test results before the project commits to build or fallback."
    - "D-14: The target remains a one-click installer; the spike informs the decision and searches for feasibility rather than prematurely giving up."
---

<objective>
Run the 3-day packaging spike as a concrete, reproducible PyInstaller-vs-Nuitka evaluation executed by GitHub Actions on `windows-2022`, with the filled spike report and produced bundles uploaded as CI artifacts before the user decision gate.
</objective>

<context>
@.planning/phases/09-cross-platform-installer/09-CONTEXT.md
@.planning/phases/09-cross-platform-installer/09-RESEARCH.md
@.planning/phases/09-cross-platform-installer/09-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: PyInstaller candidate spec</name>
  <files>packaging/cpho.spec, packaging/build_windows.ps1</files>
  <read_first>pyproject.toml, src/cpho_cli/cli/app.py, src/cpho_cli/core/ocr.py, .planning/phases/09-cross-platform-installer/09-RESEARCH.md</read_first>
  <action>Create `packaging/cpho.spec` for a console onedir build named `cpho`, with UPX disabled, package data collected for `cpho_cli`, `rapidocr`, and `onnxruntime`, and explicit inclusion of `src/cpho_cli/builtin_skills`, `src/cpho_cli/core/index/prompts`, `src/cpho_cli/core/splitting/prompts`, `src/cpho_cli/core/knowledge/prompts`, `src/cpho_cli/vocabulary`, and `src/cpho_cli/data/model_catalog`. Create `packaging/build_windows.ps1` that runs `uv run pyinstaller --clean --noconfirm packaging/cpho.spec` and writes bundle size to `packaging/SPIKE-REPORT.md`.</action>
  <verify><automated>uv run python - <<'PY'
from pathlib import Path
s = Path('packaging/cpho.spec').read_text(encoding='utf-8')
for needle in ['upx=False', 'cpho_cli', 'rapidocr', 'onnxruntime', 'builtin_skills', 'model_catalog']:
    assert needle in s
PY</automated></verify>
  <acceptance_criteria>`packaging/cpho.spec` contains explicit data/binary collection for CPHO package data, RapidOCR, and ONNX Runtime, with UPX disabled.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Nuitka fallback script</name>
  <files>packaging/build_nuitka_windows.ps1, packaging/SPIKE-REPORT.md</files>
  <read_first>pyproject.toml, .planning/phases/09-cross-platform-installer/09-RESEARCH.md</read_first>
  <action>Create `packaging/build_nuitka_windows.ps1` that attempts a Windows standalone build with `python -m nuitka --standalone`, includes package data for `cpho_cli`, includes CPHO prompt/vocabulary/model-catalog directories, records elapsed time, output size, and failure reason into `packaging/SPIKE-REPORT.md`, and exits nonzero only when the script itself is malformed rather than when Nuitka proves unsuitable.</action>
  <verify><automated>uv run python - <<'PY'
from pathlib import Path
s = Path('packaging/build_nuitka_windows.ps1').read_text(encoding='utf-8')
assert '--standalone' in s
assert 'include-package-data' in s or '--include-data-dir' in s
assert 'SPIKE-REPORT.md' in s
PY</automated></verify>
  <acceptance_criteria>Nuitka trial is reproducible and writes a comparable result row even if it fails.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Packaged clean-VM smoke script</name>
  <files>packaging/smoke_packaged_windows.ps1, packaging/SPIKE-REPORT.md</files>
  <read_first>scripts/phase09_windows_smoke.ps1, README.md, src/cpho_cli/cli/app.py</read_first>
  <action>Create `packaging/smoke_packaged_windows.ps1` that accepts a packaged executable path and optional report section label, runs `cpho --help`, runs `cpho diagnostics --packaging-smoke` to prove PyMuPDF, RapidOCR, ONNX Runtime, and packaged CPHO data files load, checks version/update command once it exists or records `PENDING update command`, runs a dry-run index against a generated Chinese workspace, and appends pass/fail output to `packaging/SPIKE-REPORT.md`.</action>
  <verify><automated>uv run python - <<'PY'
from pathlib import Path
s = Path('packaging/smoke_packaged_windows.ps1').read_text(encoding='utf-8')
for needle in ['--help', 'diagnostics --packaging-smoke', 'SPIKE-REPORT.md', '$Label']:
    assert needle in s
PY</automated></verify>
  <acceptance_criteria>Packaged smoke covers startup, runtime-heavy dependencies, package data, and Chinese workspace dry-run.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Windows CI spike executor</name>
  <files>.github/workflows/packaging-spike.yml, packaging/SPIKE-REPORT.md</files>
  <read_first>.github/workflows/windows-compat.yml, packaging/build_windows.ps1, packaging/build_nuitka_windows.ps1, packaging/smoke_packaged_windows.ps1, .planning/phases/09-cross-platform-installer/09-CONTEXT.md</read_first>
  <action>Create a GitHub Actions workflow named `Packaging spike` that runs on `windows-2022`, installs uv/Python, runs `packaging/build_windows.ps1`, runs `packaging/smoke_packaged_windows.ps1` against `dist/cpho/cpho.exe` when produced, runs `packaging/build_nuitka_windows.ps1`, runs the smoke script against the Nuitka executable when produced, appends all build/smoke results to `packaging/SPIKE-REPORT.md`, uploads the filled report as artifact `phase9-spike-report`, and uploads any PyInstaller/Nuitka bundles as separate artifacts. The workflow must still upload the report when a build fails so the spike evidence is not lost.</action>
  <verify><automated>uv run python - <<'PY'
from pathlib import Path
import yaml
p = Path('.github/workflows/packaging-spike.yml')
workflow = yaml.safe_load(p.read_text(encoding='utf-8'))
assert workflow['jobs']['windows-packaging-spike']['runs-on'] == 'windows-2022'
s = p.read_text(encoding='utf-8')
for needle in ['packaging/build_windows.ps1', 'packaging/build_nuitka_windows.ps1', 'packaging/smoke_packaged_windows.ps1', 'actions/upload-artifact@v4', 'phase9-spike-report', 'cpho-pyinstaller-windows', 'cpho-nuitka-windows']:
    assert needle in s
PY</automated></verify>
  <acceptance_criteria>GitHub Actions, not local macOS execution, is the canonical packaging spike runner and preserves both the filled report and build artifacts.</acceptance_criteria>
</task>

<task type="manual">
  <name>Task 5: Spike report and user decision gate</name>
  <files>packaging/SPIKE-REPORT.md</files>
  <read_first>.planning/phases/09-cross-platform-installer/09-CONTEXT.md, .planning/phases/09-cross-platform-installer/09-RESEARCH.md</read_first>
  <action>Prepare `packaging/SPIKE-REPORT.md` as a source-controlled report template with sections `PyInstaller result`, `Nuitka result`, `Bundle size`, `Clean-VM smoke`, `SmartScreen/signing risk`, `Mac scope note`, and `Recommendation`. The template must state that the authoritative filled report is the `phase9-spike-report` GitHub Actions artifact. End with exactly one recommendation line: `Recommendation: build-installer`, `Recommendation: fallback-docs`, or `Recommendation: continue-spike`. Stop after the workflow/report template are ready and ask the user to run/review the CI artifact before executing plans that depend on packaging output.</action>
  <verify><manual>User runs or reviews the `Packaging spike` workflow artifact `phase9-spike-report` and replies with the chosen recommendation.</manual></verify>
  <acceptance_criteria>Spike report has a concrete source-controlled recommendation, CI fills the evidence in an uploaded artifact, and the user personally approves the artifact result before installer release work starts.</acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Spike becomes a disguised final implementation | Plan stops at a manual user decision gate after the CI-filled `phase9-spike-report` artifact is reviewed. |
| Packaged binary starts but misses data files | Smoke checks CPHO prompts/vocabulary/model-catalog resources from the packaged runtime. |
| Local macOS environment produces misleading spike evidence | Windows packaging and smoke execution happen on GitHub Actions `windows-2022`; local checks only validate workflow/script structure. |
| SmartScreen risk is hand-waved | Report must include Microsoft SmartScreen behavior and unsigned/signed expectations. |
</threat_model>

<verification>
Run the `Packaging spike` GitHub Actions workflow on `windows-2022`, confirm artifact `phase9-spike-report` contains build and smoke results, and confirm any produced PyInstaller/Nuitka bundles are uploaded as artifacts.
</verification>

<success_criteria>
The project has reproducible Windows CI packaging evidence, uploaded build artifacts when produced, and an explicit user-approved go/no-go recommendation based on the CI-filled spike report.
</success_criteria>

<output>
Create `.planning/phases/09-cross-platform-installer/09-02-SUMMARY.md` when done.
</output>
