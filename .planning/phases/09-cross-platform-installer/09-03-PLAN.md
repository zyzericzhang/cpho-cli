---
phase: 09-cross-platform-installer
plan: 09-03
type: execute
wave: 3
depends_on:
  - 09-01
files_modified:
  - pyproject.toml
  - src/cpho_cli/__init__.py
  - src/cpho_cli/core/update_check.py
  - src/cpho_cli/models/update.py
  - src/cpho_cli/cli/app.py
  - src/cpho_cli/cli/repl/app.py
  - tests/test_update_check.py
  - tests/test_repl_runtime.py
autonomous: true
requirements:
  - INSTALLER-03
must_haves:
  truths:
    - "D-07: Opening the software automatically checks whether a newer GitHub Release exists and reminds users to download it without requiring manual GitHub monitoring."
---

<objective>
Add version metadata and non-blocking update checks so installed users are told when a GitHub Release is newer.
</objective>

<context>
@.planning/phases/09-cross-platform-installer/09-CONTEXT.md
@.planning/phases/09-cross-platform-installer/09-RESEARCH.md
@.planning/phases/09-cross-platform-installer/09-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Version source of truth</name>
  <files>pyproject.toml, src/cpho_cli/__init__.py, src/cpho_cli/cli/app.py, tests/test_update_check.py</files>
  <read_first>pyproject.toml, src/cpho_cli/__init__.py, src/cpho_cli/cli/app.py, tests/test_cli.py</read_first>
  <action>Add `[project.urls]` with `Homepage = "https://github.com/zyzericzhang/cpho-cli"` and `Repository = "https://github.com/zyzericzhang/cpho-cli"`. Add `get_version()` in `src/cpho_cli/__init__.py` using `importlib.metadata.version("cpho-cli")` with fallback to `0.0.0+unknown`. Add a Typer command `version` that prints `cpho-cli <version>` and the repository URL.</action>
  <verify><automated>uv run pytest tests/test_update_check.py -q</automated></verify>
  <acceptance_criteria>`uv run cpho version` prints a version string and `https://github.com/zyzericzhang/cpho-cli`.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GitHub release update checker</name>
  <files>src/cpho_cli/core/update_check.py, src/cpho_cli/models/update.py, tests/test_update_check.py</files>
  <read_first>src/cpho_cli/core/community_sync.py, src/cpho_cli/core/model_catalog.py, src/cpho_cli/core/llm.py, tests/test_model_catalog.py, tests/test_llm.py</read_first>
  <action>Implement `check_for_update(current_version, repo="zyzericzhang/cpho-cli", client=None, timeout=1.5)` that calls `GET https://api.github.com/repos/{repo}/releases/latest`, parses `tag_name`, strips a leading `v`, compares with the installed version using `packaging.version.Version`, returns an `UpdateCheckResult` model with `available`, `current_version`, `latest_version`, `release_url`, and `error`, and never raises for network/API/parse failures. Add `packaging>=24` only if needed for robust version comparison.</action>
  <verify><automated>uv run pytest tests/test_update_check.py -q</automated></verify>
  <acceptance_criteria>MockTransport tests cover newer release, same release, 404/API error, invalid version tag, and timeout/error paths without real network.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Non-blocking REPL startup notice</name>
  <files>src/cpho_cli/cli/repl/app.py, tests/test_repl_runtime.py</files>
  <read_first>src/cpho_cli/cli/repl/app.py, src/cpho_cli/cli/repl/display.py, tests/test_repl_runtime.py, src/cpho_cli/core/update_check.py</read_first>
  <action>After the REPL banner prints, call the update checker with the short timeout unless `CPHO_DISABLE_UPDATE_CHECK=1`. If a newer release exists, print one concise `display.warn` line containing the latest version and release URL. If the check fails or times out, print nothing and continue startup.</action>
  <verify><automated>uv run pytest tests/test_repl_runtime.py tests/test_update_check.py -q</automated></verify>
  <acceptance_criteria>REPL startup still reaches the prompt when update check fails, and tests prove `CPHO_DISABLE_UPDATE_CHECK=1` suppresses the network check.</acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Startup blocks on GitHub | Update check uses a short timeout, catches failures, and can be disabled with `CPHO_DISABLE_UPDATE_CHECK=1`. |
| Malformed release tag crashes CLI | Version parse errors return an error result and no user warning. |
| Release URL points to placeholder repo | Project URLs and checker use the actual git remote `zyzericzhang/cpho-cli`. |
</threat_model>

<verification>
Run `uv run pytest tests/test_update_check.py tests/test_repl_runtime.py -q` and `uv run cpho version`.
</verification>

<success_criteria>
Installed users see a concise update notice when GitHub Releases has a newer version, while offline startup remains silent and fast.
</success_criteria>

<output>
Create `.planning/phases/09-cross-platform-installer/09-03-SUMMARY.md` when done.
</output>
