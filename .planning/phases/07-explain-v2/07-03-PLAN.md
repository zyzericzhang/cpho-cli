---
phase: 07-explain-v2
plan: 07-03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/cpho_cli/core/input_routing.py
  - src/cpho_cli/core/skill_handlers.py
  - src/cpho_cli/models/runtime.py
  - tests/test_input_routing.py
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
---

<objective>
Add reusable non-index input routing that prefers original PDF/image files when model capabilities allow and records provenance.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Input routing helper and provenance</name>
  <files>src/cpho_cli/core/input_routing.py, tests/test_input_routing.py</files>
  <action>Add helper that chooses `multimodal_pdf`, `multimodal_image`, or `ocr_text` based on source file suffix and `ModelCapabilities`; return warning text for fallback. Keep Index behavior unchanged.</action>
  <verify><automated>uv run pytest tests/test_input_routing.py -q</automated></verify>
  <acceptance_criteria>PDF/image capable models route multimodal; text-only models return OCR fallback and warning.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Skill handler uses requires_multimodal</name>
  <files>src/cpho_cli/core/skill_handlers.py, tests/test_input_routing.py</files>
  <action>Use `SkillStep.requires_multimodal` before attaching source files. When fallback occurs, include a warning field in handler output when the step declares an output key named `input_modality_used` or `input_routing_warning`.</action>
  <verify><automated>uv run pytest tests/test_input_routing.py -q</automated></verify>
  <acceptance_criteria>Existing solve behavior remains compatible; new multimodal-declared steps can expose provenance.</acceptance_criteria>
</task>
</tasks>

<verification>
Run `uv run pytest tests/test_input_routing.py tests/test_skills.py -q`.
</verification>

<success_criteria>
Non-index skill steps can make explicit multimodal/OCR routing decisions and expose provenance.
</success_criteria>

<output>
Create `.planning/phases/07-explain-v2/07-03-SUMMARY.md` when done.
</output>

