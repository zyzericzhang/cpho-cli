---
phase: 07-explain-v2
plan: 07-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cpho_cli/core/model_catalog.py
  - src/cpho_cli/data/model_catalog/openrouter_fallback.json
  - pyproject.toml
  - tests/test_model_catalog.py
autonomous: true
requirements:
  - MODEL-PANEL-03
  - MODEL-PANEL-04
---

<objective>
Implement live provider model-list fetching with TTL cache and bundled fallback.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: OpenRouter model catalog service</name>
  <files>src/cpho_cli/core/model_catalog.py, src/cpho_cli/data/model_catalog/openrouter_fallback.json, pyproject.toml, tests/test_model_catalog.py</files>
  <action>Add model catalog models and OpenRouter fetch/cache/fallback functions. Cache under `~/.cache/cpho/models/`, TTL default 1h, `force_refresh` bypasses cache. Include bundled fallback in package data.</action>
  <verify><automated>uv run pytest tests/test_model_catalog.py -q</automated></verify>
  <acceptance_criteria>Tests cover live fetch via MockTransport, cached reuse, expired refresh, and fallback when network fails.</acceptance_criteria>
</task>
</tasks>

<verification>
Run `uv run pytest tests/test_model_catalog.py -q`.
</verification>

<success_criteria>
Model lists are fetched live when possible, cached, and available offline from bundled fallback.
</success_criteria>

<output>
Create `.planning/phases/07-explain-v2/07-01-SUMMARY.md` when done.
</output>

