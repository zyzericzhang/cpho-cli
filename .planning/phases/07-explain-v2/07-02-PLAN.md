---
phase: 07-explain-v2
plan: 07-02
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - src/cpho_cli/core/skill_config.py
  - src/cpho_cli/cli/repl/commands/model_panel.py
  - src/cpho_cli/cli/repl/commands/__init__.py
  - tests/test_skill_config.py
  - tests/test_repl_model_panel.py
autonomous: true
requirements:
  - MODEL-PANEL-01
  - MODEL-PANEL-02
  - MODEL-PANEL-04
---

<objective>
Expose SkillPipeline metadata and per-step model overrides through REPL commands.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Skill model config layering</name>
  <files>src/cpho_cli/core/skill_config.py, tests/test_skill_config.py</files>
  <action>Implement workspace/user/code-default resolution for per-step model overrides. Persist workspace overrides to `.cpho/skills/<skill_id>.yml`.</action>
  <verify><automated>uv run pytest tests/test_skill_config.py -q</automated></verify>
  <acceptance_criteria>Workspace overrides beat user overrides; code defaults remain fallback.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: REPL model panel commands</name>
  <files>src/cpho_cli/cli/repl/commands/model_panel.py, src/cpho_cli/cli/repl/commands/__init__.py, tests/test_repl_model_panel.py</files>
  <action>Add `/skill panel <name>`, `/skill set-model <name> <step_id> <model>`, and `/model refresh`. Panel prints step id, prompt path, current model, multimodal flag, and dependency edges.</action>
  <verify><automated>uv run pytest tests/test_repl_model_panel.py -q</automated></verify>
  <acceptance_criteria>Commands work without launching prompt_toolkit UI and do not fetch models during REPL startup.</acceptance_criteria>
</task>
</tasks>

<verification>
Run `uv run pytest tests/test_skill_config.py tests/test_repl_model_panel.py -q`.
</verification>

<success_criteria>
Users can inspect a skill pipeline and persist per-step model choices.
</success_criteria>

<output>
Create `.planning/phases/07-explain-v2/07-02-SUMMARY.md` when done.
</output>

