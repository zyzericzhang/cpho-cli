---
context: phase
phase: 02-tag-indexing
task: 02-05
total_tasks: 6
status: paused
last_updated: 2026-05-23T11:52:43.468Z
---

# Phase 2 Pause Handoff

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong-checkout patch target | Some executor agents initially wrote temporary files in the main checkout before correcting to the assigned worktree. The files were removed immediately and were not committed. | advisory | For every resumed executor, create an explicit worktree, include the absolute worktree path in the prompt, and require the first command to verify `pwd -P` and `git rev-parse --abbrev-ref HEAD`. |

<current_state>
Phase 2 is paused after completing Wave 2. Plans 02-01, 02-02, 02-03, 02-04, and 02-06 all have committed SUMMARY files and are merged into `feature/phase2`.

The user explicitly requested: "做完02-04后把工作记录，暂时停止". Do not continue into 02-05 until the user asks to resume.
</current_state>

<completed_work>

Completed plans:
- 02-01: Index models, JSONL storage, vocabulary loader.
- 02-02: Hash primitives, fingerprint composition, `decide_action`.
- 02-06: Builtin vocabulary expanded from user files into split board files; Chinese manual added at `docs/builtin-vocabulary-manual.md`.
- 02-03: OCR cache wrapper and OCR engine upgrade detection.
- 02-04: Versioned prompt templates, canonical mapping pass, LLM provider path, trace writing.

Validation just before pause:
- `uv run pytest -q` -> 130 passed.
- `uv run ruff check src tests` -> passed.
- `uv run mypy src/cpho_cli` -> passed.
</completed_work>

<remaining_work>

- 02-05: Not started. It should wire the builder, CLI, API, notebook stubs, golden fixture, stats, and determinism tests.
- 02-07: Design exists but no PLAN exists yet. It should not be executed until planned.
</remaining_work>

<decisions_made>

- `TagCategory` now uses five values: `physics_law`, `physics_model`, `math_technique`, `heuristic`, `approximation`.
- Old `system_selection` is removed from the enum and folded into `heuristic`.
- `selected_physics_models` in 02-04 accepts both `physics_law` and `physics_model` because the existing `IndexEntry.physics_model_tags` field remains the compatibility bucket for physics tags.
- Tags and topics are separate systems. Tags are flat/multiple; topics are hierarchical/single path.
- Builtin vocabulary uses `src/cpho_cli/vocabulary/builtin.yml` for the 42 core tags plus `src/cpho_cli/vocabulary/builtin/*.yml` for classified extension boards.
</decisions_made>

<blockers>

- No technical blockers.
- Manual cleanup remains useful: review duplicate `internal_id` entries listed in `docs/builtin-vocabulary-manual.md`.
</blockers>

## Required Reading (In Order)

1. `.planning/HANDOFF.json` — machine-readable pause state.
2. `.planning/phases/02-tag-indexing/02-05-PLAN.md` — next executable plan.
3. `.planning/phases/02-tag-indexing/02-01-SUMMARY.md` through `02-04-SUMMARY.md` and `02-06-SUMMARY.md` — what is already built.
4. `.planning/notes/topic-hierarchy-design.md` — required handoff for 02-07 and for keeping tags/topics separate.
5. `docs/builtin-vocabulary-manual.md` — user-facing manual and duplicate tag list.

## Infrastructure State

- No dev server or watcher is running.
- Temporary worktrees for 02-03 and 02-04 were removed.
- Branch is `feature/phase2`.
- Main checkout had workflow-state edits to `.planning/STATE.md` and `.planning/config.json`; they are included in the pause commit.

<context>
The current implementation has Wave 1 and Wave 2 merged. The safest next execution is 02-05 in a fresh worktree from current `feature/phase2`. 02-05 should account for the new category system and split builtin vocabulary.
</context>

<next_action>
Start with: read `02-05-PLAN.md`, verify the current plan index shows only `02-05` incomplete, create a fresh worktree for 02-05, and execute only that plan unless the user explicitly asks to plan 02-07 first.
</next_action>
