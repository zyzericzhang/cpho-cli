---
phase: 02.1-paper-splitting-pdf-phase-1-2
plan: "01"
subsystem: document-models
tags: [pydantic, ocr, pdf, splitting, fixtures]

requires:
  - phase: "02"
    provides: "Existing OCR models and document/workspace contracts"
provides:
  - "Strict PaperFile, ProblemEntry, SplitOutcome contracts"
  - "Deterministic rule splitter for large-problem OCR markers"
  - "Mock OCR and golden PDF splitting fixtures with embedded OCR JSON"
affects: ["phase-02.1", "indexing", "solve-inputs"]

tech-stack:
  added: []
  patterns:
    - "StrictModel contracts for persisted split outputs"
    - "Linear anchored-regex OCR marker splitting"

key-files:
  created:
    - "src/cpho_cli/core/splitting/__init__.py"
    - "src/cpho_cli/core/splitting/rules.py"
    - "tests/test_splitting_models.py"
    - "tests/test_splitting_rules.py"
    - "tests/fixtures/paper_with_5_problems.json"
    - "golden_tests/splitting/ipho_style_multi_problem.pdf"
    - "golden_tests/splitting/ipho_style_multi_problem_answers.pdf"
    - "golden_tests/splitting/ipho_style_multi_problem.expected.json"
  modified:
    - "src/cpho_cli/models/documents.py"

key-decisions:
  - "Problem IDs use paper_sha256 plus two-digit problem number, e.g. paper-sha:01."
  - "Rule splitting stores virtual ProblemEntry ranges instead of materializing per-problem PDFs."
  - "Golden fixtures embed OCRResult JSON so later tests can avoid unstable OCR."

patterns-established:
  - "Split validators protect the local OCR JSON to persisted model boundary."
  - "Rule split diagnostics are returned as strings rather than raised exceptions."

requirements-completed: [PS-01, PS-02, PS-06]

duration: 5min
completed: 2026-05-24
---

# Phase 02.1 Plan 01: Paper Splitting Foundation Summary

**Strict paper/problem contracts plus deterministic OCR rule splitting and committed golden PDF fixtures.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-24T02:31:23Z
- **Completed:** 2026-05-24T02:36:12Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Added `PaperFile`, `ProblemEntry`, `SplitOutcome`, `SplitMethod.SINGLE`, and deterministic `make_problem_id()` contracts.
- Added a rule splitter that recognizes `第N题`, `题N`, `(N)`, `N.`, and `Problem N` markers from OCR pages.
- Added a seven-page five-problem OCR fixture and golden paper/answer PDFs with embedded deterministic OCR JSON.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Define paper and problem-entry contracts tests** - `8a7d50d` (test)
2. **Task 1 GREEN: Define paper and problem-entry contracts** - `84ecac2` (feat)
3. **Task 2: Add mock and golden splitting fixtures** - `a618079` (feat)
4. **Task 3 RED: Implement deterministic rule splitting tests** - `5b2a4fb` (test)
5. **Task 3 GREEN: Implement deterministic rule splitting** - `6615c2a` (feat)

**Plan metadata:** committed in closeout commit.

## Files Created/Modified

- `src/cpho_cli/models/documents.py` - Added strict paper/problem split contracts and compatibility access for `unmatched_problems`.
- `src/cpho_cli/core/splitting/__init__.py` - Exports the rule splitting API.
- `src/cpho_cli/core/splitting/rules.py` - Implements deterministic marker detection, split materialization, answer matching, and diagnostics.
- `tests/test_splitting_models.py` - Covers strict model validation and legacy import compatibility.
- `tests/test_splitting_rules.py` - Covers fixture splitting, answer attachment, unmatched answers, and invalid-shape diagnostics.
- `tests/fixtures/paper_with_5_problems.json` - Mock OCRResult JSON with seven pages and five marker styles.
- `golden_tests/splitting/ipho_style_multi_problem.pdf` - Committed multi-page problem PDF fixture.
- `golden_tests/splitting/ipho_style_multi_problem_answers.pdf` - Committed answer PDF fixture.
- `golden_tests/splitting/ipho_style_multi_problem.expected.json` - Manual expected annotations plus deterministic OCR pages.

## Decisions Made

- Used `StrictModel` only for new persisted split contracts, leaving legacy `ProblemFile` and workspace pair classes import-compatible.
- Kept splitting output virtual with page ranges; no derived per-problem PDF files are generated.
- Used deterministic generated PDFs for the initial golden fixture while matching the real workspace shape of paper PDF plus answer PDF.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added splitting package initializer**
- **Found during:** Task 3 (Implement deterministic rule splitting)
- **Issue:** The new `cpho_cli.core.splitting.rules` module needs a package directory to import reliably and expose the planned exports.
- **Fix:** Added `src/cpho_cli/core/splitting/__init__.py` exporting `split_paper_by_rules` and `validate_rule_split`.
- **Files modified:** `src/cpho_cli/core/splitting/__init__.py`
- **Verification:** `uv run pytest tests/test_splitting_rules.py tests/test_splitting_models.py -x`
- **Committed in:** `6615c2a`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Import support only; no scope expansion beyond the planned rule splitter API.

## Issues Encountered

None.

## Verification Results

- `uv run pytest tests/test_splitting_models.py tests/test_splitting_rules.py -x` - PASS, 22 tests.
- `uv run python -c "import json, pathlib; p=pathlib.Path('golden_tests/splitting/ipho_style_multi_problem.expected.json'); d=json.loads(p.read_text()); assert 'problem_ocr_pages' in d and 'answer_ocr_pages' in d; assert pathlib.Path('golden_tests/splitting/ipho_style_multi_problem.pdf').exists(); assert pathlib.Path('golden_tests/splitting/ipho_style_multi_problem_answers.pdf').exists()"` - PASS.
- Fixture acceptance smoke with `OCRResult.model_validate()` for mock, problem OCR, and answer OCR - PASS.

## Known Stubs

None. Stub scan only matched intentional empty diagnostics/test list literals.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 02 can build single-file/single-page split paths on top of `SplitMethod.SINGLE` and the new `ProblemEntry` contract. Plan 05 can consume the golden fixture without invoking live OCR because expected JSON already embeds `OCRResult`-shaped pages.

## Self-Check: PASSED

- Created files exist on disk.
- Task commits `8a7d50d`, `84ecac2`, `a618079`, `5b2a4fb`, and `6615c2a` exist in git history.
- Plan-level verification commands passed.

---
*Phase: 02.1-paper-splitting-pdf-phase-1-2*
*Completed: 2026-05-24*
