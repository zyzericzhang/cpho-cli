---
phase: 02.1-paper-splitting-pdf-phase-1-2
plan: "02"
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/cpho_cli/core/splitting/__init__.py
  - src/cpho_cli/core/splitting/llm.py
  - src/cpho_cli/core/splitting/prompts/MANIFEST.yml
  - src/cpho_cli/core/splitting/prompts/__init__.py
  - src/cpho_cli/core/splitting/prompts/split_paper.md.j2
  - pyproject.toml
  - tests/test_splitting_llm.py
  - tests/test_splitting_fallback.py
  - tests/test_splitting_integration.py
autonomous: true
requirements: [PS-03, PS-04]
user_setup: []
must_haves:
  truths:
    - "LLM fallback uses the existing `LLMProvider` protocol and never performs direct HTTP."
    - "`split_paper()` runs rules first and falls back to LLM only for invalid rule outcomes."
    - "Image files and one-page non-PDF inputs produce exactly one `SplitMethod.SINGLE` problem without LLM fallback."
  artifacts:
    - path: "src/cpho_cli/core/splitting/__init__.py"
      provides: "Public split_paper orchestrator"
      exports: ["split_paper"]
    - path: "src/cpho_cli/core/splitting/llm.py"
      provides: "Provider-backed LLM split fallback and prompt version loader"
      exports: ["split_paper_with_llm", "load_split_prompt_version"]
    - path: "src/cpho_cli/core/splitting/prompts/split_paper.md.j2"
      provides: "Versioned Jinja2 prompt template"
  key_links:
    - from: "src/cpho_cli/core/splitting/__init__.py"
      to: "src/cpho_cli/core/splitting/rules.py"
      via: "rules-first split attempt"
      pattern: "split_paper_by_rules"
    - from: "src/cpho_cli/core/splitting/__init__.py"
      to: "src/cpho_cli/core/splitting/llm.py"
      via: "fallback on diagnostics"
      pattern: "split_paper_with_llm"
---

<objective>
Implement the provider-backed split fallback and the public split orchestrator, including explicit single-image/single-page behavior.

Purpose: PS-03 and PS-04 require rules-first splitting with LLM fallback, but the checker identified that image workspaces must remain on a single-problem path rather than triggering fallback.
Output: Versioned split prompt, LLM JSON conversion, `split_paper()`, fallback trigger tests, integration tests, and single-image tests.
</objective>

<execution_context>
@/Users/ericzhang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericzhang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md
@src/cpho_cli/core/llm.py
@src/cpho_cli/core/splitting/rules.py
@src/cpho_cli/models/config.py
@src/cpho_cli/models/documents.py
@src/cpho_cli/models/ocr.py
@pyproject.toml

<interfaces>
`LLMProvider.complete(messages, params, response_model=None)` returns `LLMResponse` and is the only allowed LLM boundary.
`ModelParams` has `name`, `temperature`, and `max_tokens`; use the resolved caller params rather than hardcoded model choices.
`SplitOutcome` and `ProblemEntry` come from Plan 01 and are the public return shape.
</interfaces>
</context>

<source_audit>
GOAL covered: turns OCR pages for a paper/answer pair into problem entries with answer pairing.
REQ covered: PS-03, PS-04, plus checker-required single-image path.
RESEARCH excluded: research was skipped by explicit user request.
CONTEXT excluded: no CONTEXT.md; no locked decisions or deferred ideas were provided.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add versioned split prompt and LLM fallback</name>
  <files>src/cpho_cli/core/splitting/llm.py, src/cpho_cli/core/splitting/prompts/MANIFEST.yml, src/cpho_cli/core/splitting/prompts/__init__.py, src/cpho_cli/core/splitting/prompts/split_paper.md.j2, pyproject.toml, tests/test_splitting_llm.py</files>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` requirement PS-03 and split prompt version constraint.
    - `src/cpho_cli/core/llm.py` `LLMProvider.complete()` and `response_model` contract.
    - `src/cpho_cli/models/config.py` `ModelParams` and `StrictModel` patterns.
    - `src/cpho_cli/models/documents.py` `ProblemEntry`, `SplitOutcome`, and `SplitMethod` contracts from Plan 01.
    - `pyproject.toml` existing package-data patterns.
  </read_first>
  <action>Create the splitting prompt package for PS-03. Add `MANIFEST.yml` with a concrete `split_prompt_version`, `split_paper.md.j2`, and package-data inclusion in `pyproject.toml` for `core/splitting/prompts/*`. Implement `load_split_prompt_version()` and `split_paper_with_llm(paper_ocr, answer_ocr=None, *, paper_file, answer_file=None, paper_sha256, provider, params)`. The function must call `provider.complete(..., response_model=...)` using an internal Pydantic response model for problem numbers, page ranges, answer ranges, and mapping; convert validated JSON to `ProblemEntry` and `SplitOutcome` with `SplitMethod.LLM`. Tests should use a fake `LLMProvider`, assert `response_model` is supplied, assert prompt version is loadable, and assert malformed model output fails before any index write path.</action>
  <verify>
    <automated>uv run pytest tests/test_splitting_llm.py -x</automated>
  </verify>
  <done>LLM fallback is provider-based, prompt-versioned, package-included, and covered by fake-provider tests.</done>
  <acceptance_criteria>
    - `load_split_prompt_version()` returns the manifest version and package data includes the splitting prompt files.
    - `split_paper_with_llm()` calls only `LLMProvider.complete()` and passes a `response_model`.
    - Fake-provider tests prove valid JSON becomes `SplitOutcome` and malformed JSON is rejected before persistence.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rules-first split orchestrator</name>
  <files>src/cpho_cli/core/splitting/__init__.py, tests/test_splitting_fallback.py, tests/test_splitting_integration.py</files>
  <behavior>
    - Valid rule output returns without calling the LLM provider.
    - Rule diagnostics for zero problems, non-contiguous numbers, duplicates, or answer-number mismatch trigger LLM fallback.
    - Fallback requires an explicit provider; when required and absent, the orchestrator raises a clear error naming LLM provider configuration.
    - Answer pairing is by problem number and unmatched answers are preserved.
  </behavior>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` requirement PS-04.
    - `src/cpho_cli/core/splitting/rules.py` `split_paper_by_rules()` and `validate_rule_split()` contracts from Plan 01.
    - `src/cpho_cli/core/splitting/llm.py` `split_paper_with_llm()` contract from Task 1.
    - `src/cpho_cli/models/documents.py` `PaperFile`, `SplitOutcome`, and `SplitMethod`.
  </read_first>
  <action>Create `split_paper()` in `src/cpho_cli/core/splitting/__init__.py` for PS-04. It must accept paper OCR, optional answer OCR, paper/answer `PaperFile`, `paper_sha256`, optional `llm_provider`, and optional `ModelParams`. Run `split_paper_by_rules()` first, validate diagnostics, and return rules output when diagnostics are empty. If diagnostics require fallback, call `split_paper_with_llm()` with the provided provider and params. Do not construct providers in this module; Plan 04 handles builder-level provider construction. Add tests that monkeypatch the LLM fallback function to prove trigger conditions and non-trigger conditions without live network calls.</action>
  <verify>
    <automated>uv run pytest tests/test_splitting_fallback.py tests/test_splitting_integration.py -x</automated>
  </verify>
  <done>`split_paper()` is importable and routes rules, fallback, and answer pairing according to PS-04.</done>
  <acceptance_criteria>
    - Valid rule diagnostics return without any fake LLM provider calls.
    - Invalid rule diagnostics call the LLM fallback exactly once when a provider is available.
    - Missing provider during required fallback raises a clear, tested exception.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add explicit single-image and single-page path</name>
  <files>src/cpho_cli/core/splitting/__init__.py, tests/test_splitting_integration.py</files>
  <behavior>
    - `.png`, `.jpg`, `.jpeg`, `.webp`, `.tif`, and `.tiff` papers return one ProblemEntry with `SplitMethod.SINGLE`.
    - A one-page non-PDF OCR input with no large-problem markers returns one ProblemEntry with `problem_page_range == (1, 1)`.
    - The single path does not call rules fallback LLM, even when no markers are found.
  </behavior>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` image-workspace boundary and PS-04 single-problem behavior.
    - `src/cpho_cli/core/splitting/__init__.py` `split_paper()` implementation from Task 2.
    - `src/cpho_cli/models/documents.py` `SplitMethod.SINGLE` and `ProblemEntry` validation.
    - `tests/test_splitting_integration.py` existing integration fixture style.
  </read_first>
  <action>Extend `split_paper()` with the checker-required single path. If `paper_file.path.suffix.lower()` is an image extension, or if `paper_file.total_pages == 1` and rule detection finds no problem markers, return a single `ProblemEntry` using all paper OCR text and optional all answer OCR text. Set `split_method=SplitMethod.SINGLE`, `split_confidence=1.0`, `problem_number=1`, and deterministic `problem_id`. This preserves the spec boundary that image workspaces stay single-problem and do not enter multi-problem split. Add tests for `.png` and `.jpg` workspaces and for one-page non-PDF OCR inputs; assert no fake LLM calls occur.</action>
  <verify>
    <automated>uv run pytest tests/test_splitting_integration.py -x</automated>
  </verify>
  <done>Image and single-page no-marker inputs are handled as one problem and cannot trigger unnecessary LLM fallback.</done>
  <acceptance_criteria>
    - Supported image suffixes produce one `ProblemEntry` with page range `(1, 1)` and `SplitMethod.SINGLE`.
    - One-page no-marker inputs produce one problem without LLM fallback.
    - Multi-page no-marker PDFs still use the normal diagnostics/fallback path.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCR text to LLM provider | Local paper text leaves deterministic code and enters a configured external model provider only on fallback. |
| LLM JSON to split contracts | Model output controls problem ranges and answer mapping before validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-02-01 | Information Disclosure | `split_paper_with_llm` | mitigate | Only call LLM on explicit fallback; tests prove valid rules and single-image paths avoid provider calls. |
| T-02.1-02-02 | Tampering | LLM split JSON | mitigate | Validate structured response through Pydantic before converting to `ProblemEntry`. |
| T-02.1-02-03 | Denial of Service | fallback routing | mitigate | Single-image and single-page no-marker inputs return immediately as `SplitMethod.SINGLE`. |
| T-02.1-02-SC | Tampering | package installs | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run `uv run pytest tests/test_splitting_llm.py tests/test_splitting_fallback.py tests/test_splitting_integration.py -x`.
</verification>

<success_criteria>
PS-03 and PS-04 are satisfied; LLM fallback is versioned and provider-backed; image and single-page no-marker inputs reliably produce one problem without LLM calls.
</success_criteria>

<output>
Create `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-02-SUMMARY.md` when done.
</output>
