---
phase: 02.1-paper-splitting-pdf-phase-1-2
plan: "03"
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/cpho_cli/core/workspace.py
  - src/cpho_cli/models/index.py
  - src/cpho_cli/core/index/hashing.py
  - src/cpho_cli/core/index/storage.py
  - src/cpho_cli/core/index/ocr_cache.py
  - tests/test_workspace.py
  - tests/test_index_models.py
  - tests/test_index_hashing.py
  - tests/test_index_storage.py
  - tests/test_index_api.py
  - tests/test_topic_api.py
  - tests/test_compose.py
  - tests/test_topic_models.py
  - tests/test_index_ocr_upgrade.py
autonomous: true
requirements: [PS-01, PS-05]
user_setup: []
must_haves:
  truths:
    - "Workspace discovery returns paper-level pairs while preserving image files as single paper inputs."
    - "IndexEntry persists per-problem page ranges and split prompt version participates in semantic fingerprinting."
    - "Old pre-02.1 index rows are treated as stale and discarded for rebuild rather than crashing before rebuild."
  artifacts:
    - path: "src/cpho_cli/core/workspace.py"
      provides: "PaperFile-based workspace discovery"
    - path: "src/cpho_cli/models/index.py"
      provides: "IndexEntry.problem_page_range and SemanticFingerprint.split_prompt_version"
    - path: "src/cpho_cli/core/index/storage.py"
      provides: "Stale index handling for schema-version mismatch or old rows"
  key_links:
    - from: "src/cpho_cli/core/index/hashing.py"
      to: "src/cpho_cli/models/index.py"
      via: "split_prompt_version in SemanticFingerprint"
      pattern: "split_prompt_version"
    - from: "src/cpho_cli/core/index/storage.py"
      to: "src/cpho_cli/core/index/builder.py"
      via: "safe existing-index load result consumed in Plan 04"
      pattern: "load_existing_index_for_rebuild"
---

<objective>
Upgrade discovery and index persistence contracts so the builder can safely rebuild old indexes into per-problem rows.

Purpose: PS-05 changes the persisted index schema, and the checker found that old `index.jsonl` rows must not crash a rebuild before they can be discarded.
Output: Paper-level discovery, split-aware index schema/fingerprints, and stale-index loading semantics with regression tests.
</objective>

<execution_context>
@/Users/ericzhang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericzhang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md
@AGENTS.md
@src/cpho_cli/core/workspace.py
@src/cpho_cli/models/documents.py
@src/cpho_cli/models/index.py
@src/cpho_cli/core/index/hashing.py
@src/cpho_cli/core/index/storage.py

<interfaces>
Existing `discover_workspace(root)` classifies supported files and answer markers from `SUPPORTED_EXTENSIONS`, `ANSWER_MARKERS`, and `ANSWER_DIRS`.
Existing `IndexEntry` requires `problem_id`, `problem_path`, optional `answer_path`, `fingerprint`, `ocr_text_length`, and `tag_prompt_version`.
Existing `load_index(workspace_root)` validates every JSONL line into `IndexEntry` and currently raises on old schema rows.
Existing `detect_ocr_engine_upgrade()` in `src/cpho_cli/core/index/ocr_cache.py` currently calls strict `load_index()` before rebuild decisions.
</interfaces>
</context>

<source_audit>
GOAL covered: prepares discovery and persisted index contracts for problem-level indexing.
REQ covered: PS-01 discovery compatibility and PS-05 index schema/hash/stale-index behavior.
RESEARCH excluded: research was skipped by explicit user request.
CONTEXT excluded: no CONTEXT.md; no locked decisions or deferred ideas were provided.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade workspace discovery to paper-level results</name>
  <files>src/cpho_cli/core/workspace.py, tests/test_workspace.py</files>
  <behavior>
    - Problem PDFs and answer PDFs are paired as `PaperAnswerPair(PaperFile, PaperFile | None)`.
    - `.png` and `.jpg` problem files are discovered as `PaperFile` records with `total_pages == 1`.
    - Existing answer-marker heuristics still match Chinese `解析` and `答案` filenames from the real workspace sample.
    - Ambiguous and unmatched outputs use paper-level types.
  </behavior>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` requirements PS-04 and PS-05.
    - `src/cpho_cli/core/workspace.py` existing answer-marker and safe file traversal logic.
    - `src/cpho_cli/models/documents.py` `PaperFile`, `PaperAnswerPair`, `AmbiguousPaperMatch`, and compatibility properties from Plan 01.
    - `AGENTS.md` real-workspace requirement and the sampled filenames under `/Users/ericzhang/Desktop/物理竞赛资料`.
  </read_first>
  <action>Modify `src/cpho_cli/core/workspace.py` for PS-04/PS-05 compatibility. Return `WorkspaceDiscoveryResult` containing `PaperAnswerPair`, `PaperFile`, and `AmbiguousPaperMatch` from Plan 01. Preserve `SUPPORTED_EXTENSIONS` and the existing answer-marker matching semantics. Determine `PaperFile.total_pages` cheaply: image extensions are 1; PDFs use the existing document/PyMuPDF path if available without OCR, falling back to 1 only when the file cannot be opened and recording no hard failure in discovery. Add tests with filenames shaped like the sampled real workspace, including `力学2试题.pdf` with `力学2解析.pdf`, `理论5试题.pdf` with `理论5-答案.pdf`, and standalone `.png`/`.jpg` problems.</action>
  <verify>
    <automated>uv run pytest tests/test_workspace.py -x</automated>
  </verify>
  <done>Workspace discovery emits paper-level pairs and preserves standalone images for the single path in Plan 02.</done>
  <acceptance_criteria>
    - `discover_workspace()` returns paper-level pairs and unmatched paper records.
    - Chinese answer markers `解析` and `答案` still pair with corresponding problem PDFs.
    - Image files are discovered with `total_pages == 1` and can flow into Plan 02's single path.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add split-aware index schema and fingerprint fields</name>
  <files>src/cpho_cli/models/index.py, src/cpho_cli/core/index/hashing.py, tests/test_index_models.py, tests/test_index_hashing.py, tests/test_index_api.py, tests/test_topic_api.py, tests/test_compose.py, tests/test_topic_models.py, tests/test_index_ocr_upgrade.py</files>
  <behavior>
    - `IndexEntry.problem_page_range` is required and validates as a 1-indexed inclusive two-item tuple.
    - `SemanticFingerprint.split_prompt_version` is required.
    - Changing only `split_prompt_version` causes `decide_action()` to return `re_tag_only` for an otherwise unchanged old entry.
    - `IndexRunStats` includes `papers_split`, `problems_extracted`, `split_method_rules`, `split_method_llm`, and `split_method_single`.
    - Every existing test/factory construction of `IndexEntry(` and `SemanticFingerprint(` supplies the new required fields.
  </behavior>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` requirement PS-05 and no-migration boundary.
    - `src/cpho_cli/models/index.py` `IndexEntry`, `SemanticFingerprint`, and `IndexRunStats`.
    - `src/cpho_cli/core/index/hashing.py` `TAG_SCHEMA_VERSION`, `compose_semantic_fingerprint()`, and `decide_action()`.
    - `tests/test_index_api.py`, `tests/test_topic_api.py`, `tests/test_compose.py`, `tests/test_topic_models.py`, and `tests/test_index_ocr_upgrade.py` existing `IndexEntry(` and `SemanticFingerprint(` factories.
  </read_first>
  <action>Update PS-05 index contracts. Add required `problem_page_range` to `IndexEntry` with the same validation as `ProblemEntry`; do not make it optional because the checker explicitly requires old rows missing the field to be stale. Add required `split_prompt_version` to `SemanticFingerprint` and thread it through `compose_semantic_fingerprint()`. Increment `TAG_SCHEMA_VERSION` because the persisted schema changes. Add split counters to `IndexRunStats`. Update every existing test/factory that instantiates `IndexEntry(` or `SemanticFingerprint(`, explicitly including `tests/test_index_api.py`, `tests/test_topic_api.py`, `tests/test_compose.py`, `tests/test_topic_models.py`, and `tests/test_index_ocr_upgrade.py`, so all constructions pass concrete page ranges and split prompt versions before final full pytest. Add a grep gate after edits using `rg -n "IndexEntry\\(|SemanticFingerprint\\(" tests src` and inspect every result; no call site may rely on the old schema.</action>
  <verify>
    <automated>uv run pytest tests/test_index_models.py tests/test_index_hashing.py tests/test_index_api.py tests/test_topic_api.py tests/test_compose.py tests/test_topic_models.py tests/test_index_ocr_upgrade.py tests/test_index_stats.py -x</automated>
    <automated>rg -n "IndexEntry\\(|SemanticFingerprint\\(" tests src</automated>
  </verify>
  <done>Index schema and fingerprint decisions reflect per-problem ranges and split prompt versioning.</done>
  <acceptance_criteria>
    - `IndexEntry.problem_page_range` and `SemanticFingerprint.split_prompt_version` are required in production models.
    - All listed legacy test factories are updated to the new schema.
    - The grep gate has no unreviewed old-schema call sites; every `IndexEntry(` includes `problem_page_range` and every direct `SemanticFingerprint(` includes `split_prompt_version`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add stale-index loading before rebuild and OCR-upgrade prompts</name>
  <files>src/cpho_cli/core/index/storage.py, src/cpho_cli/core/index/ocr_cache.py, tests/test_index_storage.py, tests/test_index_ocr_upgrade.py</files>
  <behavior>
    - `load_index()` remains strict for query/API callers and still raises on invalid JSONL.
    - A new rebuild helper catches old schema validation failures or schema-version mismatches and returns an empty existing-entry set plus a stale reason.
    - A regression fixture with pre-02.1 rows missing `problem_page_range` and `split_prompt_version` does not crash the rebuild helper.
    - `detect_ocr_engine_upgrade()` does not crash on stale pre-02.1 rows before rebuild; it treats stale-schema rows as rebuild-needed/no-prompt data rather than calling strict `load_index()` uncaught.
  </behavior>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` PS-05 no-migration/full-rebuild boundary.
    - `src/cpho_cli/core/index/storage.py` strict `load_index()` and `write_index()`.
    - `src/cpho_cli/core/index/ocr_cache.py` `detect_ocr_engine_upgrade()` current strict load path.
    - `src/cpho_cli/core/index/builder.py` OCR strategy handling that calls `detect_ocr_engine_upgrade()` before loading existing entries.
    - `tests/test_index_ocr_upgrade.py` existing OCR-upgrade prompt tests.
  </read_first>
  <action>Add a narrow helper such as `load_existing_index_for_rebuild(workspace_root, expected_schema_version)` to `src/cpho_cli/core/index/storage.py`. It should call strict validation internally but catch `pydantic.ValidationError`, JSON decoding errors, or any entry whose fingerprint semantic schema version does not equal `expected_schema_version`. On stale data, return no entries and a reason string so Plan 04 can rebuild from scratch and not copy obsolete whole-paper rows into `result_entries`. Do not weaken `load_index()` because strict reads are still correct for normal query APIs. Update `src/cpho_cli/core/index/ocr_cache.py` so `detect_ocr_engine_upgrade()` no longer lets stale pre-02.1 validation failures crash `cpho index` before rebuild. Use the rebuild-safe loader or catch the same stale-schema validation conditions locally; if the index is stale because required 02.1 fields are missing, return `None` so `ocr_strategy="prompt"` does not ask the user about OCR upgrades for rows that will be discarded. Add tests in `tests/test_index_storage.py` and `tests/test_index_ocr_upgrade.py` writing a pre-02.1 `index.jsonl` row without `problem_page_range` and without `split_prompt_version`; assert the helper returns empty entries and `detect_ocr_engine_upgrade()` returns `None` instead of raising. Plan 04 adds the end-to-end `build_index(..., ocr_strategy="prompt")` regression through the builder.</action>
  <verify>
    <automated>uv run pytest tests/test_index_storage.py tests/test_index_ocr_upgrade.py -x</automated>
  </verify>
  <done>Rebuild code can safely discard stale old index rows, while strict index reads remain strict.</done>
  <acceptance_criteria>
    - Strict `load_index()` still raises for invalid or old-schema JSONL.
    - Rebuild-safe loading returns an empty entry set and stale reason for pre-02.1 rows.
    - OCR-upgrade detection returns `None` for stale pre-02.1 rows and preserves existing delta behavior for valid current-schema rows.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem to workspace discovery | User-controlled filenames and paths determine paper/answer classification. |
| persisted JSONL to rebuild logic | Old or tampered index rows are read before rebuild decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-03-01 | Tampering | stale `index.jsonl` | mitigate | Strict `load_index()` stays strict; rebuild helper discards stale schema rows and prevents copying obsolete whole-paper entries. |
| T-02.1-03-02 | Spoofing | answer pairing by filename | accept | Pairing remains local heuristic from Phase 1; ambiguous matches are surfaced as ambiguous paper matches and skipped by builder. |
| T-02.1-03-03 | Repudiation | split prompt cache changes | mitigate | Persist `split_prompt_version` inside `SemanticFingerprint` and include it in cache decisions. |
| T-02.1-03-SC | Tampering | package installs | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run `uv run pytest tests/test_workspace.py tests/test_index_models.py tests/test_index_hashing.py tests/test_index_storage.py tests/test_index_api.py tests/test_topic_api.py tests/test_compose.py tests/test_topic_models.py tests/test_index_ocr_upgrade.py tests/test_index_stats.py -x`, then run the grep gate from Task 2 and inspect every reported call site.
</verification>

<success_criteria>
Workspace discovery returns paper-level inputs, index rows require per-problem ranges, split prompt version participates in semantic fingerprints, and old pre-02.1 index rows are discarded during rebuild.
</success_criteria>

<output>
Create `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-03-SUMMARY.md` when done.
</output>
