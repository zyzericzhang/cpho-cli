---
phase: 01-core-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - README.md
  - src/cpho_cli/__init__.py
  - src/cpho_cli/cli/__init__.py
  - src/cpho_cli/cli/app.py
  - src/cpho_cli/core/__init__.py
  - src/cpho_cli/core/config.py
  - src/cpho_cli/models/__init__.py
  - src/cpho_cli/models/config.py
  - tests/test_cli.py
  - tests/test_config.py
autonomous: true
requirements:
  - CORE-01
user_setup: []
must_haves:
  truths:
    - "CORE-01/D-01: `uv sync` installs a Python >=3.11 src-layout package named `cpho-cli`."
    - "CORE-01/D-04: `uv run cpho --help`, `uv run ruff check .`, `uv run mypy .`, and `uv run pytest` are valid project commands."
    - "CORE-01/D-10/D-13: config resolution reads `OPENROUTER_API_KEY`, supports a local YAML config path, and never hardcodes an API key."
  artifacts:
    - pyproject.toml
    - src/cpho_cli/cli/app.py
    - src/cpho_cli/core/config.py
    - src/cpho_cli/models/config.py
    - tests/test_cli.py
    - tests/test_config.py
  key_links:
    - "pyproject.toml console script `cpho` -> src/cpho_cli/cli/app.py Typer app"
    - "src/cpho_cli/cli/app.py -> src/cpho_cli/core/config.py for non-interactive config loading"
    - "src/cpho_cli/core/config.py -> src/cpho_cli/models/config.py for Pydantic validation"
---

<objective>
Create the Python project foundation for CPHO CLI.

Purpose: This plan establishes the uv-managed src-layout project, command entrypoint, config model, API key loading, and quality gates required by D-01 through D-04, D-10, and D-13.
Output: A runnable `cpho` CLI shell with validated config loading and automated tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-PATTERNS.md
</context>

<threat_model>
ASVS level: 1
Block on severity: high

Threats:
- HIGH: OpenRouter API keys could be hardcoded, committed, printed, or written to trace output.
- MEDIUM: Local YAML config can be malformed or include unexpected fields.
- LOW: CLI help or error output can expose local config paths unnecessarily.

Required mitigations:
- Read API key only from `OPENROUTER_API_KEY` or a local config file outside committed examples.
- Add `.gitignore` entries for `.env`, local config, output traces, and evaluation outputs.
- Validate config with Pydantic and reject unknown top-level config keys.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create uv src-layout package and CLI entrypoint</name>
  <files>pyproject.toml, uv.lock, .gitignore, README.md, src/cpho_cli/__init__.py, src/cpho_cli/cli/__init__.py, src/cpho_cli/cli/app.py, src/cpho_cli/core/__init__.py, src/cpho_cli/models/__init__.py, tests/test_cli.py</files>
  <read_first>AGENTS.md, README.md, .planning/phases/01-core-foundation/01-CONTEXT.md, .planning/phases/01-core-foundation/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `uv run cpho --help` exits 0 and includes command names `solve` and `eval`.
    - Test 2: `CliRunner().invoke(app, ["--help"])` exits 0.
  </behavior>
  <action>Create `pyproject.toml` per D-01/D-02/D-03 with project name `cpho-cli`, Python requirement `>=3.11`, console script `cpho = "cpho_cli.cli.app:app"`, dependencies `typer`, `pydantic`, `pyyaml`, `jinja2`, `httpx`, `pymupdf`, `rapidocr`, and `onnxruntime`, and dev dependencies `pytest`, `ruff`, and `mypy`. Create `src/cpho_cli/cli/app.py` with a Typer app exposing `solve` and `eval` command stubs that return clear "not implemented yet" messages through Typer without importing OCR or LLM code. Update README with uv command examples from D-04. Add `.gitignore` entries for `.env`, `.cpho/`, `output/`, `traces/`, `*.trace.jsonl`, and local config files.</action>
  <verify>
    <automated>uv sync && uv run cpho --help && uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `requires-python = ">=3.11"`.
    - `pyproject.toml` contains `cpho = "cpho_cli.cli.app:app"`.
    - `uv run cpho --help` exits 0 and prints `solve`.
    - `uv run cpho --help` exits 0 and prints `eval`.
    - `.gitignore` contains `.env` and `*.trace.jsonl`.
  </acceptance_criteria>
  <done>The project installs with uv and the CLI entrypoint is testable without requiring an OpenRouter API key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement config and API key resolution</name>
  <files>src/cpho_cli/core/config.py, src/cpho_cli/models/config.py, tests/test_config.py</files>
  <read_first>AGENTS.md, src/cpho_cli/cli/app.py, .planning/phases/01-core-foundation/01-CONTEXT.md, .planning/phases/01-core-foundation/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: environment variable `OPENROUTER_API_KEY` is returned when no config file is supplied.
    - Test 2: CLI overrides beat per-skill settings, and per-skill settings beat global defaults.
    - Test 3: config loading uses `yaml.safe_load` and rejects unknown fields.
  </behavior>
  <action>Implement D-10/D-13 config contracts. Create Pydantic models `ModelParams`, `ProviderConfig`, `SkillConfig`, and `AppConfig` with global model defaults, per-skill overrides, and provider settings. Implement `load_config(path: Path | None) -> AppConfig`, `resolve_api_key(config: AppConfig, env: Mapping[str, str]) -> str`, and `resolve_model_params(config: AppConfig, skill_name: str, cli_overrides: ModelParams | None) -> ModelParams`. Use `yaml.safe_load`, never `yaml.load`. Raise a project-owned `ConfigError` when the API key is missing, config YAML is invalid, or unknown fields are present. Do not print or log the key.</action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q && uv run ruff check src tests && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - `src/cpho_cli/core/config.py` contains `yaml.safe_load`.
    - `src/cpho_cli/core/config.py` does not contain `yaml.load(`.
    - `tests/test_config.py` asserts `OPENROUTER_API_KEY` is accepted.
    - `tests/test_config.py` asserts missing API key raises `ConfigError` without including the secret value.
    - `tests/test_config.py` asserts CLI override precedence over per-skill and global values.
  </acceptance_criteria>
  <done>Config loading is validated, deterministic, secret-safe, and ready for provider and skill runtime plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv sync` succeeds.
- [ ] `uv run cpho --help` succeeds and lists `solve` and `eval`.
- [ ] `uv run ruff check .` succeeds.
- [ ] `uv run mypy .` succeeds.
- [ ] `uv run pytest` succeeds.
</verification>

<success_criteria>
- D-01 through D-04 are implemented as project scaffolding and commands.
- CORE-01 has a working, non-interactive API key/config foundation.
- API keys are never hardcoded, committed, printed, or traced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-01-SUMMARY.md`.
</output>

