---
phase: 01-core-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/cpho_cli/core/workspace.py
  - src/cpho_cli/core/documents.py
  - src/cpho_cli/core/ocr.py
  - src/cpho_cli/models/documents.py
  - src/cpho_cli/models/ocr.py
  - tests/test_workspace.py
  - tests/test_documents.py
  - tests/test_ocr.py
autonomous: true
requirements:
  - CORE-02
  - CORE-03
user_setup: []
must_haves:
  truths:
    - "CORE-02: a local folder scan discovers PDF and image problem files and pairs them with answer keys using deterministic naming heuristics."
    - "CORE-03/D-03: OCR extraction works through a project-owned abstraction on Python >=3.11."
    - "CORE-03: OCR output preserves text blocks, page references, bounding boxes where available, confidence values, and low-confidence flags."
  artifacts:
    - src/cpho_cli/core/workspace.py
    - src/cpho_cli/core/documents.py
    - src/cpho_cli/core/ocr.py
    - src/cpho_cli/models/documents.py
    - src/cpho_cli/models/ocr.py
  key_links:
    - "workspace discovery -> document loader via ProblemFile and AnswerKeyPair models"
    - "document loader -> OCR adapter through page image/text inputs"
    - "OCR adapter -> normalized OCRBlock records consumed by solve runtime"
---

<objective>
Implement workspace discovery, answer-key pairing, PDF/image loading, and the OCR abstraction.

Purpose: This plan satisfies CORE-02 and CORE-03 while preserving the OCR uncertainty data needed by the solve pipeline.
Output: Tested discovery and OCR input layer that later plans can call without depending on Typer.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-PATTERNS.md
@.planning/phases/01-core-foundation/01-01-SUMMARY.md
</context>

<threat_model>
ASVS level: 1
Block on severity: high

Threats:
- MEDIUM: Workspace scanning may read files outside the requested workspace through symlinks or path traversal.
- MEDIUM: OCR traces may retain sensitive problem text in unexpected output locations.
- LOW: Ambiguous answer-key pairing may silently pair a problem with the wrong answer file.

Required mitigations:
- Resolve paths and keep discovered files under the selected workspace root.
- Return explicit ambiguity diagnostics instead of guessing when multiple answer files match.
- Keep OCR output as local model objects; do not write trace files in this plan.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discover workspace files and answer-key pairs</name>
  <files>src/cpho_cli/core/workspace.py, src/cpho_cli/models/documents.py, tests/test_workspace.py</files>
  <read_first>src/cpho_cli/core/config.py, src/cpho_cli/models/config.py, .planning/phases/01-core-foundation/01-CONTEXT.md, .planning/phases/01-core-foundation/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: a workspace containing `2024-final-p1.pdf` and `2024-final-p1-answer.pdf` returns one matched pair.
    - Test 2: unmatched problem files are reported in diagnostics, not dropped.
    - Test 3: ambiguous answer matches are reported and not auto-selected.
  </behavior>
  <action>Implement D-02/CORE-02 file discovery in core, not CLI. Create Pydantic models `ProblemFile`, `AnswerKeyFile`, `ProblemAnswerPair`, and `WorkspaceDiscoveryResult`. Support extensions `.pdf`, `.png`, `.jpg`, `.jpeg`, `.webp`, `.tif`, and `.tiff`. Implement `discover_workspace(root: Path) -> WorkspaceDiscoveryResult` with deterministic sort order. Pair files by normalized stem heuristics: exact stem match after removing suffixes `answer`, `answers`, `solution`, `solutions`, `ans`, `key`, Chinese `答案`, and `解析`; also support sibling directories named `answers`, `answer`, `solutions`, or `答案`. If more than one answer candidate has the same score, record ambiguity and leave the pair unresolved.</action>
  <verify>
    <automated>uv run pytest tests/test_workspace.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `discover_workspace` returns deterministic ordering for repeated scans.
    - `tests/test_workspace.py` includes unmatched-file and ambiguous-answer cases.
    - No code in `src/cpho_cli/core/workspace.py` imports `typer`.
    - Symlink or resolved path tests prove discovered files remain under the workspace root.
  </acceptance_criteria>
  <done>Workspace discovery can find problems, pair answer keys, and surface pairing issues without user prompts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Load PDFs/images and normalize OCR blocks</name>
  <files>src/cpho_cli/core/documents.py, src/cpho_cli/core/ocr.py, src/cpho_cli/models/ocr.py, tests/test_documents.py, tests/test_ocr.py</files>
  <read_first>src/cpho_cli/models/documents.py, .planning/phases/01-core-foundation/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: document loader returns page records for a small generated PDF fixture.
    - Test 2: RapidOCR adapter converts raw OCR tuples/objects into `OCRBlock` records.
    - Test 3: confidence below configured threshold sets `low_confidence=True`.
  </behavior>
  <action>Implement D-06/CORE-03 document and OCR contracts. Create `DocumentPage`, `DocumentInput`, `OCRBlock`, `OCRPageResult`, and `OCRResult` models. Implement `load_document(path: Path) -> DocumentInput` using PyMuPDF for PDFs and image files, preserving source path, page number, embedded text when available, and rasterized page/image bytes or pixmap data required for OCR. Define `OCRProvider` protocol with `extract(document: DocumentInput) -> OCRResult`. Implement `RapidOCRProvider` that calls `RapidOCR()` and normalizes text, bounding box, confidence, page number, and low-confidence flags. Keep the provider swappable; do not leak RapidOCR-native output classes into callers.</action>
  <verify>
    <automated>uv run pytest tests/test_documents.py tests/test_ocr.py -q && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - `src/cpho_cli/core/ocr.py` contains `class OCRProvider`.
    - `src/cpho_cli/core/ocr.py` contains `class RapidOCRProvider`.
    - `OCRBlock` contains `text`, `page_number`, `confidence`, and `low_confidence`.
    - `tests/test_ocr.py` verifies low-confidence regions are preserved.
    - No OCR code calls OpenRouter or imports LLM provider code.
  </acceptance_criteria>
  <done>OCR is abstracted, RapidOCR is the default implementation, and uncertainty survives into project-owned data models.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_workspace.py tests/test_documents.py tests/test_ocr.py -q` succeeds.
- [ ] `uv run ruff check src tests` succeeds.
- [ ] `uv run mypy src` succeeds.
</verification>

<success_criteria>
- CORE-02 and CORE-03 are covered by tested core APIs.
- Wrong or missing answer-key pairing cannot be silently accepted.
- OCR confidence and page/region provenance are available to later solve steps.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-02-SUMMARY.md`.
</output>

