---
phase: 02.2-tui-repl-repl-tui-inserted
plan: 02
subsystem: cli
tags: [repl, session, persistence]
requires:
  - phase: 02.2-01
    provides: REPL package skeleton
provides:
  - Command dataclass and registry
  - SessionState and IndexMeta
  - XDG session persistence
affects: [phase-02.2, cli]
tech-stack:
  added: []
  patterns: [registry-driven commands, XDG persistence, frozen index metadata]
key-files:
  created:
    - src/cpho_cli/cli/repl/session.py
    - src/cpho_cli/cli/repl/persistence.py
    - tests/test_repl_commands.py
    - tests/test_repl_session.py
    - tests/test_repl_persistence.py
  modified:
    - src/cpho_cli/cli/repl/commands/__init__.py
key-decisions:
  - "Use `commands/__init__.py` as the registry surface because Python cannot import both `commands.py` and a `commands/` package."
requirements-completed: [TUI-01, TUI-02]
duration: 18 min
completed: 2026-05-24
---

# Phase 02.2 Plan 02: Core Abstractions Summary

**Registry-driven command abstraction, mutable REPL session state, and atomic XDG session snapshots**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-24T10:52:00Z
- **Completed:** 2026-05-24T11:10:00Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added mutable `Command` and `registry` plus deferred built-in command installation.
- Added mutable `SessionState`, frozen `IndexMeta`, and `load_index_meta`.
- Added `session.json`, `history.txt`, and `repl.log` paths with atomic JSON writes.

## Task Commits

1. **Command registry** - `61a0c75`
2. **Session state** - `8382b69`
3. **Persistence** - `528f8a1`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Reconciled impossible `commands.py` plus `commands/` package layout**
- **Found during:** Task 1
- **Issue:** Python resolves `cpho_cli.cli.repl.commands` to the `commands/` package, making a sibling `commands.py` unreachable and breaking `commands.search`.
- **Fix:** Put `Command`, `registry`, and `install_builtin_commands` in `commands/__init__.py`.
- **Files modified:** `src/cpho_cli/cli/repl/commands/__init__.py`
- **Verification:** `from cpho_cli.cli.repl.commands import Command` and `commands.search` imports both work.
- **Committed in:** `61a0c75`

---

**Total deviations:** 1 auto-fixed blocking planner layout issue.
**Impact on plan:** Import surface remains the same for callers; implementation file path differs from the impossible plan path.

## Issues Encountered

None beyond the documented layout correction.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Wave 3 can build prompt_toolkit runtime and built-in commands against stable registry/session APIs.

---
*Phase: 02.2-tui-repl-repl-tui-inserted*
*Completed: 2026-05-24*
