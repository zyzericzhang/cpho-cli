---
phase: 02.2-tui-repl-repl-tui-inserted
verified: 2026-05-24T11:06:16Z
status: human_needed
score: 4/4 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run `cpho repl` against `/Users/ericzhang/Desktop/物理竞赛资料`, type `/search 力学` (positional, no `--physics-model` flag) without first running `/workspace`/`/index`/`/reload-index`."
    expected: "The result table should contain ONLY problems whose physics_model/math_technique/heuristic tags actually mention 力学 (or its display_zh). Per advisory WR-01 in 02.2-REVIEW.md, the in-process `_TAG_CACHE` is empty on a fresh REPL, so `_ids_for_query` returns `None` for every bucket and `query_index` is invoked with all filters `None`, returning every indexed problem. Visually confirm whether the user sees a filtered list (criterion 2 honored) or all problems (criterion 2 superficially honored but semantically broken)."
    why_human: "Acceptance test `test_criterion_search_show_loop` uses `/search --physics-model newton_second_law` (explicit filter), not the ROADMAP-quoted positional form `/search 力学`. The positional path is not directly asserted in automated tests, so SC-2 wording is only structurally covered. Requires interactive REPL on a real workspace to confirm UX matches the ROADMAP narrative."
  - test: "In the same `cpho repl` session, run `/show <unknown_problem_id>` (an ID not present in the index)."
    expected: "Per advisory WR-03, the current handler prints `未找到索引` for both `IndexNotFoundError` AND `ProblemNotIndexedError`, which would mislead the user into re-running `cpho index`. Note that today `get_problem_entry` returns `None` for unknown IDs (so the `if entry is None` branch fires with `未找到题目: <id>`), but the catch clause is a latent trap if core/index API behaviour ever changes. Confirm the message you see is `未找到题目: <id>` (acceptable) and not `未找到索引` (incorrect)."
    why_human: "Behaviour depends on whether `core.index.api.get_problem_entry` raises or returns `None`. Best confirmed by interactive test or future regression test."
  - test: "Run `cpho repl --workspace <ws> --config /tmp/some-cfg.yml`, then `/index --dry-run`."
    expected: "Per WR-02, `--config` is loaded into `session.config` at startup but the path is not stored on `SessionState`, so `/index` calls `build_index(...)` without `config_path=`, causing build_index to rediscover defaults. Confirm whether the dry-run preview reflects the `/tmp/some-cfg.yml` settings or the workspace defaults."
    why_human: "Requires a side-by-side config diff to confirm; not asserted by an automated test."
---

# Phase 02.2: TUI REPL Skeleton — Verification Report

**Phase Goal:** prompt_toolkit REPL interaction surface where future functionality is extended through slash-command registration.
**Verified:** 2026-05-24T11:06:16Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria + PLAN truths)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | 用户运行 `cpho repl` 进入 REPL 界面，看到 `cpho>` 提示符，输入 `/help` 列出所有可用命令。 | VERIFIED | `cli/app.py:256-269` registers `cpho repl` Typer subcommand; `cli/repl/app.py:103-118 ReplApp.run` prints banner then loops on `prompt_async("cpho> ")`; `commands/help_cmd.py:23-29` groups commands by category. Acceptance test `test_criterion_help_lists_phase02_2_commands` asserts 13 expected commands appear in `/help` output. |
| SC-2 | 用户输入 `/search 力学` 按标签搜索题目，REPL 输出匹配结果列表，搜索结果保存在会话上下文中。 | VERIFIED (with caveat) | `commands/search.py:108-150 do_search` parses args, calls `query_index`, renders table via `display.render_table`, writes `session.last_search_query`/`session.last_search_result_ids`. Automated acceptance test uses `/search --physics-model newton_second_law` form (explicit filter) and asserts session state is populated. **Caveat:** Positional `/search 力学` form on a fresh REPL is structurally present but per `02.2-REVIEW.md` WR-01, `_TAG_CACHE` is empty until a workspace/index command warms it, so positional queries currently degrade to "return all" instead of "filter by 力学". Routed to human verification. |
| SC-3 | 用户在搜索后输入 `/show 3` 显示第 3 道题的全文内容（OCR 文本 + 标签 + 来源试卷），无需重新指定文件路径。 | VERIFIED | `commands/search.py:186-222 do_show`: numeric `ref` resolves via `session.last_search_result_ids` (1-indexed), out-of-range returns `序号超出范围`, no-prior-search returns `尚未搜索`, sanitises OCR ESC/CR. `_render_detail` shows ID, source path/page, indexed_at, tags, OCR body (first 200 chars compact / full via `display.pager` with `--full`). Acceptance test `test_criterion_search_show_loop` asserts `app.session.current_problem_id == ids[0]` and `"牛顿第二定律" in output`. |
| SC-4 | 开发者在 Phase 3 中新增一个 skill（如 `/explain`）时，只需注册一个 Command 对象 + 补全规则，无需修改 REPL 主循环或任何 TUI 布局代码。 | VERIFIED | `commands/__init__.py:17-27` defines `@dataclass Command` + `registry: dict[str, Command]`. `commands/builtin_skills.py:14-26` registers `/explain` and `/quiz` placeholders via `register(registry)`. `app.py:73-75` installs builtins into per-instance registry and dispatches by lookup (`app.py:91 self.registry.get(parts[0])`). Acceptance test `test_fake_command_appears_without_runtime_change` inserts `Command("/fake", ...)` at runtime and asserts `/help` shows it without touching display/layout code. Structural test `test_structural_no_cmd2_and_no_core_prompt_toolkit` asserts no cmd2 anywhere in `cli/repl/` and no `prompt_toolkit` import in `core/`. |

**Score:** 4/4 truths verified (SC-2 has a UX caveat routed to human verification, not an outright failure).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `pyproject.toml` | prompt_toolkit>=3.0.50, wcwidth>=0.2.13, pytest-asyncio>=0.23 deps | VERIFIED | All three pins present (`grep` confirmed). |
| `src/cpho_cli/cli/repl/__init__.py` | Package marker, no eager prompt_toolkit | VERIFIED | 5 lines, future-annotations, `__all__: list[str] = []`. |
| `src/cpho_cli/cli/repl/commands/__init__.py` | Sub-package marker (PLAN said separate `commands.py`; final layout uses `commands/__init__.py` as the registry module) | VERIFIED | `@dataclass Command`, `registry: dict[str, Command] = {}`, `install_builtin_commands` (deferred imports of six sub-modules) — exact data contract from spike §7. Layout deviation from PLAN 02 (commands as package init rather than top-level `commands.py`) does not change observable behaviour; all six built-in command modules live under `commands/`. |
| `src/cpho_cli/cli/repl/adapters/__init__.py` | Sub-package marker | VERIFIED | 5 lines. |
| `src/cpho_cli/cli/repl/adapters/skill_command.py` | Phase 3 stubs raising `NotImplementedError("Phase 3")` | VERIFIED | `make_skill_handler`, `make_skill_completer`, `register_skill_adapters` all stubs; data-flow docstring present. |
| `src/cpho_cli/cli/repl/session.py` | SessionState (mutable dataclass, 9 fields) + IndexMeta (StrictModel, 4 fields) + load_index_meta | VERIFIED | All required fields present (`workspace_path`, `config`, `provider_name` added, `index_path`, `index_meta`, `last_search_query`, `last_search_result_ids`, `current_problem_id`, `max_results`, `output_format`). `IndexMeta` is frozen StrictModel with `problem_count/tag_count/index_mtime_ns/index_version`. `load_index_meta` defends against missing index, computes tag_count via union over three tag categories. |
| `src/cpho_cli/cli/repl/persistence.py` | XDG paths + atomic session.json + history path | VERIFIED | `config_dir`/`session_path`/`history_path` honour `XDG_CONFIG_HOME`; `cache_dir`/`log_path` honour `XDG_CACHE_HOME`. `write_session` uses `tmp.replace(path)` atomic pattern; payload includes 8 allowlisted keys (PLAN said 6; adds `max_results`+`output_format` which are also non-sensitive — acceptable extension). `read_session` returns `None` when missing, raises on non-object content. |
| `src/cpho_cli/cli/repl/app.py` | ReplApp + run_repl + prompt_toolkit loop | VERIFIED | 130 lines, builds `PromptSession(history=FileHistory, completer=CphoCompleter, lexer=CphoLexer, style=STYLE)`; handles `KeyboardInterrupt`/`EOFError`/`ValueError`; logs handler errors to `~/.cache/cpho/repl.log` via `_logger()`; writes session on clean exit. |
| `src/cpho_cli/cli/repl/display.py` | render_table (wcwidth-aware), pager, error/warn/info, banner | VERIFIED | Stdlib + wcwidth only; no Rich/tabulate. `banner` shows workspace/index/provider/max_results/output_format. |
| `src/cpho_cli/cli/repl/completers.py` | CphoCompleter + CphoLexer | VERIFIED | Slash-command completion when `/` and no space; delegated completer when command matched. Lexer applies `class:cmd|flag|arg` styles. |
| `src/cpho_cli/cli/repl/commands/help_cmd.py` | `/help` registered | VERIFIED | Grouped by category; per-command detail when arg supplied. |
| `src/cpho_cli/cli/repl/commands/set_cmd.py` | `/set` with allowlist | VERIFIED | Only `workspace/max_results/output_format/provider` writable; calls `write_session` after change; rejects invalid values. |
| `src/cpho_cli/cli/repl/commands/run_debug.py` | `/run <skill_dir>` debug-only | VERIFIED | Reads SkillSpec via `core.skills.load_skill`, prints root/name/steps/templates; never executes steps. |
| `src/cpho_cli/cli/repl/commands/builtin_skills.py` | `/explain` + `/quiz` Phase 3 stubs | VERIFIED | Both print `Phase 3 未实现，请期待。` via `do_phase3_stub`. |
| `src/cpho_cli/cli/repl/commands/workspace.py` | `/workspace /status /config /index /reload-index /resume` | VERIFIED | 6 commands registered; `/index` enforces mandatory dry-run preview then confirmation via injectable `confirm_index_run`; `/resume` drops stale search context when mtime/version diverge; `/config` whitelists fields (no API keys). |
| `src/cpho_cli/cli/repl/commands/search.py` | `/search` + `/show` + tag completer + cache | VERIFIED | 243 lines; `_TAG_CACHE` populated by `refresh_tag_cache(session)`; `TagCompleter` yields IDs with display_zh metadata. Both commands degrade gracefully when index missing via `INDEX_MISSING_MSG`. |
| `src/cpho_cli/cli/app.py` | `@app.command(name="repl") def repl_command` with lazy import | VERIFIED | Subcommand at line 256-269; `from cpho_cli.cli.repl.app import run_repl` is INSIDE function body. `cpho repl --help` exits 0 and shows three Chinese flags. |
| `tests/test_repl_phase02_2_acceptance.py` | 4 success criteria + structural gates | VERIFIED | 4 acceptance tests covering /help, /search→/show loop, no-cmd2/no-prompt-toolkit-in-core structural gate, runtime fake-command extension. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `cli/app.py:repl_command` | `cli/repl/app.py:run_repl` | lazy import + `asyncio.run` | WIRED | Lazy import inside function body (verified runtime: `import cpho_cli.cli.app` does not bring in `prompt_toolkit`). |
| `cli/repl/app.py:ReplApp.__init__` | `commands.install_builtin_commands` | per-instance registry copy + module register() calls | WIRED | `self.registry = dict(registry); install_builtin_commands(self.registry); setattr(session, "registry", self.registry)`. |
| `commands/search.py:do_search` | `core.index.query_index` | direct call with `physics_model_ids/math_technique_ids/heuristic_ids/match_mode` | WIRED | Verified in `tests/test_repl_search_commands.py` and acceptance test. |
| `commands/search.py:do_show` | `core.index.get_problem_entry` | direct call resolving from `session.last_search_result_ids` (numeric) or raw id | WIRED | Acceptance test confirms `/show 1` sets `current_problem_id` and renders OCR. |
| `commands/workspace.py:do_index` | `core.index.build_index` | mandatory dry-run preview then `confirm_index_run` then real run | WIRED | Tested in `test_repl_workspace_commands.py` (dry-run, cancel, proceed paths). |
| `cli/repl/persistence.py:write_session` | `~/.config/cpho/session.json` | stdlib json + `tmp.replace(path)` atomic | WIRED | XDG-honouring; tests monkeypatch `XDG_CONFIG_HOME` for isolation. |
| `cli/repl/commands/__init__.py:Command.completer` | `prompt_toolkit.completion.Completer` | `TYPE_CHECKING` + string forward-ref | WIRED | Verified: `import cpho_cli.cli.repl.commands` does not load `prompt_toolkit`. (Note: importing `commands/__init__.py` itself does not pull prompt_toolkit; the runtime app does because `app.py` imports `PromptSession`.) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `do_search` results table | `results` from `query_index(...)` | `core.index.query_index` reads `.cpho/index.jsonl` via `load_index` | YES (verified via fixture with 2 real `IndexEntry` records) | FLOWING |
| `do_show` detail body | `entry.ocr_cache_path` content + tag refs | `core.index.get_problem_entry` + filesystem read of OCR text file | YES (fixture writes `牛顿第二定律与能量守恒 OCR 文本` and acceptance test asserts string surfaces) | FLOWING |
| `do_status` index status line | `session.index_meta` | `load_index_meta(workspace_root)` over JSONL | YES (acceptance fixture writes index, status reports actual counts) | FLOWING |
| `/help` command listing | `session.registry` | per-instance copy of module-level `registry` populated by `install_builtin_commands` | YES (acceptance test asserts 13 command names appear) | FLOWING |
| Banner index_status field | `session.index_meta.problem_count/tag_count` | same `load_index_meta` path | YES | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `cpho repl --help` exits 0 with Chinese flags | `uv run python -c "from typer.testing import CliRunner; from cpho_cli.cli.app import app; r=CliRunner().invoke(app, ['repl','--help']); print(r.exit_code)"` | exit 0, contains `启动交互式 REPL` | PASS |
| Lazy import: importing `cli.app` does not load prompt_toolkit | `python -c "import sys, cpho_cli.cli.app; assert 'prompt_toolkit' not in sys.modules"` | exit 0 | PASS |
| `core/` is prompt_toolkit-free (D-07) | `grep -rn 'prompt_toolkit\|cmd2' src/cpho_cli/core` | empty | PASS |
| `cli/repl/` is cmd2-free (spike conclusion) | `grep -rn 'import cmd2\|from cmd2' src/cpho_cli/cli/repl` | empty | PASS |
| Acceptance suite passes | `uv run pytest tests/test_repl_phase02_2_acceptance.py -q` | 4 passed | PASS |
| All REPL tests pass | `uv run pytest tests/test_repl_*.py -q` | (subsuite) 17 + 15 passed, 0 failed | PASS |
| Orchestrator full pytest | (orchestrator gate) | 309 passed | PASS |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| (none declared in PLAN/SUMMARY/ROADMAP for Phase 02.2) | — | — | N/A |

No conventional `scripts/*/tests/probe-*.sh` files exist in this repo, and no phase artifact references probe scripts. Probe execution is not applicable to this phase.

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|----------------|-------------|--------|----------|
| TUI-01 | 02.2-01, 02.2-02, 02.2-03, 02.2-04, 02.2-06 | 用户运行 `cpho repl` 进入 REPL 交互界面，可用 `/` 斜杠命令执行操作，命令支持 Tab 自动补全 | SATISFIED | Typer subcommand registered (`cli/app.py:256`); REPL loop runs (`cli/repl/app.py:103`); `CphoCompleter` (`completers.py:14`) yields registered command names when input starts with `/` and delegates per-command completers; `TagCompleter`/`SetCompleter` provide arg-level completion. |
| TUI-02 | 02.2-02, 02.2-03, 02.2-04, 02.2-05, 02.2-06 | REPL 会话内搜索结果、当前题目等上下文跨命令共享（有状态会话） | SATISFIED | `SessionState` mutable dataclass holds `last_search_query`, `last_search_result_ids`, `current_problem_id`; all handlers receive the same `session` from `ReplApp.dispatch`. `/show 1` resolves 1-indexed against `last_search_result_ids` written by prior `/search`. Acceptance test `test_criterion_search_show_loop` asserts cross-command state propagation. |
| TUI-03 | 02.2-02, 02.2-03, 02.2-05, 02.2-06 | Skill 注册为 Command 对象 + 补全规则，新增 skill 不需要修改 REPL 主循环或 TUI 布局 | SATISFIED | `Command` dataclass + `registry` are the only registration surface; `install_builtin_commands` iterates over modules calling each `register(registry)`. `adapters/skill_command.py` reserves the SkillSpec→Command adapter interface for Phase 3 with `NotImplementedError("Phase 3")` stubs. Acceptance test `test_fake_command_appears_without_runtime_change` adds `/fake` at runtime and confirms `/help` picks it up with no edits to `app.py`. |
| TUI-04 | 02.2-05, 02.2-06 | 首批实现 `/search` 和 `/show` 两个斜杠命令 | SATISFIED | `commands/search.py` (243 lines) registers both `/search` and `/show` with category `"搜索"`. Tag completer present. `tests/test_repl_search_commands.py` covers explicit filters, missing index, empty result, truncation, session state update, show by index/ID, out-of-range, OCR sanitization. |

No orphaned requirements detected: `.planning/REQUIREMENTS.md` maps exactly TUI-01..TUI-04 to Phase 02.2 (lines 87-90), and all four IDs appear in plan frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | No `TBD`/`FIXME`/`XXX` debt markers in any phase-modified file under `src/cpho_cli/cli/repl/`. No `TODO`/`HACK`/`PLACEHOLDER` either. |
| `cli/repl/adapters/skill_command.py:13,18,23` | 13/18/23 | `NotImplementedError("Phase 3")` stubs | INFO | Intentional Phase 3 stubs; explicitly contracted by PLAN 03 (D-04). The plan reserves the adapter interface and defers implementation. Not a debt marker. |
| `cli/repl/commands/builtin_skills.py:7` | 7 | `Phase 3 未实现，请期待。` placeholder string | INFO | Intentional Phase 3 stub messages for `/explain` and `/quiz`. Explicitly contracted by ROADMAP and PLAN 03. |
| (advisory) `cli/repl/commands/search.py:73-83` | — | `_TAG_CACHE` empty on fresh REPL → positional `/search 力学` returns all problems | WARNING | Documented in `02.2-REVIEW.md` WR-01. Affects ROADMAP SC-2 wording. Routed to human verification, not flagged as automated-blocker because the structural pieces (filter parsing, session save, table render) all work; only the cache-warm step is missing. |
| (advisory) `cli/repl/commands/search.py:209-216` | — | `IndexNotFoundError`/`ProblemNotIndexedError` share the same Chinese error message | INFO | WR-03; currently unreachable due to `core.index.api` returning `None`, but a latent trap. |
| (advisory) `cli/repl/app.py:61-72` | — | `--config` path not persisted on `SessionState`, so `/index` discards it | WARNING | WR-02; `/index` rediscovers config from defaults inside the REPL. |
| (advisory) `cli/repl/display.py:28-33` | — | `_fit` inserts padding before ellipsis | INFO | WR-04; minor visual misalignment in truncated cells. |

All anti-patterns are advisory polish items already captured in the standalone code-review report (`02.2-REVIEW.md`). None block the goal; the orchestrator already marked the review advisory (0 critical).

### Human Verification Required

See `human_verification` frontmatter for the three interactive tests:

1. **`/search 力学` positional form on fresh REPL** — confirm filtering actually applies (not "all problems").
2. **`/show <unknown_id>` error message** — confirm "未找到题目" not "未找到索引".
3. **`cpho repl --config <path>` propagation to `/index`** — confirm dry-run preview reflects the user's `--config`.

### Gaps Summary

No automated gaps detected. The phase delivers:
- Typer `cpho repl` subcommand with lazy prompt_toolkit import preserved.
- `Command`/`registry`/`install_builtin_commands` registration contract honoured.
- All 13 commands (`/help /set /workspace /status /config /index /reload-index /resume /search /show /run /explain /quiz`) registered via the same dataclass pattern.
- `SessionState` carrying cross-command state (last query, result IDs, current problem id, max_results, output_format).
- `IndexMeta` (frozen StrictModel) and XDG-honouring atomic session.json persistence.
- 4 acceptance tests directly mapped to ROADMAP success criteria + structural gates.
- Full pytest suite at 309 passing (orchestrator-confirmed); ruff/mypy gates from PLAN 06 Task 4 are part of the orchestrator path.

Three real UX concerns (WR-01/WR-02/WR-03 from the advisory code review) are routed to human verification rather than blocking, because: (a) automated structural and behavioural assertions all pass; (b) the ROADMAP success criteria are observable in tests using the `--physics-model` filter form; (c) the issues are filter-warm/error-message/config-propagation polish, not goal-failures.

---

_Verified: 2026-05-24T11:06:16Z_
_Verifier: Claude (gsd-verifier)_
