---
phase: 02.1-paper-splitting-pdf-phase-1-2
plan: "05"
type: execute
wave: 4
depends_on: ["02.1-04"]
files_modified:
  - tests/test_splitting_golden.py
  - tests/test_phase021_acceptance.py
autonomous: true
requirements: [PS-01, PS-02, PS-03, PS-04, PS-05, PS-06]
user_setup: []
must_haves:
  truths:
    - "Golden splitting regression uses deterministic OCR JSON from the expected fixture and still asserts PDFs exist."
    - "Final acceptance covers models, rules, fallback, single-image path, stale-index handling, builder integration, CLI stats, and full pytest."
    - "A sample index path demonstrates `problems_extracted > papers_split` for a multi-problem paper."
    - "Guarded real-workspace acceptance samples three papers when present and runs offline split/index with fake OCR and fake LLM/tagging."
  artifacts:
    - path: "tests/test_splitting_golden.py"
      provides: "Golden paper splitting regression"
      contains: "problem_ocr_pages"
    - path: "tests/test_phase021_acceptance.py"
      provides: "End-to-end Phase 02.1 acceptance smoke and guarded real-workspace offline acceptance"
      contains: "problems_extracted"
  key_links:
    - from: "tests/test_splitting_golden.py"
      to: "golden_tests/splitting/ipho_style_multi_problem.expected.json"
      via: "deterministic OCR fixture and manual annotation"
      pattern: "problem_ocr_pages"
    - from: "tests/test_phase021_acceptance.py"
      to: "src/cpho_cli/core/index/builder.py"
      via: "sample split-aware indexing"
      pattern: "problems_extracted"
---

<objective>
Add the golden and final acceptance tests for the complete Phase 02.1 plan set.

Purpose: PS-06 requires a stable golden gate, and the checker identified the previous golden plan depended on OCR data that the expected JSON did not contain.
Output: Golden regression using embedded OCR pages, phase acceptance smoke, and final verification commands.
</objective>

<execution_context>
@/Users/ericzhang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericzhang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md
@golden_tests/splitting/ipho_style_multi_problem.expected.json
@src/cpho_cli/core/splitting/__init__.py
@src/cpho_cli/core/index/builder.py
@src/cpho_cli/cli/app.py
@tests/conftest.py
</context>

<source_audit>
GOAL covered: verifies multi-problem PDFs become individual `ProblemEntry` and index rows.
REQ covered: PS-01, PS-02, PS-03, PS-04, PS-05, PS-06.
RESEARCH excluded: research was skipped by explicit user request.
CONTEXT excluded: no CONTEXT.md; no locked decisions or deferred ideas were provided.
Checker blockers covered: plan count merged to five; provider construction planned in 02.1-04; image/single path planned in 02.1-02 and 02.1-04; stale-index handling planned in 02.1-03 and consumed in 02.1-04; golden OCR JSON planned in 02.1-01 and used here.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic golden splitting regression</name>
  <files>tests/test_splitting_golden.py</files>
  <behavior>
    - Referenced golden problem and answer PDFs exist on disk.
    - The test constructs `OCRResult` from `problem_ocr_pages` and `answer_ocr_pages` in expected JSON, not live OCR.
    - Problem-number set exactly equals the manual annotation.
    - Page ranges match expected ranges within plus or minus one page.
    - Answer pairing is 100 percent for the golden fixture.
  </behavior>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` requirement PS-06.
    - `golden_tests/splitting/ipho_style_multi_problem.expected.json` expected OCR and manual annotations from Plan 01.
    - `src/cpho_cli/core/splitting/__init__.py` `split_paper()` contract.
    - `src/cpho_cli/models/ocr.py` `OCRResult` validation shape.
  </read_first>
  <action>Create `tests/test_splitting_golden.py` for PS-06. Load `golden_tests/splitting/ipho_style_multi_problem.expected.json`, assert the referenced PDFs exist, validate `problem_ocr_pages` and `answer_ocr_pages` into `OCRResult`, then call `split_paper()` with golden `PaperFile` metadata. Assert exact problem-number set, page range tolerance of plus or minus one page, and 100 percent answer pairing by problem number. Do not call RapidOCR or any live LLM in the golden passing path; if fallback would be needed, use the deterministic fake provider pattern from the splitting tests rather than network.</action>
  <verify>
    <automated>uv run pytest tests/test_splitting_golden.py -x</automated>
  </verify>
  <done>Golden regression is stable in CI because it uses committed deterministic OCR JSON while still validating fixture PDFs exist.</done>
  <acceptance_criteria>
    - Golden test validates both referenced PDFs exist before using embedded OCR JSON.
    - Problem numbers match the manual expected set exactly.
    - Answer pairing is complete for the golden fixture and no live OCR/LLM call occurs.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add phase acceptance smoke coverage</name>
  <files>tests/test_phase021_acceptance.py</files>
  <behavior>
    - Imports for `PaperFile`, `ProblemEntry`, `SplitMethod`, `split_paper`, `IndexEntry`, and `IndexRunStats` succeed.
    - A sample build/index path produces stats with `problems_extracted > papers_split`.
    - Acceptance covers image single path and stale-index discard using small temp fixtures.
    - CLI runner output contains `切分试卷数` and `提取题目数`.
    - When `/Users/ericzhang/Desktop/物理竞赛资料` exists, the test samples three real non-answer paper PDFs and runs the split/index path offline with deterministic fake OCR, fake LLM/tagging, and assertions on problem counts plus first problem text.
  </behavior>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` final acceptance criteria, especially the three-paper real-workspace sample.
    - `AGENTS.md` real-workspace reference requirement.
    - `src/cpho_cli/core/index/builder.py` `build_index()` test injection points for fake OCR and fake LLM/tagging.
    - `src/cpho_cli/core/splitting/__init__.py` rules/fallback/single behavior.
    - `tests/test_index_builder.py`, `tests/test_index_cli.py`, and `tests/test_splitting_integration.py` fake provider and monkeypatch patterns.
  </read_first>
  <action>Create `tests/test_phase021_acceptance.py` as an automated smoke for PS-01 through PS-05. Reuse fake OCR, fake LLM, and fake tagging patterns from existing index/splitting tests rather than duplicating long fixtures. Assert the public imports, split stats, per-problem `IndexEntry.problem_page_range`, image single-path behavior, stale old index discard, and CLI split-stat rendering. Add a guarded offline real-workspace acceptance test: if `/Users/ericzhang/Desktop/物理竞赛资料` is absent, `pytest.skip()` with a clear reason; if present, select the first three non-answer PDFs from the real workspace using the same answer-marker exclusions as discovery, copy or symlink them into a temporary workspace with matching fake answer PDFs when discovered, monkeypatch OCR to return deterministic `OCRResult` text containing the real paper stem plus `第1题` and `第2题` markers, monkeypatch LLM/tagging so no live OpenRouter call is possible, run the real `build_index()` path, then assert `papers_split == 3`, `problems_extracted >= 6`, every sampled paper produced at least two entries, and the first entry text recorded in the test artifact contains the real paper stem and a recognizable `第1题` prefix. Write a small JSON acceptance record under the test temp directory with sampled file names, per-paper problem counts, and first problem text snippets; assert the record is non-empty inside the test. This replaces the old dry-run-only smoke with a CI-safe offline acceptance path.</action>
  <verify>
    <automated>uv run pytest tests/test_phase021_acceptance.py -x</automated>
  </verify>
  <done>Acceptance smoke proves Phase 02.1 interfaces connect from models through splitting to index and CLI stats.</done>
  <acceptance_criteria>
    - Core acceptance smoke passes without live OCR or live LLM calls.
    - Guarded real-workspace test skips only when the real workspace path is absent; when present, it samples three actual PDFs and executes `build_index()` with fake OCR/LLM/tagging.
    - The guarded acceptance record includes sampled file names, problem counts, and first problem text snippets and is asserted in-test.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 02.1 verification</name>
  <files>tests/test_splitting_golden.py, tests/test_phase021_acceptance.py</files>
  <read_first>
    - `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-SPEC.md` full acceptance criteria.
    - `tests/test_phase021_acceptance.py` guarded offline real-workspace acceptance test from Task 2.
    - `tests/test_splitting_golden.py` deterministic golden regression from Task 1.
    - `AGENTS.md` real-workspace testing requirement.
  </read_first>
  <action>Run the focused Phase 02.1 test set and full pytest. The focused set must include model, rule, LLM fallback, integration, golden, acceptance, workspace, index model/hash/storage/builder/CLI/determinism/stats tests. Do not use `cpho index /Users/ericzhang/Desktop/物理竞赛资料 --dry-run` as the final acceptance because that does not exercise OCR, splitting, tagging, JSONL writes, problem counts, or first-text sanity. Instead, rely on `tests/test_phase021_acceptance.py` to perform the guarded offline real-workspace acceptance: when the real workspace path exists, it samples three actual paper PDFs, uses deterministic fake OCR plus fake LLM/tagging, executes the split/index builder path, writes JSONL in a temp workspace, and asserts/records per-paper problem counts and recognizable first problem text snippets; when the path is absent, it skips only that guarded real-workspace test while all committed-fixture tests still run in CI. Record in the summary whether the guarded real-workspace acceptance ran or skipped and include the sampled filenames/counts/snippets when it ran.</action>
  <verify>
    <automated>uv run pytest tests/test_splitting_models.py tests/test_splitting_rules.py tests/test_splitting_llm.py tests/test_splitting_fallback.py tests/test_splitting_integration.py tests/test_splitting_golden.py tests/test_phase021_acceptance.py tests/test_workspace.py tests/test_index_models.py tests/test_index_hashing.py tests/test_index_storage.py tests/test_index_builder.py tests/test_index_cli.py tests/test_index_determinism.py tests/test_index_stats.py -x</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>Focused Phase 02.1 tests and full test suite pass; summary records guarded offline real-workspace acceptance status and sampled paper evidence when available.</done>
  <acceptance_criteria>
    - Focused Phase 02.1 pytest command passes.
    - Full `uv run pytest -q` passes.
    - No final acceptance relies on a dry-run-only `cpho index` command.
    - Summary states whether the guarded offline real-workspace acceptance ran or skipped and includes evidence when it ran.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed golden fixtures to test assertions | Fixture JSON and PDFs define regression truth and can drift if edited incorrectly. |
| user-local workspace to smoke command | Real workspace contains user-controlled PDFs/images and must not be required for CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-05-01 | Tampering | golden expected JSON | mitigate | Golden test asserts referenced PDFs exist and validates embedded OCR JSON before comparing annotations. |
| T-02.1-05-02 | Denial of Service | local real workspace acceptance | mitigate | Guarded test samples only three papers, uses fake OCR/LLM/tagging, writes only to temp workspace, and skips when the path is absent. |
| T-02.1-05-03 | Information Disclosure | final tests | mitigate | Acceptance tests use fake providers and do not make live LLM calls. |
| T-02.1-05-SC | Tampering | package installs | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run the focused Phase 02.1 pytest command and `uv run pytest -q`; record guarded offline real-workspace acceptance status and sampled evidence in the summary.
</verification>

<success_criteria>
PS-01 through PS-06 are covered by automated tests; golden splitting is deterministic; final acceptance proves per-problem extraction, split stats, stale-index safety, and guarded offline real-workspace sample evidence; full pytest passes.
</success_criteria>

<output>
Create `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-05-SUMMARY.md` when done.
</output>
