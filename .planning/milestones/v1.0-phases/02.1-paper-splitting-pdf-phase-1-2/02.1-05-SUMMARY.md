---
phase: 02.1-paper-splitting-pdf-phase-1-2
plan: "05"
subsystem: testing
tags: [pytest, splitting, golden-tests, index, real-workspace]

requires:
  - phase: "02.1-01"
    provides: "Golden paper/answer PDFs with embedded OCR JSON"
  - phase: "02.1-04"
    provides: "Split-aware build_index orchestration and CLI split counters"
provides:
  - "Deterministic golden splitting regression using committed OCR JSON"
  - "Phase 02.1 acceptance smoke covering models, split/index integration, CLI stats, stale rows, and real workspace sampling"
  - "Final focused and full pytest verification evidence"
affects: ["phase-02.1", "indexing", "paper-splitting", "verification"]

tech-stack:
  added: []
  patterns:
    - "Golden splitting tests validate fixture PDFs but use deterministic committed OCR JSON"
    - "Real-workspace acceptance copies sampled PDFs into a temp workspace and uses fake OCR/LLM providers"

key-files:
  created:
    - "tests/test_splitting_golden.py"
    - "tests/test_phase021_acceptance.py"
    - ".planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-05-SUMMARY.md"
  modified: []

key-decisions:
  - "Final acceptance does not use dry-run cpho index; it runs build_index with deterministic fake OCR and fake LLM/tagging."
  - "Guarded real-workspace acceptance samples actual PDFs but copies them into pytest temp space so safe traversal still applies."

patterns-established:
  - "Use fake providers that raise on split fallback when a test must prove no live LLM path is reachable."
  - "Record real-workspace sample names, per-paper counts, and first-problem snippets inside the guarded test artifact."

requirements-completed: [PS-01, PS-02, PS-03, PS-04, PS-05, PS-06]

duration: 5min
completed: 2026-05-24
---

# Phase 02.1 Plan 05: Golden and Final Acceptance Summary

**Deterministic golden splitting plus full Phase 02.1 acceptance gates, including guarded offline sampling from the real physics workspace.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-24T08:21:43Z
- **Completed:** 2026-05-24T08:27:09Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- Added `tests/test_splitting_golden.py`, which validates fixture PDFs exist, builds `OCRResult` from committed `problem_ocr_pages` and `answer_ocr_pages`, checks exact problem numbers, applies plus/minus-one page tolerance, and asserts complete answer pairing without live OCR or LLM calls.
- Added `tests/test_phase021_acceptance.py`, covering public imports, split-aware builder stats, per-problem `IndexEntry.problem_page_range`, image single-path behavior, stale-index discard, and CLI split-stat rendering.
- Added guarded offline real-workspace acceptance for `/Users/ericzhang/Desktop/物理竞赛资料`; on this checkout it ran against three actual PDFs using fake OCR/LLM/tagging and wrote/asserted a temp JSON acceptance record.
- Ran the focused Phase 02.1 pytest command and the full test suite successfully.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add deterministic golden splitting regression** - `543168e` (test)
2. **Task 2: Add phase acceptance smoke coverage** - `158fb1c` (test)
3. **Task 3: Run final Phase 02.1 verification** - `558fc7b` (test, empty verification commit)

**Plan metadata:** committed in closeout commit.

## Files Created/Modified

- `tests/test_splitting_golden.py` - Deterministic PS-06 golden split regression using embedded OCR JSON and fixture PDF existence checks.
- `tests/test_phase021_acceptance.py` - End-to-end Phase 02.1 acceptance smoke and guarded offline real-workspace acceptance.
- `.planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-05-SUMMARY.md` - Plan closeout, verification evidence, deviations, and self-check.

## Decisions Made

- The final acceptance gate relies on the new guarded pytest path instead of a dry-run-only `cpho index` command, because dry-run would not exercise OCR, splitting, tagging, JSONL writes, problem counts, or first-text sanity.
- The real-workspace test copies sampled PDFs into a temp workspace rather than symlinking them, preserving the production safe traversal behavior that rejects paths resolving outside the workspace.

## Guarded Real-Workspace Acceptance

**Status:** Ran.

The guarded test sampled three actual non-answer PDFs from `/Users/ericzhang/Desktop/物理竞赛资料`, copied them into a pytest temp workspace, used deterministic fake OCR text with `第1题` and `第2题` markers, used fake LLM/tagging providers, ran `build_index()`, wrote JSONL under the temp `.cpho` directory, and asserted `papers_split == 3`, `problems_extracted >= 6`, and at least two entries per sampled paper.

| Sampled PDF | Problem count | First problem snippet |
|-------------|---------------|-----------------------|
| `第35届全国中学生物理竞赛决赛训练试题第01套v1(1)(1).pdf` | 2 | `第1题 第35届全国中学生物理竞赛决赛训练试题第01套v1(1)(1)` |
| `第35届全国中学生物理竞赛决赛训练试题第02套(1)(1).pdf` | 2 | `第1题 第35届全国中学生物理竞赛决赛训练试题第02套(1)(1)` |
| `第35届全国中学生物理竞赛决赛训练试题第04套v3(1)(1).pdf` | 2 | `第1题 第35届全国中学生物理竞赛决赛训练试题第04套v3(1)(1)` |

## Verification Results

- `uv run pytest tests/test_splitting_golden.py -x` - PASS, 1 test.
- `uv run pytest tests/test_phase021_acceptance.py -x` - PASS, 5 tests; guarded real-workspace test ran.
- `uv run pytest tests/test_splitting_models.py tests/test_splitting_rules.py tests/test_splitting_llm.py tests/test_splitting_fallback.py tests/test_splitting_integration.py tests/test_splitting_golden.py tests/test_phase021_acceptance.py tests/test_workspace.py tests/test_index_models.py tests/test_index_hashing.py tests/test_index_storage.py tests/test_index_builder.py tests/test_index_cli.py tests/test_index_determinism.py tests/test_index_stats.py -x` - PASS, 135 tests.
- `uv run pytest -q` - PASS, 277 tests.

All pytest runs emitted only existing PyMuPDF/SWIG deprecation warnings.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Copied real PDFs instead of symlinking them for guarded acceptance**
- **Found during:** Task 2 (Add phase acceptance smoke coverage)
- **Issue:** The initial guarded test symlinked sampled real PDFs into the temp workspace, but production safe traversal resolves paths and excludes files that point outside the workspace.
- **Fix:** Changed the test to copy the three sampled PDFs into the temp workspace before running `build_index()`.
- **Files modified:** `tests/test_phase021_acceptance.py`
- **Verification:** `uv run pytest tests/test_phase021_acceptance.py -x`
- **Committed in:** `158fb1c`

**2. [Rule 2 - Missing Critical] Tightened guarded real-workspace sample filtering**
- **Found during:** Task 2 (Add phase acceptance smoke coverage)
- **Issue:** The real workspace contains answer-like filenames using markers such as `sol` and `解答` that are not currently in production discovery markers; the acceptance requirement specifically asks for non-answer paper PDFs.
- **Fix:** Kept production discovery unchanged, but added test-only extra exclusions for obvious answer-like markers so the guarded evidence samples actual problem papers.
- **Files modified:** `tests/test_phase021_acceptance.py`
- **Verification:** `uv run pytest tests/test_phase021_acceptance.py -x` and sampled filename evidence above.
- **Committed in:** `158fb1c`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 missing critical)
**Impact on plan:** Both changes preserve the planned acceptance scope and avoid production behavior changes in this test-only plan.

## Issues Encountered

- The TDD-marked tasks added tests for behavior implemented in prior plans, so the new tests passed immediately after being written. No production GREEN implementation was needed.
- The Task 3 verification step had no file changes; it was recorded with an empty verification commit to preserve one atomic commit per task.

## Known Stubs

None. Stub scan only matched an intentional fake-provider `raw={}` test response payload.

## Threat Flags

None. This plan added tests only and introduced no new network endpoints, auth paths, file access patterns in production code, or schema changes at trust boundaries.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 02.1 is covered end-to-end: models, deterministic rules, LLM fallback boundaries, single-image path, stale-index safety, split-aware builder integration, CLI stats, golden regression, guarded real-workspace acceptance, focused tests, and full pytest all pass.

## Self-Check: PASSED

- Created files exist on disk.
- Task commits `543168e`, `158fb1c`, and `558fc7b` exist in git history.
- Focused Phase 02.1 verification and full `uv run pytest -q` passed.
- Guarded real-workspace acceptance ran and recorded sampled file/count/snippet evidence in-test.

---
*Phase: 02.1-paper-splitting-pdf-phase-1-2*
*Completed: 2026-05-24*
