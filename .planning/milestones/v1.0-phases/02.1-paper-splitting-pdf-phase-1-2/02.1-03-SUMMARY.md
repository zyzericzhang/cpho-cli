---
phase: 02.1-paper-splitting-pdf-phase-1-2
plan: "03"
subsystem: indexing
tags: [pydantic, jsonl, hashing, workspace, pdf]

requires:
  - phase: "02.1-01"
    provides: "PaperFile, PaperAnswerPair, AmbiguousPaperMatch, ProblemEntry page-range contracts"
  - phase: "02.1-02"
    provides: "Versioned split prompt manifest and single-image split behavior"
provides:
  - "Paper-level workspace discovery with Chinese answer pairing and image preservation"
  - "Split-aware IndexEntry and SemanticFingerprint persistence contracts"
  - "Rebuild-safe stale index loading for pre-02.1 JSONL rows"
affects: ["phase-02.1", "indexing", "workspace-discovery", "ocr-upgrade"]

tech-stack:
  added: []
  patterns:
    - "Strict query/API loads stay strict while rebuild paths use an explicit stale-index result"
    - "Split prompt version participates in semantic fingerprint equality"

key-files:
  created:
    - ".planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-03-SUMMARY.md"
  modified:
    - "src/cpho_cli/core/workspace.py"
    - "src/cpho_cli/models/documents.py"
    - "src/cpho_cli/models/index.py"
    - "src/cpho_cli/core/index/hashing.py"
    - "src/cpho_cli/core/index/storage.py"
    - "src/cpho_cli/core/index/ocr_cache.py"
    - "src/cpho_cli/core/index/builder.py"
    - "tests/test_workspace.py"
    - "tests/test_index_models.py"
    - "tests/test_index_hashing.py"
    - "tests/test_index_storage.py"
    - "tests/test_index_api.py"
    - "tests/test_topic_api.py"
    - "tests/test_compose.py"
    - "tests/test_topic_models.py"
    - "tests/test_index_ocr_upgrade.py"

key-decisions:
  - "Workspace discovery now emits paper-level models while retaining compatibility properties for existing callers."
  - "TAG_SCHEMA_VERSION is v2 because persisted index rows now require per-problem ranges and split prompt versions."
  - "Pre-02.1 index rows are not migrated; rebuild-only loading discards them with a stale reason."

patterns-established:
  - "Rebuild-safe persisted reads return entries plus stale_reason instead of weakening strict load_index()."
  - "Paper discovery treats unreadable PDFs as one-page inputs rather than failing workspace discovery."

requirements-completed: [PS-01, PS-05]

duration: 8min
completed: 2026-05-24
---

# Phase 02.1 Plan 03: Discovery and Index Persistence Summary

**Paper-level discovery plus split-aware index fingerprints and stale pre-02.1 JSONL discard semantics.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-24T02:49:54Z
- **Completed:** 2026-05-24T02:57:31Z
- **Tasks:** 3
- **Files modified:** 16

## Accomplishments

- Upgraded workspace discovery to return `PaperAnswerPair`, `PaperFile`, and `AmbiguousPaperMatch`, including real-workspace Chinese marker shapes like `力学2解析.pdf` and `理论5-答案.pdf`.
- Added required `IndexEntry.problem_page_range` and `SemanticFingerprint.split_prompt_version`, with page-range validation and split-version cache invalidation.
- Added rebuild-safe index loading so stale pre-02.1 rows are discarded before rebuild while strict `load_index()` still raises for query/API callers.

## Task Commits

Each task was committed atomically:

1. **Task 1: Upgrade workspace discovery to paper-level results** - `7697883` (feat)
2. **Task 2: Add split-aware index schema and fingerprint fields** - `e272f98` (feat)
3. **Task 3: Add stale-index loading before rebuild and OCR-upgrade prompts** - `6441146` (feat)

**Plan metadata:** committed in closeout commit.

## Files Created/Modified

- `src/cpho_cli/core/workspace.py` - Emits paper-level discovery results, keeps safe traversal, handles image page counts, and preserves Chinese answer matching.
- `src/cpho_cli/models/documents.py` - Adds compatibility properties so paper-level pairs still satisfy existing `.problem` access patterns.
- `src/cpho_cli/models/index.py` - Requires `problem_page_range` and `split_prompt_version`; adds split stats counters.
- `src/cpho_cli/core/index/hashing.py` - Bumps schema version to `v2` and threads split prompt version into semantic fingerprints.
- `src/cpho_cli/core/index/storage.py` - Adds `load_existing_index_for_rebuild()` with stale-schema detection.
- `src/cpho_cli/core/index/ocr_cache.py` - Treats stale pre-02.1 rows as no-prompt/no-delta data for OCR-upgrade checks.
- `src/cpho_cli/core/index/builder.py` - Passes split prompt version and uses rebuild-safe existing-index loading.
- `tests/test_*.py` files listed in frontmatter - Updated factories and regression coverage for discovery, schema, hashing, and stale rows.

## Decisions Made

- Kept `load_index()` strict; only rebuild code gets a forgiving loader.
- Used `v2` for `TAG_SCHEMA_VERSION` because old rows missing required fields must be treated as stale.
- Used `load_split_prompt_version()` as the source for index semantic fingerprints.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated builder for the new fingerprint/index schema**
- **Found during:** Task 2 (Add split-aware index schema and fingerprint fields)
- **Issue:** Changing `compose_semantic_fingerprint()` and `IndexEntry` made the production builder call site incomplete.
- **Fix:** Passed `split_prompt_version` from the split prompt manifest and added a temporary whole-file `(1, 1)` page range until Plan 04 wires real split entries.
- **Files modified:** `src/cpho_cli/core/index/builder.py`
- **Verification:** Task 2 suite, plan-level suite, and full `uv run pytest -x`.
- **Committed in:** `e272f98`

**2. [Rule 2 - Missing Critical] Routed builder rebuild loading through the stale-safe helper**
- **Found during:** Task 3 (Add stale-index loading before rebuild and OCR-upgrade prompts)
- **Issue:** OCR-upgrade detection no longer crashed on stale rows, but builder existing-entry loading would still crash before rebuild.
- **Fix:** Used `load_existing_index_for_rebuild()` for builder existing-entry reads so stale rows are discarded consistently.
- **Files modified:** `src/cpho_cli/core/index/builder.py`
- **Verification:** `uv run pytest tests/test_index_storage.py tests/test_index_ocr_upgrade.py -x` and full `uv run pytest -x`.
- **Committed in:** `6441146`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 missing critical)
**Impact on plan:** Both fixes were required to keep production rebuild paths coherent with the new schema. No additional feature scope was added.

## Issues Encountered

- OCR-upgrade fixtures initially used old `tag_schema_version="v1"` after `TAG_SCHEMA_VERSION` was bumped to `v2`; those current-schema fixtures were updated to `v2` so stale detection still has a meaningful boundary.

## Verification Results

- `uv run pytest tests/test_workspace.py -x` - PASS, 5 tests.
- `uv run pytest tests/test_index_models.py tests/test_index_hashing.py tests/test_index_api.py tests/test_topic_api.py tests/test_compose.py tests/test_topic_models.py tests/test_index_ocr_upgrade.py tests/test_index_stats.py -x` - PASS, 84 tests.
- `uv run pytest tests/test_index_storage.py tests/test_index_ocr_upgrade.py -x` - PASS, 16 tests.
- `uv run pytest tests/test_workspace.py tests/test_index_models.py tests/test_index_hashing.py tests/test_index_storage.py tests/test_index_api.py tests/test_topic_api.py tests/test_compose.py tests/test_topic_models.py tests/test_index_ocr_upgrade.py tests/test_index_stats.py -x` - PASS, 97 tests.
- `rg -n "IndexEntry\\(|SemanticFingerprint\\(" tests src` - PASS; all direct constructor call sites inspected.
- `uv run pytest -x` - PASS, 266 tests.

## Known Stubs

None. Stub scan only matched intentional optional defaults, empty test assertions, and typed accumulator lists.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 04 can wire actual paper splitting into `build_index()` using the paper-level discovery output and can rely on stale pre-02.1 indexes being discarded instead of loaded into result entries.

## Self-Check: PASSED

- Key modified files and this summary exist on disk.
- Task commits `7697883`, `e272f98`, and `6441146` exist in git history.
- Plan-level verification, grep gate, and full test suite passed.

---
*Phase: 02.1-paper-splitting-pdf-phase-1-2*
*Completed: 2026-05-24*
