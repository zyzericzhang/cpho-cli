---
phase: 02.1-paper-splitting-pdf-phase-1-2
plan: "04"
subsystem: indexing
tags: [pdf, splitting, indexing, cli, openrouter, pytest]

requires:
  - phase: "02.1-02"
    provides: "split_paper orchestration, SplitMethod.SINGLE, and provider-backed fallback"
  - phase: "02.1-03"
    provides: "paper-level discovery, split-aware IndexEntry schema, and stale-index rebuild loading"
provides:
  - "Split-aware build_index orchestration that writes one IndexEntry per ProblemEntry"
  - "Configured OpenRouterProvider construction for split fallback when no provider is injected"
  - "Chinese cpho index split-layer statistics"
affects: ["phase-02.1", "indexing", "cli", "paper-splitting"]

tech-stack:
  added: []
  patterns:
    - "OCR paper and answer files once, then tag each extracted ProblemEntry"
    - "Builder owns a single shared LLM provider for split, tag, and topic paths"
    - "Stale pre-02.1 index rows are discarded before result entry initialization"

key-files:
  created:
    - ".planning/phases/02.1-paper-splitting-pdf-phase-1-2/02.1-04-SUMMARY.md"
  modified:
    - "src/cpho_cli/core/index/builder.py"
    - "src/cpho_cli/core/index/notebook.py"
    - "src/cpho_cli/cli/app.py"
    - "tests/test_index_builder.py"
    - "tests/test_index_determinism.py"
    - "tests/test_index_cli.py"

key-decisions:
  - "build_index uses ProblemEntry.problem_id and source paper paths for persisted index rows."
  - "The builder constructs one OpenRouterProvider from resolved config and reuses it for split, tag, and topic LLM calls."
  - "Split-layer CLI counters are rendered as a separate Chinese section between scan and OCR stats."

patterns-established:
  - "Index rebuild paths initialize result_entries only from load_existing_index_for_rebuild().entries."
  - "Split problem IDs with colon separators are valid notebook lookup IDs."

requirements-completed: [PS-03, PS-04, PS-05]

duration: 9min
completed: 2026-05-24
---

# Phase 02.1 Plan 04: Split-Aware Index Builder Summary

**Normal `cpho index` now indexes extracted paper problems, constructs configured fallback providers, and reports split counters in Chinese.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-05-24T08:07:48Z
- **Completed:** 2026-05-24T08:16:45Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Reworked `build_index()` to discover papers, OCR each paper/answer once, call `split_paper()`, and persist one `IndexEntry` per `ProblemEntry`.
- Added stale pre-02.1 regression coverage so old whole-paper JSONL rows are discarded before rebuild output is initialized.
- Added builder-level OpenRouter provider construction from resolved config and shared it across split fallback, tag refinement, and topic assignment.
- Added `cpho index` split stats: `切分试卷数`, `提取题目数`, `规则切分`, `LLM 切分`, and `单题路径`.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Add split-aware builder regression tests** - `401c257` (test)
2. **Task 1 GREEN: Index split problem entries** - `269ac1a` (feat)
3. **Task 2 RED: Add provider construction fallback regression** - `9f08b9b` (test)
4. **Task 2 GREEN: Construct shared provider for index fallback** - `4ae64c4` (feat)
5. **Task 3 RED: Add CLI split stat rendering assertions** - `e556784` (test)
6. **Task 3 GREEN: Render split stats in index CLI** - `7909106` (feat)

**Plan metadata:** committed in closeout commit.

## Files Created/Modified

- `src/cpho_cli/core/index/builder.py` - Splits paper OCR into `ProblemEntry` rows, discards stale rebuild rows, and shares the configured LLM provider.
- `src/cpho_cli/core/index/notebook.py` - Allows split problem IDs containing `:` for notebook lookup paths.
- `src/cpho_cli/cli/app.py` - Renders split-layer stats in normal `cpho index` output.
- `tests/test_index_builder.py` - Covers multi-problem PDFs, single-image split paths, stale JSONL rebuilds, prompt-strategy stale rows, and provider construction without network I/O.
- `tests/test_index_determinism.py` - Updates golden determinism checks to use split-generated problem IDs.
- `tests/test_index_cli.py` - Covers normal and quiet-mode split-stat output.

## Decisions Made

- Used the source paper and answer file fingerprints for every extracted problem from that paper; `split_prompt_version` remains part of the semantic fingerprint.
- Kept `total_problems` aligned with persisted extracted problem rows after splitting.
- Constructed the shared provider at builder level because split fallback can happen before tag refinement decides whether it needs an LLM.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Allowed split problem IDs in notebook lookup**
- **Found during:** Task 1 (Build index entries from split ProblemEntry records)
- **Issue:** `ProblemEntry.problem_id` values intentionally use `paper_sha256:NN`, but the notebook path validator rejected `:` before indexing could proceed.
- **Fix:** Updated the notebook ID regex to allow colon separators.
- **Files modified:** `src/cpho_cli/core/index/notebook.py`
- **Verification:** `uv run pytest tests/test_index_builder.py tests/test_index_determinism.py -x`
- **Committed in:** `269ac1a`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Required compatibility fix for the split ID contract. No feature scope was added.

## Issues Encountered

None.

## Verification Results

- `uv run pytest tests/test_index_builder.py tests/test_index_determinism.py -x` - PASS, 24 tests.
- `uv run pytest tests/test_index_builder.py::test_build_index_constructs_provider_for_split_fallback_without_network -x` - PASS, 1 test.
- `uv run pytest tests/test_index_cli.py -x` - PASS, 8 tests.
- `uv run pytest tests/test_index_builder.py tests/test_index_determinism.py tests/test_index_cli.py -x` - PASS, 33 tests.

## Known Stubs

None. Stub scan only matched test monkeypatches and planned output labels.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 05 can run end-to-end golden checks against the normal `cpho index` path. The builder now produces per-problem rows, exposes split counters, and can reach fallback splitting with configured provider credentials.

## Self-Check: PASSED

- Created summary file exists on disk.
- Task commits `401c257`, `269ac1a`, `9f08b9b`, `4ae64c4`, `e556784`, and `7909106` exist in git history.
- Plan-level verification command passed.
- Stub and threat-surface scan found no untracked stubs or security surfaces beyond the plan threat model.

---
*Phase: 02.1-paper-splitting-pdf-phase-1-2*
*Completed: 2026-05-24*
