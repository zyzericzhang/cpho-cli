---
phase: 03-skill-cross-cutting-core-skills
plan: 03-02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pyproject.toml
  - uv.lock
  - src/cpho_cli/core/skill_progress.py
  - src/cpho_cli/core/followup.py
  - src/cpho_cli/cli/repl/display.py
  - tests/test_skill_progress.py
  - tests/test_followup.py
  - tests/test_repl_display.py
  - docs/phase3-progress-followup-decisions.md
autonomous: false
requirements:
  - CROSS-PROGRESS
  - CROSS-FOLLOWUP
must_haves:
  truths:
    - "Skill runs can report current step, activity, and elapsed time without changing SkillRuntime DAG semantics."
    - "Non-TTY progress degrades to readable text."
    - "A completed skill can enter cpho:followup> and exit on /exit or two empty lines."
  artifacts:
    - path: src/cpho_cli/core/skill_progress.py
      provides: "Step-handler wrapper and Rich/non-TTY progress adapter."
    - path: src/cpho_cli/core/followup.py
      provides: "Provider.complete-backed follow-up loop helpers."
    - path: src/cpho_cli/cli/repl/display.py
      provides: "Shared confirm-list and display utilities for Solve and Explain."
    - path: docs/phase3-progress-followup-decisions.md
      provides: "Decision notes for Rich use and no LangChain/litellm follow-up."
  key_links:
    - from: src/cpho_cli/core/skill_progress.py
      to: src/cpho_cli/core/runtime.py
      via: "wraps StepHandler callables before SkillRuntime construction; does not edit runtime.py"
    - from: src/cpho_cli/core/followup.py
      to: src/cpho_cli/core/llm.py
      via: "uses LLMProvider.complete with local message history"
---

<objective>
Add the shared progress and follow-up primitives used by Solve, Explain, and Probe while preserving the SkillRuntime boundary.

Purpose: Implement D-02 and D-03 as caller-side adapters, not as a new runtime or an agent framework.
Output: Rich-backed progress, non-TTY fallback, confirm-list display helpers, provider-backed follow-up, and tests.
</objective>

<context>
@.planning/phases/03-skill-cross-cutting-core-skills/03-CONTEXT.md
@.planning/phases/03-skill-cross-cutting-core-skills/03-RESEARCH.md
@.planning/phases/03-skill-cross-cutting-core-skills/03-PATTERNS.md
@src/cpho_cli/core/runtime.py
@src/cpho_cli/core/llm.py
@src/cpho_cli/cli/repl/display.py
@src/cpho_cli/cli/repl/commands/workspace.py
@tests/test_repl_display.py
@tests/test_repl_runtime.py
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Rich package legitimacy before installation</name>
  <read_first>
    - .planning/phases/03-skill-cross-cutting-core-skills/03-RESEARCH.md
    - pyproject.toml
  </read_first>
  <files>pyproject.toml, uv.lock</files>
  <action>Before adding `rich`, verify the PyPI package `rich` manually because RESEARCH.md marks it `[ASSUMED]` after slopcheck was unavailable. Confirm that the registry page is `https://pypi.org/project/rich/`, the source project is Textualize/Rich, and the package matches D-03's Spinner/Live need. Do not install any package other than `rich>=13.0`.</action>
  <verify>
    <automated>python - <<'PY'\nimport urllib.request\nfor url in ['https://pypi.org/pypi/rich/json']:\n    with urllib.request.urlopen(url, timeout=10) as r:\n        assert r.status == 200\nprint('rich package metadata reachable')\nPY</automated>
    <human-check>Human confirms Rich legitimacy before the implementation task edits pyproject.toml.</human-check>
  </verify>
  <acceptance_criteria>`rich>=13.0` is approved for installation; no adjacent dependency additions are approved by this checkpoint.</acceptance_criteria>
  <done>`rich>=13.0` is approved for installation; no adjacent dependency additions are approved by this checkpoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement progress adapter without runtime fan-out changes</name>
  <read_first>
    - src/cpho_cli/core/runtime.py
    - src/cpho_cli/cli/repl/display.py
    - tests/test_repl_display.py
  </read_first>
  <files>pyproject.toml, uv.lock, src/cpho_cli/core/skill_progress.py, src/cpho_cli/cli/repl/display.py, tests/test_skill_progress.py, tests/test_repl_display.py, docs/phase3-progress-followup-decisions.md</files>
  <action>Add `rich>=13.0` using the project's uv workflow. Implement progress as a wrapper around existing `StepHandler` callables: each wrapped handler emits start/finish/error events with step id, human label, activity text, and elapsed seconds, then delegates to the original handler. Use `rich.live.Live` and `rich.spinner.Spinner` only inside the terminal adapter; when stdout is not a TTY, print simple lines containing the same step and elapsed fields. Do not modify `src/cpho_cli/core/runtime.py`, do not add fan_out/fan_in concepts, and do not make `SkillRuntime.run()` async. Record those boundary decisions in docs.</action>
  <verify>
    <automated>uv run pytest tests/test_skill_progress.py tests/test_repl_display.py -q</automated>
  </verify>
  <acceptance_criteria>Progress tests prove step names and elapsed text appear in non-TTY output, Rich imports are isolated to the adapter, and `core/runtime.py` remains unchanged by this plan.</acceptance_criteria>
  <done>Progress tests prove step names and elapsed text appear in non-TTY output, Rich imports are isolated to the adapter, and `core/runtime.py` remains unchanged by this plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement follow-up loop and shared confirm-list display</name>
  <read_first>
    - src/cpho_cli/core/llm.py
    - src/cpho_cli/cli/repl/commands/workspace.py
    - tests/test_repl_runtime.py
  </read_first>
  <files>src/cpho_cli/core/followup.py, src/cpho_cli/cli/repl/display.py, tests/test_followup.py, tests/test_repl_display.py, docs/phase3-progress-followup-decisions.md</files>
  <action>Create a small follow-up helper that accepts a prompt function, provider, model params, skill context markdown, and optional export path. It should prompt with `cpho:followup>`, append user/assistant turns to local message history, call `provider.complete`, exit on `/exit` or two consecutive empty inputs, and optionally append the transcript to the skill markdown file using the Plan 03-01 helper. Do not add LangChain, litellm, or agent orchestration. Add `confirm_list` in display.py for later Solve discrepancy and Explain tag confirmation: accept/reject/edit individual items and optional `+tag` append when enabled.</action>
  <verify>
    <automated>uv run pytest tests/test_followup.py tests/test_repl_display.py -q</automated>
  </verify>
  <acceptance_criteria>Follow-up uses only existing LLMProvider.complete semantics, exits exactly as D-02 requires, and confirm_list supports the interaction shapes required by D-05 and D-10.</acceptance_criteria>
  <done>Follow-up uses only existing LLMProvider.complete semantics, exits exactly as D-02 requires, and confirm_list supports the interaction shapes required by D-05 and D-10.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry -> local dependency | `rich` is a new PyPI dependency added to the local environment. |
| user prompt -> LLM provider | Follow-up sends user turns and skill context to the configured provider. |
| terminal capabilities -> renderer | TTY/non-TTY detection controls Rich Live vs plain text output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-SC | Tampering | `rich` install | mitigate | Blocking human package-legitimacy checkpoint before editing dependency files. |
| T-03-02-01 | Information Disclosure | `followup.py` | mitigate | Append follow-up transcript only when caller provides an export path/option; do not persist by default inside session state. |
| T-03-02-02 | Denial of Service | progress rendering | mitigate | Non-TTY fallback prints bounded status lines and avoids terminal control loops. |
</threat_model>

<verification>
Run `uv run pytest tests/test_skill_progress.py tests/test_followup.py tests/test_repl_display.py -q`.
</verification>

<success_criteria>
All later skill callers can opt into progress and follow-up through small helpers, with SkillRuntime remaining tone-unaware and fan-out-free.
</success_criteria>

<output>
Create `.planning/phases/03-skill-cross-cutting-core-skills/03-02-SUMMARY.md` when done.
</output>
