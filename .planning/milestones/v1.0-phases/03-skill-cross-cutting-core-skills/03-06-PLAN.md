---
phase: 03-skill-cross-cutting-core-skills
plan: 03-06
type: execute
wave: 5
depends_on:
  - 03-01
  - 03-02
  - 03-05
files_modified:
  - src/cpho_cli/models/probe.py
  - src/cpho_cli/core/probe.py
  - src/cpho_cli/builtin_skills/probe/SKILL.md
  - src/cpho_cli/builtin_skills/probe/skill.yml
  - src/cpho_cli/builtin_skills/probe/prompts/next_turn.md.j2
  - tests/test_probe.py
  - tests/test_skills.py
  - docs/phase3-probe-decisions.md
autonomous: true
requirements:
  - SKILL-PROBE
  - CROSS-EXPORT
  - CROSS-FOLLOWUP
must_haves:
  truths:
    - "Probe conducts a continuous Q+A loop for one problem until explicit user exit."
    - "Probe appends each completed turn immediately to markdown."
    - "Probe final markdown has all questions first and corresponding answers second."
  artifacts:
    - path: src/cpho_cli/core/probe.py
      provides: "Probe conversation loop helpers and markdown append/finalize functions."
    - path: src/cpho_cli/models/probe.py
      provides: "Probe turn and transcript models."
    - path: src/cpho_cli/builtin_skills/probe/skill.yml
      provides: "Per-turn LLM prompt contract."
    - path: docs/phase3-probe-decisions.md
      provides: "Decision notes distinguishing Probe from old Quiz/YAML ideas."
  key_links:
    - from: src/cpho_cli/core/probe.py
      to: src/cpho_cli/core/skill_outputs.py
      via: "incremental append and final atomic rewrite"
    - from: src/cpho_cli/core/probe.py
      to: src/cpho_cli/core/runtime.py
      via: "one normal runtime run per turn; loop remains in caller/service layer"
---

<objective>
Create the Probe core skill and transcript writer.

Purpose: Implement D-11, D-12, and the service part of D-13 without reviving the old Quiz/YAML direction.
Output: Probe models, per-turn skill folder, conversation helpers, incremental markdown writing, final markdown layout, and tests.
</objective>

<context>
@.planning/phases/03-skill-cross-cutting-core-skills/03-CONTEXT.md
@.planning/phases/03-skill-cross-cutting-core-skills/03-RESEARCH.md
@.planning/phases/03-skill-cross-cutting-core-skills/03-PATTERNS.md
@docs/new-understanding-2026-05-26.md
@src/cpho_cli/core/runtime.py
@src/cpho_cli/core/skill_outputs.py
@src/cpho_cli/builtin_skills/solve/skill.yml
@tests/test_repl_runtime.py
@tests/test_skills.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify Probe transcript and loop behavior</name>
  <read_first>
    - tests/test_repl_runtime.py
    - tests/test_skills.py
    - src/cpho_cli/core/skill_outputs.py
  </read_first>
  <files>tests/test_probe.py, tests/test_skills.py</files>
  <action>Write tests for D-11 and D-12. Cover `/exit`, two empty lines, soft limit at `probe_max_rounds` prompting whether to continue, immediate append after each Q+A turn, and final markdown rewrite with `## 问题` before `## 解答` and matching numbered pairs. Use fake prompt input and fake provider/runtime output; use temp files and copied or seeded real-workspace-shaped problem names rather than writing under `/Users/ericzhang/Desktop/物理竞赛资料`.</action>
  <verify>
    <automated>uv run pytest tests/test_probe.py tests/test_skills.py -q</automated>
  </verify>
  <acceptance_criteria>Tests fail before Probe exists and prove the old `/quiz` stub behavior cannot satisfy SKILL-PROBE.</acceptance_criteria>
  <done>Tests fail before Probe exists and prove the old `/quiz` stub behavior cannot satisfy SKILL-PROBE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Probe models, skill folder, and markdown writers</name>
  <read_first>
    - src/cpho_cli/core/runtime.py
    - src/cpho_cli/core/skill_outputs.py
    - src/cpho_cli/core/skill_handlers.py
  </read_first>
  <files>src/cpho_cli/models/probe.py, src/cpho_cli/core/probe.py, src/cpho_cli/builtin_skills/probe/SKILL.md, src/cpho_cli/builtin_skills/probe/skill.yml, src/cpho_cli/builtin_skills/probe/prompts/next_turn.md.j2, tests/test_probe.py, tests/test_skills.py, docs/phase3-probe-decisions.md</files>
  <action>Implement Probe as a per-turn skill: the loop owns user interaction, max-round prompting, and transcript state; each turn calls a normal SkillRuntime run with problem context, optional current solve report text, previous turns, and latest user response. Append each completed turn through the shared markdown helper immediately, then atomically rewrite the final file into the questions-first/answers-second layout at exit. Keep Probe focused on finding key points and processing steps; do not add scoring, Anki/Obsidian export, or old Quiz YAML behavior. Document that replacement explicitly.</action>
  <verify>
    <automated>uv run pytest tests/test_probe.py tests/test_skills.py -q</automated>
  </verify>
  <acceptance_criteria>Probe service tests pass with fake provider/runtime, prompt templates render under StrictUndefined, and each turn is recoverable from the incremental markdown file before final rewrite.</acceptance_criteria>
  <done>Probe service tests pass with fake provider/runtime, prompt templates render under StrictUndefined, and each turn is recoverable from the incremental markdown file before final rewrite.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user conversation -> provider | Probe sends user answers and problem context to the configured provider. |
| conversation transcript -> markdown file | Each turn is appended to local markdown output. |
| real workspace -> tests | Tests may sample real filenames/files only through temp copies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | Probe transcript | mitigate | Write only to the resolved export path; do not store transcripts in session.json. |
| T-03-06-02 | Denial of Service | Probe loop | mitigate | Enforce soft max-round prompt from `probe_max_rounds` and exit on `/exit` or two blank inputs. |
| T-03-06-03 | Tampering | final markdown rewrite | mitigate | Append each turn immediately, then use atomic rewrite for the final layout. |
</threat_model>

<verification>
Run `uv run pytest tests/test_probe.py tests/test_skills.py -q`.
</verification>

<success_criteria>
Probe exists as a continuous, export-backed Q+A skill with soft round limits and no dependency on the deprecated Quiz/YAML design.
</success_criteria>

<output>
Create `.planning/phases/03-skill-cross-cutting-core-skills/03-06-SUMMARY.md` when done.
</output>
