---
phase: 03-skill-cross-cutting-core-skills
plan: 03-04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cpho_cli/core/llm.py
  - tests/test_llm.py
  - docs/phase3-explain-decisions.md
autonomous: true
requirements:
  - SKILL-EXPLAIN-NEW
must_haves:
  truths:
    - "OpenRouter-compatible providers expose `stream(messages, params)` without changing existing `complete()` behavior."
    - "Fake SSE chunks are parsed in order, `[DONE]` terminates iteration, and malformed/failed streams use existing error-redaction patterns."
    - "Streaming yields prose chunks only; structured JSON validation remains a final complete-response concern."
  artifacts:
    - path: src/cpho_cli/core/llm.py
      provides: "OpenRouter-compatible stream() method and SSE delta parsing."
    - path: tests/test_llm.py
      provides: "Fake-SSE provider tests with no network credentials."
    - path: docs/phase3-explain-decisions.md
      provides: "Decision notes for streaming prose vs structured JSON parsing."
  key_links:
    - from: src/cpho_cli/core/llm.py
      to: src/cpho_cli/core/explain.py
      via: "Plan 03-05 consumes provider.stream for visible per-tone prose chunks"
    - from: tests/test_llm.py
      to: src/cpho_cli/core/llm.py
      via: "httpx.MockTransport SSE fixtures cover stream request and chunk parser behavior"
---

<objective>
Add OpenRouter-compatible LLM streaming support as a narrow provider-layer change.

Purpose: Split provider streaming from Explain orchestration so D-07 has a tested stream primitive before multi-tone rendering is built.
Output: `LLMProvider.stream(...)`, fake SSE chunk tests, and notes that streamed content is prose, not partial structured JSON.
</objective>

<context>
@.planning/phases/03-skill-cross-cutting-core-skills/03-CONTEXT.md
@.planning/phases/03-skill-cross-cutting-core-skills/03-RESEARCH.md
@.planning/phases/03-skill-cross-cutting-core-skills/03-PATTERNS.md
@src/cpho_cli/core/llm.py
@tests/test_llm.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify stream() with fake SSE chunks</name>
  <read_first>
    - src/cpho_cli/core/llm.py
    - tests/test_llm.py
  </read_first>
  <files>tests/test_llm.py</files>
  <action>Add tests for `LLMProvider.stream(messages, params)` using `httpx.MockTransport` with fake server-sent-event lines. Assert the request JSON includes `stream: true`, model params are preserved, chunks such as `老师型第一段`, `继续推导`, and `结尾` are yielded in order, `[DONE]` terminates iteration, transient HTTP/provider errors follow existing retry/redaction behavior, and DeepSeek either inherits the OpenAI-compatible stream behavior or raises a clear provider error if unsupported by config. Do not parse partial chunks as structured JSON.</action>
  <verify>
    <automated>uv run pytest tests/test_llm.py -q</automated>
  </verify>
  <acceptance_criteria>Tests fail before implementation, require fake chunk streaming, and do not depend on OpenRouter credentials or live network.</acceptance_criteria>
  <done>Tests fail before implementation, require fake chunk streaming, and do not depend on OpenRouter credentials or live network.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement provider streaming support</name>
  <read_first>
    - src/cpho_cli/core/llm.py
    - tests/test_llm.py
  </read_first>
  <files>src/cpho_cli/core/llm.py, tests/test_llm.py, docs/phase3-explain-decisions.md</files>
  <action>Add `stream(messages, params)` to the provider protocol and OpenAI-compatible provider path used by OpenRouter. Send the same model/options shape as `complete()` plus `stream: true`; parse `data: ...` SSE lines, yield `choices[].delta.content` text chunks in order, stop at `[DONE]`, and reuse existing error handling/redaction conventions. Preserve all existing `complete()` tests and behavior. In docs, record that D-07 streamed chunks are visible prose only; final structured models/tags are validated after complete content is available in Plan 03-05.</action>
  <verify>
    <automated>uv run pytest tests/test_llm.py -q</automated>
  </verify>
  <acceptance_criteria>`stream()` is available for Explain, fake SSE tests pass, and existing non-streaming provider behavior remains unchanged.</acceptance_criteria>
  <done>`stream()` is available for Explain, fake SSE tests pass, and existing non-streaming provider behavior remains unchanged.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider SSE stream -> local iterator | Remote streaming lines are parsed into local prose chunks. |
| provider errors -> terminal/tests | Provider failures may include sensitive request context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | stream parser | mitigate | Treat streamed chunks as prose only; structured validation happens after complete content is available. |
| T-03-04-02 | Information Disclosure | provider error handling | mitigate | Reuse existing redaction conventions for provider exceptions and traces. |
| T-03-04-03 | Denial of Service | SSE iteration | mitigate | Stop on `[DONE]` and propagate bounded provider errors instead of looping indefinitely. |
</threat_model>

<verification>
Run `uv run pytest tests/test_llm.py -q`.
</verification>

<success_criteria>
The LLM provider layer has tested streaming support that Plan 03-05 can consume for visible multi-tone prose rendering.
</success_criteria>

<output>
Create `.planning/phases/03-skill-cross-cutting-core-skills/03-04-SUMMARY.md` when done.
</output>
