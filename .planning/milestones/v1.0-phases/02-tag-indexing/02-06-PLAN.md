---
id: "02-06"
phase: 02
title: "Starter Vocabulary Content (builtin.yml) and Package Data"
wave: 1
depends_on: []
files_modified:
  - src/cpho_cli/vocabulary/__init__.py
  - src/cpho_cli/vocabulary/builtin.yml
  - pyproject.toml
  - tests/test_index_builtin_vocab.py
  - .planning/phases/02-tag-indexing/02-06-REVIEW-NOTE.md
requirements:
  - IDX-01
autonomous: false
must_haves:
  truths:
    - "src/cpho_cli/vocabulary/builtin.yml exists with 42 starter tags across physics_law, physics_model, math_technique, heuristic, approximation categories."
    - "Each entry has internal_id (snake_case), display_zh (Chinese), category (TagCategory enum value), aliases (list, possibly empty), description (one-line Chinese)."
    - "NOTE: Category system was redesigned 2026-05-23. Old physics_model split into physics_law + physics_model. system_selection merged into heuristic. See .planning/notes/topic-hierarchy-design.md."
    - "pyproject.toml [tool.setuptools.package-data] includes vocabulary/*.yml so the file ships in the installed wheel."
    - "A human-review checkpoint is invoked so the user (a physics-olympiad domain expert) can adjust display_zh/aliases for terminology fit before the file is locked in."
  artifacts:
    - path: "src/cpho_cli/vocabulary/builtin.yml"
      provides: "42 starter canonical tags"
      contains: "newton_second_law"
    - path: "pyproject.toml"
      provides: "Updated package-data so builtin.yml ships with the wheel"
      contains: 'vocabulary/*.yml'
  key_links:
    - from: "src/cpho_cli/core/index/vocabulary.py"
      to: "src/cpho_cli/vocabulary/builtin.yml"
      via: "_builtin_vocab_path() resolves to this file"
      pattern: "vocabulary/builtin.yml"
truths:
  - "builtin.yml is loadable by the loader Plan 02-01 built — no schema drift."
  - "Wheel installation includes the YAML asset (verified by importlib.resources or `python -c '...'`)."
  - "User has reviewed and accepted the starter list (R8 mitigation)."
---

<objective>
Author the 30-50 starter canonical tags promised by D-09 + IDX-01 and ship them as packaged YAML data. Verify the file loads against the schema from Plan 02-01 and is included in the installable wheel. Surface a brief human-review checkpoint so the domain-expert user can adjust Chinese display names and aliases before they ossify into a stable vocabulary.

Purpose: IDX-01 success criterion 1 requires canonical tags with Chinese display names + stable English IDs. Without this file, Plan 02-04 cannot load any vocabulary and Plan 02-05 cannot run end-to-end.

Output: `src/cpho_cli/vocabulary/builtin.yml` (42 entries), `src/cpho_cli/vocabulary/__init__.py` (empty package marker), `pyproject.toml` package-data update, a load-validation test, and a `02-06-REVIEW-NOTE.md` summarizing review questions for the user.
</objective>

<context>
Read these BEFORE writing any task:
- `.planning/phases/02-tag-indexing/02-CONTEXT.md` D-09 (three-layer vocab, builtin is the first layer), D-11 (Chinese display + snake_case ID + aliases).
- `.planning/phases/02-tag-indexing/02-RESEARCH.md` §6 (Starter Vocabulary — full 42-tag table with internal_id, display_zh, category, sample aliases); §13 R8 (review checkpoint mandatory because tags are [ASSUMED]).
- `.planning/phases/02-tag-indexing/02-PATTERNS.md` "builtin.yml" section (package-data layout, wheel inclusion).
- `pyproject.toml` (current `[tool.setuptools.package-data]` line: `cpho_cli = ["builtin_skills/**/*"]` — must be extended).
- `src/cpho_cli/models/index.py` from Plan 02-01 (CanonicalTag schema: internal_id, display_zh, category, aliases, description, status, visibility, layer).
- `src/cpho_cli/core/index/vocabulary.py` from Plan 02-01 (the loader this file must satisfy).

<interfaces>
Consumed:
- `cpho_cli.core.index.vocabulary._builtin_vocab_path()` (Plan 02-01) resolves to `Path(__file__).resolve().parents[2] / "vocabulary" / "builtin.yml"`. This plan must place the file there.
- `cpho_cli.models.index.CanonicalTag` (Plan 02-01) — every YAML entry validates against this Pydantic model.

Provided:
- The YAML file itself.
- Updated `[tool.setuptools.package-data]` in pyproject.toml.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package data → loader | builtin.yml is shipped inside the wheel and read on every `load_merged_vocabulary` call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | builtin.yml lives in the source tree; a malicious PR could change semantic mappings without detection | mitigate | Load-validation test in tests/test_index_builtin_vocab.py runs in CI on every change; snapshot of tag count + key internal_ids is asserted. |
| T-02-06b | DoS | Pathological YAML (deep nesting, huge aliases list) | accept | Authored by hand from a fixed 42-entry table; reviewer catches anomalies. |
</threat_model>

<tasks>

<task id="02-06-T1" type="auto">
  <name>Task 1: Create vocabulary package directory and starter YAML</name>
  <files>src/cpho_cli/vocabulary/__init__.py, src/cpho_cli/vocabulary/builtin.yml</files>
  <read_first>
    - .planning/phases/02-tag-indexing/02-RESEARCH.md §6 (the 42-entry table — copy internal_id, display_zh, category, aliases verbatim; add a one-line Chinese description for each entry per RESEARCH.md §6 last paragraph: "每个 entry 在 YAML 里需补 description 一行简介")
    - .planning/phases/02-tag-indexing/02-RESEARCH.md §2 (YAML format example with all required fields)
    - src/cpho_cli/models/index.py (CanonicalTag field list — internal_id required, display_zh required, category required, aliases optional list, description optional str, status default canonical, visibility default public, layer must be builtin)
    - src/cpho_cli/builtin_skills/solve/ (reference for how Phase 1 ships packaged YAML — directory shape)
  </read_first>
  <action>
Create empty `src/cpho_cli/vocabulary/__init__.py` (one-line module docstring: `"""Packaged controlled-vocabulary YAML files for Phase 2 index."""`). This makes the directory a Python package so setuptools-find finds it; it is also needed so `Path(__file__).resolve().parents[2]` from `core/index/vocabulary.py` lands on a legitimate package directory.

Create `src/cpho_cli/vocabulary/builtin.yml` with the structure:
```
version: "v0.1"
tags:
  - internal_id: newton_second_law
    display_zh: 牛顿第二定律
    category: physics_law
    aliases: ["F=ma", "Newton 第二", "动力学基本方程"]
    description: 力等于质量乘加速度；运动学与动力学的桥梁。
    status: canonical
    visibility: public
    layer: builtin
  - internal_id: momentum_conservation
    display_zh: 动量守恒
    ...
```

Populate with all 42 entries from RESEARCH.md §6 (categories updated per 2026-05-23 redesign):
- 10 physics_law entries (momentum_conservation, energy_conservation, angular_momentum_conservation, ideal_gas_law, first_law_thermo, second_law_thermo, electrostatics_gauss, circuit_kirchhoff, electromagnetic_induction, wave_interference).
- 4 physics_model entries (circular_motion, simple_harmonic_motion, rigid_body_rotation, geometric_optics).
- 12 math_technique entries (dimensional_analysis, small_angle_approximation, taylor_expansion, separation_of_variables, ode_first_order, ode_second_order, vector_decomposition, coordinate_transform, calculus_integral, binomial_approximation, symmetry_argument, limit_analysis).
- 14 heuristic entries (newton_second_law, system_selection, reference_frame_choice, conservation_law_selection, coordinate_system_choice, free_body_diagram, identify_constraint, symmetry_recognition, limiting_case_check, boundary_condition_setup, variable_substitution, equivalent_circuit, superposition_principle, analogy_mapping).
- 2 approximation entries (approximation_to_first_order, approximation_to_second_order).

For each entry: copy `internal_id`, `display_zh`, `category`, sample `aliases` verbatim from RESEARCH.md §6. Add a one-line `description` in Chinese (≤ 30 characters) that briefly states the concept's role. Set `status: canonical`, `visibility: public`, `layer: builtin` on every entry. Aliases may include both Chinese synonyms and English/symbolic forms (e.g., `["F=ma", "Newton 第二"]` mixes both, per D-11).

Verify the file count matches 42 by a final visual review before saving.
  </action>
  <verify>
    <automated>python -c "import yaml; data = yaml.safe_load(open('src/cpho_cli/vocabulary/builtin.yml', encoding='utf-8')); assert data['version'] == 'v0.1'; assert len(data['tags']) == 42, f'expected 42 tags, got {len(data[\"tags\"])}'; print('ok')"</automated>
    <automated>python -c "import yaml; data = yaml.safe_load(open('src/cpho_cli/vocabulary/builtin.yml', encoding='utf-8')); ids = [t['internal_id'] for t in data['tags']]; assert len(ids) == len(set(ids)), 'duplicate internal_ids'; assert all(id.replace('_','').isalnum() and id.islower() for id in ids), 'non-snake_case id'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/cpho_cli/vocabulary/builtin.yml` succeeds.
    - `test -f src/cpho_cli/vocabulary/__init__.py` succeeds.
    - First automated check above prints `ok` (exactly 42 tags, version v0.1).
    - Second automated check above prints `ok` (unique snake_case ids).
    - `grep -c "category: physics_law" src/cpho_cli/vocabulary/builtin.yml` returns 10.
    - `grep -c "category: physics_model" src/cpho_cli/vocabulary/builtin.yml` returns 4.
    - `grep -c "category: math_technique" src/cpho_cli/vocabulary/builtin.yml` returns 12.
    - `grep -c "category: heuristic\b" src/cpho_cli/vocabulary/builtin.yml` returns 14.
    - `grep -c "category: approximation" src/cpho_cli/vocabulary/builtin.yml` returns 2.
    - `grep -c "layer: builtin" src/cpho_cli/vocabulary/builtin.yml` returns 42.
    - `grep -c "status: canonical" src/cpho_cli/vocabulary/builtin.yml` returns 42.
    - `grep -c "display_zh:" src/cpho_cli/vocabulary/builtin.yml` returns 42.
    - `grep -c "description:" src/cpho_cli/vocabulary/builtin.yml` returns 42.
  </acceptance_criteria>
  <done>builtin.yml ships 42 canonical tags with full schema fields; vocabulary/ is a Python package.</done>
</task>

<task id="02-06-T2" type="auto">
  <name>Task 2: Extend pyproject.toml package-data and add load-validation test</name>
  <files>pyproject.toml, tests/test_index_builtin_vocab.py</files>
  <read_first>
    - pyproject.toml (current `[tool.setuptools.package-data]` line — only `builtin_skills/**/*` is listed)
    - .planning/phases/02-tag-indexing/02-PATTERNS.md "builtin.yml" section (wheel inclusion mechanism)
    - src/cpho_cli/core/index/vocabulary.py from Plan 02-01 (load_merged_vocabulary and _builtin_vocab_path — this is what we are validating against)
    - src/cpho_cli/vocabulary/builtin.yml from T1
  </read_first>
  <action>
Modify `pyproject.toml`: in the `[tool.setuptools.package-data]` section, extend the `cpho_cli` list to include `"vocabulary/*.yml"`. Final line shape: `cpho_cli = ["builtin_skills/**/*", "vocabulary/*.yml"]`. Do NOT touch any other key. Verify the change with a grep gate.

Forward-compatibility note (no implementation needed in this plan, but record in 02-06-SUMMARY.md): Plan 02-04 will add `core/index/prompts/*` to this same list when it ships Jinja2 templates and MANIFEST.yml.

Create `tests/test_index_builtin_vocab.py`:
(a) `test_builtin_vocabulary_loads()` — call `load_merged_vocabulary(tmp_path)` on a fresh tmp_path (no workspace/private overrides); assert returned Vocabulary has ≥ 30 and ≤ 50 tags (the D-09 "30-50 starter" range; this is the canonical regression guard against accidental tag deletion).
(b) `test_builtin_vocabulary_has_required_anchors()` — assert specific internal_ids that other tests will rely on are present: `newton_second_law`, `momentum_conservation`, `energy_conservation`, `dimensional_analysis`, `system_selection` all in `vocab.tags`.
(c) `test_builtin_vocabulary_chinese_display_names()` — assert `vocab.tags["newton_second_law"].display_zh == "牛顿第二定律"` (UTF-8 round-trip works through the loader).
(d) `test_builtin_vocabulary_alias_index_resolves_aliases()` — assert `vocab.alias_index[normalize_alias("F=ma")] == "newton_second_law"` (alias index built correctly from this file).
(e) `test_builtin_vocabulary_categories_have_expected_counts()` — assert counts: 10 physics_law, 4 physics_model, 12 math_technique, 14 heuristic, 2 approximation (use `Counter(tag.category for tag in vocab.tags.values())`).
(f) `test_builtin_vocabulary_all_layer_builtin()` — assert `all(tag.layer == TagLayer.BUILTIN for tag in vocab.tags.values())` (defense against bad YAML).
(g) `test_builtin_vocabulary_no_duplicate_internal_ids()` — load raw YAML, assert `len(ids) == len(set(ids))` (catches manual edit errors that Pydantic's dict-keyed schema would silently overwrite).
  </action>
  <verify>
    <automated>uv run pytest tests/test_index_builtin_vocab.py -x</automated>
    <automated>grep -c 'vocabulary/\*\.yml' pyproject.toml</automated>
    <automated>python -c "from pathlib import Path; from cpho_cli.core.index.vocabulary import load_merged_vocabulary; import tempfile; vocab = load_merged_vocabulary(Path(tempfile.mkdtemp())); print(f'{len(vocab.tags)} tags loaded')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vocabulary/\*\.yml' pyproject.toml` returns ≥ 1.
    - `grep -c 'builtin_skills/\*\*/\*' pyproject.toml` returns ≥ 1 (proves we did not remove the existing entry).
    - `pytest tests/test_index_builtin_vocab.py -x` exits 0 with 7 tests.
    - The third automated check prints "42 tags loaded".
    - `tests/test_index_builtin_vocab.py::test_builtin_vocabulary_alias_index_resolves_aliases` PASSES.
    - `tests/test_index_builtin_vocab.py::test_builtin_vocabulary_chinese_display_names` PASSES (UTF-8 end-to-end).
  </acceptance_criteria>
  <done>pyproject.toml ships builtin.yml in the wheel; load-validation suite proves the file conforms to Plan 02-01 schemas and that alias lookup, Chinese display, layer attribution, and per-category counts are all correct.</done>
</task>

<task id="02-06-T3" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of starter vocabulary content</name>
  <what-built>
    A 42-entry `src/cpho_cli/vocabulary/builtin.yml` with Chinese display names, snake_case internal IDs, aliases, and one-line descriptions covering: 15 physics models (Newton's second law, momentum/energy/angular momentum conservation, circular motion, SHM, rigid body, ideal gas, thermo 1st/2nd laws, Gauss, Kirchhoff, induction, geometric optics, wave interference), 12 math techniques (dimensional analysis, small-angle, Taylor, separation of variables, ODE 1st/2nd, vector decomposition, coordinate transform, integrals, binomial, symmetry, limit), 15 heuristics/approximations/system-selection entries.

    Plus a brief review note at `.planning/phases/02-tag-indexing/02-06-REVIEW-NOTE.md` listing the open questions for the user.
  </what-built>
  <how-to-verify>
    1. Open `src/cpho_cli/vocabulary/builtin.yml` and skim all 42 entries.
    2. For each entry confirm: is the `display_zh` term what you would actually use when talking to a Chinese physics olympiad student? (Common revision: `刚体定轴转动` vs `刚体转动` vs `定轴转动` — pick the canonical phrasing.)
    3. For each entry's `aliases` list, add at least one synonym you have personally heard students or coaches use that is NOT already in the list. Or remove an alias that feels redundant/wrong.
    4. For each `description`, confirm it accurately states the concept's role in 一行 (one line, ≤ 30 chars).
    5. Read `.planning/phases/02-tag-indexing/02-06-REVIEW-NOTE.md` and answer each numbered question (or write "skip" if the default is fine).
    6. If you make changes, re-run `uv run pytest tests/test_index_builtin_vocab.py -x` to confirm the schema still validates.

    This checkpoint exists because per RESEARCH.md §13 R8 the 42 starter tags are [ASSUMED] from generic physics-olympiad knowledge and need user verification before they ossify. After this Phase 2 ships, any rename of an `internal_id` is a breaking schema change.
  </how-to-verify>
  <resume-signal>Type "approved" (no changes needed), or "modified" (after you've edited builtin.yml and re-run the test), or paste a list of changes you want made and request a revision.</resume-signal>
</task>

<task id="02-06-T4" type="auto">
  <name>Task 4: Write the reviewer note artifact</name>
  <files>.planning/phases/02-tag-indexing/02-06-REVIEW-NOTE.md</files>
  <read_first>
    - src/cpho_cli/vocabulary/builtin.yml (from T1, after T2 validation, possibly after T3 modification)
    - .planning/phases/02-tag-indexing/02-RESEARCH.md §13 R8 (review checkpoint rationale)
  </read_first>
  <action>
Create `.planning/phases/02-tag-indexing/02-06-REVIEW-NOTE.md` (this is a planning artifact, not a code change; it is the agenda for the T3 human-verify checkpoint).

Contents (in Chinese, ≤ 60 lines):

```
# Phase 2 Starter Vocabulary — Review Note

## 背景

builtin.yml 内含 42 个 starter canonical tag，由 Phase 2 researcher 基于通用物理竞赛知识起草（[ASSUMED] 标记）。
这些标签的 `internal_id` 一旦发布，重命名属于 breaking schema change，需要全量重跑索引才能迁移。
此 review 在 ID 锁定前请用户（domain expert）调整中文展示名 / 别名 / 描述。

## 用户回答

请逐项回答（"skip" 表示接受默认）：

1. **物理模型类（15 条）**：是否有遗漏的常见竞赛模型？例如：相对论、广义动量、变质量系统、谐振耦合？
   - [ ] 添加新条目（请列出 internal_id + display_zh）
   - [ ] 接受现状

2. **数学技巧类（12 条）**：是否有遗漏？例如：留数定理、Fourier 级数、Lagrange 乘子、变分？
   - [ ] 添加新条目
   - [ ] 接受现状

3. **启发/策略类（15 条）**：display_zh 是否符合教练日常用语？
   - 示例疑问：`研究对象选择` vs `系统选取`？`受力分析` vs `分离体图`？
   - [ ] 修改具体 display_zh（列出 internal_id → 新 display_zh）
   - [ ] 接受现状

4. **别名补全**：你能至少给以下 5 个 high-traffic tag 各加一个别名吗？
   - newton_second_law
   - momentum_conservation
   - small_angle_approximation
   - free_body_diagram
   - symmetry_recognition

5. **category 归类争议**：以下条目在新分类体系下是否分类正确？
   - `superposition_principle` → heuristic（也许应为 math_technique？）
   - `analogy_mapping` → heuristic
   - `equivalent_circuit` → heuristic（也许应为 physics_model？注意 physics_model 现在指论文中的具体模型）
   - `newton_second_law` → heuristic（从旧 physics_model 移入。过于基础，也许应删除？）

## 修改后必跑

```
uv run pytest tests/test_index_builtin_vocab.py -x
```

## 不在此 review 范围

- 添加 candidate tag 流程（Plan 02-04 中的 candidate tag 机制会接住 LLM 提议的新 tag，无需在 builtin.yml 中提前枚举）。
- 用户私有词表（workspace.yml / private.yml）由用户自行维护，不在内置 review 范围。
- 完整物理学 taxonomy（被 RESEARCH.md §14 显式 defer）。
```

This note is short, scoped to the user's review burden, and stays in `.planning/phases/02-tag-indexing/`.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-tag-indexing/02-06-REVIEW-NOTE.md</automated>
    <automated>grep -c "^## " .planning/phases/02-tag-indexing/02-06-REVIEW-NOTE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-tag-indexing/02-06-REVIEW-NOTE.md` exists.
    - Contains ≥ 4 `## ` section headers (背景, 用户回答, 修改后必跑, 不在此 review 范围).
    - Contains the phrase `uv run pytest tests/test_index_builtin_vocab.py -x`.
    - References RESEARCH.md §13 R8 (or "[ASSUMED]") so the user understands why they are being asked to review.
  </acceptance_criteria>
  <done>Reviewer note exists and provides a structured agenda for the T3 human checkpoint.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_index_builtin_vocab.py -x` passes both before and after any T3 human edits.
- `uv run python -c "from cpho_cli.core.index.vocabulary import load_merged_vocabulary; from pathlib import Path; import tempfile; vocab = load_merged_vocabulary(Path(tempfile.mkdtemp())); assert len(vocab.tags) == 42"` exits 0.
- `grep 'vocabulary/\*\.yml' pyproject.toml` finds a match.
- T3 checkpoint receives an explicit "approved" or "modified" resume signal from the user.
</verification>

<acceptance_summary>
| must_have | verified by |
|-----------|-------------|
| 42 starter tags exist | test_builtin_vocabulary_loads + grep count gates |
| All categories represented | test_builtin_vocabulary_categories_have_expected_counts |
| Chinese UTF-8 round-trip | test_builtin_vocabulary_chinese_display_names |
| Alias index built correctly | test_builtin_vocabulary_alias_index_resolves_aliases |
| Package data ships YAML | grep 'vocabulary/\*\.yml' pyproject.toml |
| User reviewed content (R8) | T3 checkpoint resume signal |
</acceptance_summary>

<output>
After all four tasks pass, create `.planning/phases/02-tag-indexing/02-06-SUMMARY.md` recording:
- Final tag count and category breakdown.
- Reviewer resolution ("approved" or list of accepted changes).
- Note that Plan 02-04 will extend pyproject package-data again for `core/index/prompts/*`.
</output>
</content>
</invoke>