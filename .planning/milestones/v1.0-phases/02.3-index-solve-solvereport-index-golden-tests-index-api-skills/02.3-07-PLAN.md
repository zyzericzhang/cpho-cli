---
phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills
plan: "07"
type: execute
wave: 6
depends_on:
  - 02.3-06
files_modified:
  - src/cpho_cli/core/documents.py
  - src/cpho_cli/core/workspace.py
  - src/cpho_cli/cli/repl/session.py
  - src/cpho_cli/cli/repl/commands/set_cmd.py
  - tests/test_documents.py
  - tests/test_workspace.py
  - tests/test_repl_session.py
  - tests/test_repl_commands.py
autonomous: true
requirements:
  - D-18
  - D-21
  - SC-07
must_haves:
  truths:
    - "D-21: PNG, JPG, JPEG, WEBP, GIF, and PDF suffix handling is aligned between document loading and workspace discovery."
    - "D-18: SessionState can cache model capabilities for the current provider/model."
    - "D-18: `/set provider <name>` re-resolves provider config and refreshes model capabilities."
    - "Capability refresh failures do not crash the REPL; they record conservative text-only capabilities and a Chinese warning."
  artifacts:
    - path: src/cpho_cli/core/documents.py
      provides: "document suffix support including GIF as non-animated image input"
      contains: [".gif"]
    - path: src/cpho_cli/core/workspace.py
      provides: "workspace discovery suffix support aligned with document loader"
      contains: [".gif"]
    - path: src/cpho_cli/cli/repl/session.py
      provides: "session-level model_capabilities cache"
      contains: ["model_capabilities"]
    - path: src/cpho_cli/cli/repl/commands/set_cmd.py
      provides: "provider-switch capability refresh"
      contains: ["capabilities"]
  key_links:
    - from: src/cpho_cli/cli/repl/commands/set_cmd.py
      to: src/cpho_cli/core/llm.py
      via: "capability detection helper after provider change"
      pattern: "detect.*capabilit|model_capabilities"
---

<objective>
Finish multimodal workspace suffix support and REPL provider capability refresh as a small dependent slice.

Purpose: These changes depend on Plan 06 capability primitives but are separate from provider payload construction and solve routing, keeping the multimodal work safer to execute.
Output: GIF/document suffix alignment, SessionState capability cache, and `/set provider` refresh behavior.
</objective>

<execution_context>
@/Users/ericzhang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericzhang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-RESEARCH.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md
</context>

<must_haves>
See frontmatter `must_haves`; executor must satisfy those truths, artifacts, and key_links before writing the summary.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align document and workspace suffix support</name>
  <files>src/cpho_cli/core/documents.py, src/cpho_cli/core/workspace.py, tests/test_documents.py, tests/test_workspace.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
    - src/cpho_cli/core/documents.py
    - src/cpho_cli/core/workspace.py
    - tests/test_documents.py
    - tests/test_workspace.py
  </read_first>
  <behavior>
    - D-21: GIF is recognized consistently with PNG/JPG/JPEG/WEBP as a supported image suffix.
    - Existing PDF and image discovery behavior for Chinese nested workspaces remains unchanged.
    - Unsupported suffixes continue to produce existing validation errors.
  </behavior>
  <action>Add .gif to the project suffix lists where documents.py and workspace.py currently define supported image/document types. Treat GIF as a non-animated image input; do not add animation parsing. Update tests so document loading and workspace discovery include GIF without changing PDF/JPG behavior.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_documents.py tests/test_workspace.py -x` passes.
    - `rg -n "\\.gif" src/cpho_cli/core/documents.py src/cpho_cli/core/workspace.py tests/test_documents.py tests/test_workspace.py` finds suffix coverage.
    - No code converts PDFs to images for the OpenRouter path.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_documents.py tests/test_workspace.py -x</automated>
    <automated>rg -n "\\.gif" src/cpho_cli/core/documents.py src/cpho_cli/core/workspace.py tests/test_documents.py tests/test_workspace.py</automated>
  </verify>
  <done>D-21 suffix support is consistent at document loading and workspace discovery boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh capabilities in REPL provider sessions</name>
  <files>src/cpho_cli/cli/repl/session.py, src/cpho_cli/cli/repl/commands/set_cmd.py, tests/test_repl_session.py, tests/test_repl_commands.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md
    - src/cpho_cli/cli/repl/session.py
    - src/cpho_cli/cli/repl/commands/set_cmd.py
    - src/cpho_cli/core/config.py
    - src/cpho_cli/core/llm.py
    - tests/test_repl_session.py
    - tests/test_repl_commands.py
  </read_first>
  <behavior>
    - D-18: SessionState can cache model_capabilities for the current provider/model.
    - D-18: `/set provider <name>` re-resolves provider config and refreshes model capabilities.
    - Capability refresh failures do not crash the REPL; they record conservative text-only capabilities and a Chinese warning.
  </behavior>
  <action>Add an optional model_capabilities field to SessionState and update provider-setting command code to call the new capability detection helper after provider changes. Keep prompt_toolkit imports confined to cli/repl. Tests should monkeypatch capability detection and assert it is called on provider change; they should also assert a detection exception leaves the provider change handled with a warning and text-only capabilities.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_repl_session.py tests/test_repl_commands.py -x` passes.
    - `/set provider` tests prove capabilities are redetected when provider changes.
    - No core module imports prompt_toolkit.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_repl_session.py tests/test_repl_commands.py -x</automated>
    <automated>rg -n "prompt_toolkit" src/cpho_cli/core; test $? -eq 1</automated>
  </verify>
  <done>D-18 session-level and provider-switch capability detection are implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workspace file suffix -> processing route | Local filenames determine document parsing, OCR fallback, and multimodal eligibility. |
| REPL provider setting -> model capability cache | Interactive provider changes alter future routing behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-21 | Denial of Service | documents/workspace suffixes | mitigate | Add only D-21 suffixes and preserve unsupported-type validation; do not add GIF animation processing. |
| T-02.3-22 | Tampering | REPL capability refresh | mitigate | Treat detection failures as text-only and warn in Chinese rather than forcing multimodal routing. |
| T-02.3-23 | Information Disclosure | provider capability errors | mitigate | Reuse core/llm redaction behavior when surfacing detection failures. |
| T-02.3-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>uv run pytest tests/test_documents.py tests/test_workspace.py tests/test_repl_session.py tests/test_repl_commands.py -x</automated>
<automated>rg -n "\\.gif|model_capabilities" src/cpho_cli/core/documents.py src/cpho_cli/core/workspace.py src/cpho_cli/cli/repl/session.py src/cpho_cli/cli/repl/commands/set_cmd.py</automated>
<automated>rg -n "prompt_toolkit" src/cpho_cli/core; test $? -eq 1</automated>
</verification>

<success_criteria>
- D-18 REPL provider-switch refresh is covered.
- D-21 suffix support is covered outside the provider/solve multimodal slice.
- Plan 06 no longer owns REPL/session refresh or workspace suffix integration.
</success_criteria>

<output>
Create `.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-07-SUMMARY.md` when done.
</output>
