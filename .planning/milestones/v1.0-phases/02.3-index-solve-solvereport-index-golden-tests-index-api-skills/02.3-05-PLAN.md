---
phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills
plan: "05"
type: execute
wave: 4
depends_on:
  - 02.3-03
files_modified:
  - src/cpho_cli/core/solve.py
  - src/cpho_cli/core/skill_handlers.py
  - src/cpho_cli/builtin_skills/solve/skill.yml
  - src/cpho_cli/builtin_skills/solve/prompts/answer_structure.md.j2
  - src/cpho_cli/builtin_skills/solve/prompts/discrepancies.md.j2
  - src/cpho_cli/builtin_skills/solve/prompts/normalize.md.j2
  - src/cpho_cli/builtin_skills/solve/prompts/derive.md.j2
  - src/cpho_cli/builtin_skills/solve/prompts/cross_check.md.j2
  - src/cpho_cli/builtin_skills/solve/prompts/final_report.md.j2
  - tests/test_solve.py
  - tests/test_runtime.py
  - tests/test_skills.py
autonomous: true
requirements:
  - D-12
  - D-13
  - D-14
  - D-15
must_haves:
  truths:
    - "D-12: solve_problem delegates the seven-step builtin solve skill to SkillRuntime instead of running hardcoded one-call solve logic."
    - "D-13: cpho solve command shape remains unchanged because solve_problem signature remains compatible."
    - "D-14: shared llm and python_tool handlers exist; python_tool only performs blackboard string/data transforms and does not execute arbitrary Python."
    - "D-15: tests use fake providers and do not introduce a replacement golden eval framework."
  artifacts:
    - path: src/cpho_cli/core/skill_handlers.py
      provides: "shared SkillRuntime handlers for llm and python_tool"
      exports: ["make_llm_handler", "python_tool_handler"]
    - path: src/cpho_cli/core/solve.py
      provides: "runtime-backed solve_problem"
      contains: "SkillRuntime"
    - path: src/cpho_cli/builtin_skills/solve/prompts
      provides: "one prompt template per solve LLM step"
      contains: ["answer_structure.md.j2", "discrepancies.md.j2"]
  key_links:
    - from: src/cpho_cli/core/solve.py
      to: src/cpho_cli/core/runtime.py
      via: "SkillRuntime(handlers={'python_tool': ..., 'llm': ...}).run(...)"
      pattern: "SkillRuntime"
    - from: src/cpho_cli/builtin_skills/solve/skill.yml
      to: src/cpho_cli/builtin_skills/solve/prompts
      via: "prompt_template names"
      pattern: "answer_structure.md.j2|discrepancies.md.j2"
---

<objective>
Demote solve into the builtin skill runtime path.

Purpose: solve should validate the runtime/handler approach that later skills reuse, while keeping the existing `cpho solve` user interface stable.
Output: runtime-backed solve_problem, text-path llm/python_tool handlers, complete solve prompt coverage, and fake-provider tests.
</objective>

<execution_context>
@/Users/ericzhang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericzhang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-RESEARCH.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
@.planning/phases/01-core-foundation/01-04-SUMMARY.md
@.planning/phases/02.2-tui-repl-repl-tui-inserted/02.2-CONTEXT.md
</context>

<must_haves>
See frontmatter `must_haves`; executor must satisfy those truths, artifacts, and key_links before writing the summary.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement shared text-path SkillRuntime handlers</name>
  <files>src/cpho_cli/core/skill_handlers.py, tests/test_runtime.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
    - src/cpho_cli/core/runtime.py
    - src/cpho_cli/models/skills.py
    - src/cpho_cli/core/llm.py
    - src/cpho_cli/builtin_skills/solve/skill.yml
    - tests/test_runtime.py
  </read_first>
  <behavior>
    - D-14: llm handler renders a Jinja2 prompt_template from the skill's prompts directory, calls provider.complete(), parses strict JSON mapping output, and returns every declared output_key.
    - D-14: when output_keys is solve_report, llm handler validates provider content as SolveReport and returns the SolveReport object or JSON-compatible mapping expected by solve_problem.
    - D-14: python_tool handler supports extract_problem_answer by copying problem_text to raw_problem and answer_text to raw_answer; it does not import subprocess, eval, exec, or dynamic Python tooling.
  </behavior>
  <action>Create src/cpho_cli/core/skill_handlers.py. Implement make_llm_handler(provider, params, skill_dir, response_models=None) and python_tool_handler(step, values). Use Jinja2 StrictUndefined and the existing StepHandler contract from runtime.py. Parse non-final LLM responses as JSON objects containing the output keys; raise SkillRuntimeError when JSON is invalid or an output key is missing. For the final solve_report output, validate with SolveReport. Add tests that instantiate SkillRuntime with these handlers and fake providers.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_runtime.py -x` passes.
    - `rg -n "eval\\(|exec\\(|subprocess|__import__" src/cpho_cli/core/skill_handlers.py` prints no matches.
    - Tests prove llm handler uses provider.complete and returns missing-output errors through SkillRuntimeError.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_runtime.py -x</automated>
    <automated>rg -n "eval\\(|exec\\(|subprocess|__import__" src/cpho_cli/core/skill_handlers.py; test $? -eq 1</automated>
  </verify>
  <done>D-14 handlers exist and are constrained to provider calls plus local blackboard transforms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete solve skill prompt coverage</name>
  <files>src/cpho_cli/builtin_skills/solve/skill.yml, src/cpho_cli/builtin_skills/solve/prompts/answer_structure.md.j2, src/cpho_cli/builtin_skills/solve/prompts/discrepancies.md.j2, src/cpho_cli/builtin_skills/solve/prompts/normalize.md.j2, src/cpho_cli/builtin_skills/solve/prompts/derive.md.j2, src/cpho_cli/builtin_skills/solve/prompts/cross_check.md.j2, src/cpho_cli/builtin_skills/solve/prompts/final_report.md.j2, tests/test_skills.py, tests/test_solve.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
    - src/cpho_cli/builtin_skills/solve/skill.yml
    - src/cpho_cli/builtin_skills/solve/prompts/normalize.md.j2
    - src/cpho_cli/builtin_skills/solve/prompts/derive.md.j2
    - src/cpho_cli/builtin_skills/solve/prompts/cross_check.md.j2
    - src/cpho_cli/builtin_skills/solve/prompts/final_report.md.j2
    - tests/test_skills.py
    - tests/test_solve.py
  </read_first>
  <behavior>
    - D-12/D-14: each llm step in builtin_skills/solve/skill.yml references a prompt template whose variables match the step input_keys.
    - The seven solve step ids remain extract_problem_answer, normalize_problem, validate_answer_structure, derive_subproblems, cross_check_official_answer, mark_discrepancies, assemble_final_report.
  </behavior>
  <action>Add answer_structure.md.j2 for validate_answer_structure and discrepancies.md.j2 for mark_discrepancies. Update skill.yml so validate_answer_structure and mark_discrepancies use those specific templates instead of reusing normalize/cross_check. Adjust prompt variable names to match declared input_keys exactly. Add tests that load the builtin solve skill and assert every llm prompt_template file exists and renders under StrictUndefined with representative blackboard values.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_skills.py tests/test_solve.py -x` passes.
    - Every prompt_template listed in skill.yml exists under builtin_skills/solve/prompts.
    - No solve prompt uses undefined variable names under a representative render test.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_skills.py tests/test_solve.py -x</automated>
    <automated>uv run python -c "from pathlib import Path; import yaml; root=Path('src/cpho_cli/builtin_skills/solve'); spec=yaml.safe_load((root/'skill.yml').read_text()); missing=[s['prompt_template'] for s in spec['steps'] if s.get('kind')=='llm' and not (root/'prompts'/s['prompt_template']).exists()]; assert not missing, missing"</automated>
  </verify>
  <done>D-12/D-14 solve DAG prompt coverage is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route solve_problem through SkillRuntime</name>
  <files>src/cpho_cli/core/solve.py, src/cpho_cli/core/skill_handlers.py, tests/test_solve.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
    - src/cpho_cli/core/solve.py
    - src/cpho_cli/core/runtime.py
    - src/cpho_cli/core/skills.py
    - src/cpho_cli/core/ocr.py
    - src/cpho_cli/core/documents.py
    - tests/test_solve.py
  </read_first>
  <behavior>
    - D-12: solve_problem loads builtin_skills/solve via load_skill() and executes SkillRuntime with llm/python_tool handlers.
    - D-13: solve_problem keeps the existing public signature used by cpho solve; dry_run still validates inputs without provider calls.
    - D-15: tests use fake provider/OCR objects and do not call OpenRouter or golden eval.
  </behavior>
  <action>Replace the hardcoded one-call provider.complete path in solve_problem with SkillRuntime execution. Keep config/provider resolution and OCR extraction behavior from the existing function. Initial blackboard must include problem_text, answer_text, ocr_warnings, problem_path, and answer_path where available. Convert the final solve_report blackboard value into SolveReport if it is JSON/dict, then reuse _write_report(). Keep report_has_assertion unchanged. Update tests/test_solve.py to assert the seven runtime steps execute in order through fake provider responses and that the CLI-compatible dry_run path does not call provider.complete.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_solve.py -x` passes.
    - `rg -n "provider\\.complete" src/cpho_cli/core/solve.py` prints no matches outside handler construction or injected handler references.
    - `uv run cpho solve --help` succeeds and still shows problem, --answer, --config, --provider, --output-dir, and --dry-run.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_solve.py -x</automated>
    <automated>uv run cpho solve --help</automated>
    <automated>rg -n "provider\\.complete" src/cpho_cli/core/solve.py; test $? -eq 1</automated>
  </verify>
  <done>D-12, D-13, and D-15 are complete for text-path solve execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill YAML/prompt files -> runtime handlers | Local skill config controls prompt templates and step ids. |
| OCR text -> solve prompts | OCR text and answer text are untrusted prompt content. |
| LLM response -> SolveReport model | Provider output is untrusted JSON and must be validated before report write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-14 | Elevation of Privilege | python_tool handler | mitigate | Handler only implements named blackboard transforms; grep gate forbids eval/exec/subprocess/import escape hatches. |
| T-02.3-15 | Tampering | llm handler JSON parsing | mitigate | Validate output key presence and SolveReport schema before writing reports. |
| T-02.3-16 | Information Disclosure | runtime traces/provider errors | mitigate | Reuse redact_secrets patterns from core/runtime.py and core/llm.py when surfacing handler errors. |
| T-02.3-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>uv run pytest tests/test_runtime.py tests/test_skills.py tests/test_solve.py -x</automated>
<automated>uv run cpho solve --help</automated>
<automated>rg -n "eval\\(|exec\\(|subprocess|__import__" src/cpho_cli/core/skill_handlers.py; test $? -eq 1</automated>
</verification>

<success_criteria>
- D-12 through D-15 are covered for text-path solve.
- `cpho solve` remains user-compatible while internally using SkillRuntime.
- No new eval/golden framework is added.
</success_criteria>

<output>
Create `.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-05-SUMMARY.md` when done.
</output>
