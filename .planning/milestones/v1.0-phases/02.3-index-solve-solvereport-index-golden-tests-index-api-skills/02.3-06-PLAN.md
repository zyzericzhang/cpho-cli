---
phase: 02.3-index-solve-solvereport-index-golden-tests-index-api-skills
plan: "06"
type: execute
wave: 5
depends_on:
  - 02.3-05
files_modified:
  - src/cpho_cli/models/llm.py
  - src/cpho_cli/core/llm.py
  - src/cpho_cli/core/multimodal.py
  - src/cpho_cli/core/skill_handlers.py
  - src/cpho_cli/core/solve.py
  - src/cpho_cli/builtin_skills/solve/skill.yml
  - tests/conftest.py
  - tests/test_llm.py
  - tests/test_solve.py
autonomous: true
requirements:
  - D-16
  - D-17
  - D-18
  - D-19
  - D-21
  - SC-07
must_haves:
  truths:
    - "D-16/SC-07: cpho solve can send PDF files through OpenRouter Universal PDF Support using file content blocks when the model supports file input."
    - "D-17: LLMProvider.complete accepts OpenAI/OpenRouter-style multimodal message content lists as well as text strings."
    - "D-18: model capabilities are detected/cached for a provider session with conservative text-only fallback."
    - "D-19: reusable multimodal content-block helpers live in neutral core code, not only in skill_handlers."
    - "D-21: shared helpers can build image_url blocks for supported images and file blocks for PDFs without PDF-to-image conversion."
  artifacts:
    - path: src/cpho_cli/models/llm.py
      provides: "message content and model capability models"
      contains: ["ModelCapabilities"]
    - path: src/cpho_cli/core/multimodal.py
      provides: "neutral multimodal content-block helpers used by solve and index"
      exports: ["build_multimodal_content"]
    - path: src/cpho_cli/core/llm.py
      provides: "multimodal message typing and OpenRouter capability detection"
      contains: ["input_modalities"]
    - path: src/cpho_cli/core/skill_handlers.py
      provides: "shared skill LLM routing that consumes neutral multimodal helpers"
      contains: ["build_multimodal_content"]
  key_links:
    - from: src/cpho_cli/core/skill_handlers.py
      to: src/cpho_cli/core/multimodal.py
      via: "shared content-block helper import"
      pattern: "build_multimodal_content"
    - from: src/cpho_cli/core/solve.py
      to: src/cpho_cli/core/skill_handlers.py
      via: "problem_file/answer_file blackboard values"
      pattern: "problem_file|answer_file"
---

<objective>
Add the neutral multimodal LLM primitives and wire solve to use them through the shared skill handler.

Purpose: D-16 through D-19 make multimodal a reusable skill capability. This split keeps provider/content-block work away from REPL/session state and workspace suffix changes.
Output: multimodal LLM message support, OpenRouter capability detection, neutral content-block helpers, and solve PDF/image routing with OCR fallback.
</objective>

<execution_context>
@/Users/ericzhang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericzhang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-RESEARCH.md
@.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
@.planning/phases/01-core-foundation/01-04-SUMMARY.md
</context>

<must_haves>
See frontmatter `must_haves`; executor must satisfy those truths, artifacts, and key_links before writing the summary.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen LLM messages and add neutral multimodal helpers</name>
  <files>src/cpho_cli/models/llm.py, src/cpho_cli/core/llm.py, src/cpho_cli/core/multimodal.py, tests/test_llm.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-RESEARCH.md
    - src/cpho_cli/models/llm.py
    - src/cpho_cli/core/llm.py
    - tests/test_llm.py
    - tests/conftest.py
  </read_first>
  <behavior>
    - D-17: provider.complete serializes messages whose content is either str or a list of text/image_url/file blocks.
    - D-16: PDF blocks use OpenRouter file content with filename and data:application/pdf;base64 file_data; images use image_url data URLs.
    - D-18: capability detection reads OpenRouter /models metadata when available and falls back conservatively without leaking API keys.
    - D-19: content-block construction is reusable from core/multimodal.py or core/llm.py, not embedded only in skill_handlers.py.
  </behavior>
  <action>Add typed models or aliases for chat content blocks in models/llm.py, including text, image_url, and file blocks. Widen LLMProvider.complete and _OpenAICompatibleProvider.complete message typing accordingly without changing the payload key names. Add ModelCapabilities with input_modalities and supported_parameters plus helpers such as supports_image and supports_file. Create neutral helper code in src/cpho_cli/core/multimodal.py for base64 data URLs and OpenRouter PDF file blocks; skill/index code will import it. Add a core/llm.py function that queries OpenRouter models metadata with the existing httpx client pattern, extracts architecture.input_modalities and supported_parameters for the selected model, caches per provider label/model for the process, and redacts secrets in failures. Tests must use httpx.MockTransport; no live OpenRouter calls.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_llm.py -x` passes.
    - A test captures provider payload JSON and verifies image_url and file_data blocks are preserved exactly.
    - A capability detection test verifies image/file support from mocked architecture.input_modalities and verifies API keys are absent from errors.
    - `rg -n "def build_multimodal_content|file_data|image_url" src/cpho_cli/core/multimodal.py src/cpho_cli/core/llm.py` finds neutral core helpers and provider support.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_llm.py -x</automated>
    <automated>rg -n "image_url|file_data|input_modalities|ModelCapabilities" src/cpho_cli/models/llm.py src/cpho_cli/core/llm.py src/cpho_cli/core/multimodal.py tests/test_llm.py</automated>
  </verify>
  <done>D-16, D-17, D-18 provider detection, and D-19 neutral helper placement are implemented with fake-provider coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route solve multimodal inputs through shared handlers</name>
  <files>src/cpho_cli/core/skill_handlers.py, src/cpho_cli/core/solve.py, src/cpho_cli/builtin_skills/solve/skill.yml, tests/test_solve.py</files>
  <read_first>
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-CONTEXT.md
    - .planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-PATTERNS.md
    - src/cpho_cli/core/skill_handlers.py
    - src/cpho_cli/core/solve.py
    - src/cpho_cli/core/multimodal.py
    - src/cpho_cli/builtin_skills/solve/skill.yml
    - tests/test_solve.py
  </read_first>
  <behavior>
    - D-18/D-19: llm handler chooses multimodal blocks when blackboard contains problem_file/answer_file and capabilities support the required modality; otherwise it renders the OCR text prompt path.
    - D-16/SC-07: solve with a PDF and a file-capable fake model sends a `file` content block, not an image_url.
    - D-21/SC-07: solve with a supported image and an image-capable fake model sends an image_url block; text-only fake models use OCR fallback.
  </behavior>
  <action>Extend make_llm_handler in skill_handlers.py to accept or resolve ModelCapabilities and use build_multimodal_content from the neutral core module. Extend solve initial blackboard and skill.yml input_keys so llm steps can receive problem_file and answer_file references in addition to text values. The handler must not convert PDFs to images. If required modality is unsupported or capability detection fails, use the existing rendered text prompt path with OCR text. Add solve tests with fake capabilities for PDF file support, image support, and text-only fallback.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_solve.py -x` passes.
    - A solve test for PDF multimodal captures a `{"type": "file"}` block, not `image_url`.
    - A solve test for image multimodal captures `image_url` blocks.
    - A solve test for a text-only model proves OCR text appears in the prompt and no file/image block is sent.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_solve.py -x</automated>
    <automated>rg -n "build_multimodal_content|problem_file|answer_file" src/cpho_cli/core/skill_handlers.py src/cpho_cli/core/solve.py src/cpho_cli/builtin_skills/solve/skill.yml tests/test_solve.py</automated>
  </verify>
  <done>D-16 through D-19 and SC-07 are implemented for solve using the neutral multimodal helper surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local file -> remote OpenRouter API | User problem PDFs/images may be uploaded when multimodal routing is supported. |
| OpenRouter metadata -> routing decision | Remote metadata can be unavailable or incomplete; routing must fail closed to OCR text. |
| OCR text/file names -> prompt payload | File names and OCR text are untrusted content entering LLM messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-17 | Information Disclosure | solve multimodal file upload | mitigate | Only send file/image blocks when model capabilities support the required modality; text-only models use local OCR fallback. |
| T-02.3-18 | Tampering | OpenRouter capability metadata | mitigate | Treat missing/failed metadata as text-only; do not force multimodal payloads on uncertain support. |
| T-02.3-19 | Information Disclosure | provider/capability errors | mitigate | Redact API keys using existing redact_secrets pattern in all metadata/provider failures. |
| T-02.3-20 | Denial of Service | large/unsupported files | mitigate | Recognize only supported suffixes and keep downstream model file limits as provider errors with redacted messages; do not add PDF-to-image expansion. |
| T-02.3-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>uv run pytest tests/test_llm.py tests/test_solve.py -x</automated>
<automated>rg -n "image_url|file_data|input_modalities|ModelCapabilities" src/cpho_cli/models/llm.py src/cpho_cli/core/llm.py src/cpho_cli/core/multimodal.py src/cpho_cli/core/skill_handlers.py</automated>
<automated>rg -n "PDF.*image|pdf.*image" src/cpho_cli/core/multimodal.py src/cpho_cli/core/skill_handlers.py; test $? -eq 1</automated>
</verification>

<success_criteria>
- SC-07 is complete for solve multimodal routing and OCR fallback.
- D-16 through D-19 are covered without placing reusable content-block helpers only inside skill_handlers.
- Multimodal support uses existing dependencies only.
</success_criteria>

<output>
Create `.planning/phases/02.3-index-solve-solvereport-index-golden-tests-index-api-skills/02.3-06-SUMMARY.md` when done.
</output>
